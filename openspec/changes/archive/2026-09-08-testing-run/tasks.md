# tasks — testing-run（M10b）

- [x] T1 测试先行 A：D10 黄金矩阵落盘（预计 ~38，tests/ 目录形经 files 映射控制、单文件形钉 `.we` 命名），对当前构建运行必须先红
  来源：proposal 目标 1–8 / design D10 D12
  验证：`go test ./internal/conformance/ -run TestGoldenCases` 红（新增失败清单，既有 543 枚零回归），红因分类（we test exit 70 边界行 / 编译期既有诊断 / 锁定绿）记于完成记录；D8 表六枚既有黄金预期落点（翻绿三/回落两/维持余）实测定档

  **完成记录（2026-09-08）**：45 枚落盘 `internal/conformance/testdata/cases/`（test-* 前缀，bash heredoc 生成器一次写入；D10 ~38 → **45 定数**——runner 组 +e1907 锁定绿枚、clock 组以 now-tick 替域外真实钟[见下披露]，proposal 计数随 T8 收尾对齐）。分布：runner 14 / 边界与断言 9 / mock 7 / 虚拟钟 7 / 多函数 8。conformance 对账：总数 543 → **588**，**红 44 = 45 新枚 − 1 锁定绿**（失败清单双向对账：红 ∖ 新 = 空、全部 test-* 前缀），**既有 543 枚零回归**。D8 表六枚既有黄金 T1 阶段全绿（落点变更发生在 T3——翻绿三/回落两随批披露，`git status` 既有 testdata 零触碰为证）。
  **红因分类**：exit-70 边界行 42 枚（`we: test is not implemented in this reference build yet`——含 test-fn-generic-bnd/test-fn-body-bnd 两枚停点黄金与 test-asserteq-domain-bnd 域外枚[目标 70 换 What]与 test-run-compile-fail[目标 exit 2 E0501 诊断——红因同源皆边界行]）；usage exit 2 两枚（test-run-filter-subset/empty——`--filter` 未注册，parseOptions 先于 implemented 检查的直证：`we: unknown option "--filter"` + usage 四行）；**锁定绿 1 枚**：test-e1907-notfound（E1907 分发前触发对 test 子命令成立——M0 裁决的扩展锁定，stderr/exit 复用 build-e1907-human 逐字）。
  **实现自有面随黄金钉死（D6 收窄面的契约先行）**：人类报告行 `pass  {file}: {name} ({dur}ms)` / `fail  …` + 失败原因行缩进两空格 / 汇总 `total {N}, passed {P}, failed {F} ({D}ms)`；JSON 事件字段序 type 前置（`{"type":"test-result","file","name","status","duration_ms"}`）；**--json 模式失败原因走 stderr 行 `{file}: {name}: {reason}`（披露）**——ch21 R7 字段集封闭（file/name/status/duration_ms 四字段 + 只增不删改名承诺），加 reason 字段 = 单方面发明规范面；duration_ms = 虚拟钟差（advance(100) → 100ms 钉死）；死锁报文 `we: deadlock: 1 tasks parked with no wake source (the virtual clock only advances when a runnable task calls advanceTime)` + exit 1、stdout 空（计数 = 虚拟 park 任务数——D4 决议精化，驱动任务 park 在 await 非 clock 不计）；断言报文四形 `assertion failed` / `assertion failed: expected false` / `assertion failed: got 3, want 4` / `assertion failed: got "abc", want "abd"`（D7 词面）。
  **std.time 域外真实钟重分类（披露）**：D10 时钟组第 5 枚「域外真实钟 sleep(50) 真睡 + now 差 ≥50」byte-exact 黄金不可断言（≥ 断言无法钉字节）——移 T7 黑盒电池真探针承接；替位枚 test-clock-now-tick（advanceTime(50) → now 差恰 50——虚拟钟推进量的正钉）。域内时序断言全部放测试内部（assertTrue/assertEqual 形）保黄金字节确定。
  **形状约束**：一切用例省略 files 字段（产工件跳树对比——conformance 既有惯例）；项目形 setup = we.toml + tests/*.we 无 src/main.we（钉 test 根无 main 要求——E1305 不适用，D6）；单文件形文件在根；源码语法形状全部自既有黄金抄录（fn 段序 `effect tags -> Ret`、mock 段序 `-> Ret effect tags`、task/scope/`?` 形）；bnd What 三枚逐字与 M10a 常量原文一致（test-fn-record-param 用 record 透传形 + test 体内 String 字段读——非 String 字段读不在 M9b 发射集，emitFieldChainString 只支持 String 链，B1 面）。

- [x] T2 测试先行 B：单测先红——codegen m10b（槽发射形、fn ABI 全族矩阵 = D2 定形契约、harness 综合形、bndGenericFns/bndFnBody 停点、双模式入口、mock 体同 ABI、main 降格）+ typecheck m10b（std.time 装载面）+ cli m10b（发现排序/filter/退出码映射）
  来源：proposal 目标 1–7 / design D2 D3 D5 D11
  验证：`go test ./internal/codegen/ ./internal/typecheck/ ./internal/cli/` 红（新用例引用未定义符号/断言不匹配），red 证据记于完成记录；runtime C 钟状态机单测随 T4 先红（M9b 惯例：C harness 测试与实现同任务）

  **完成记录（2026-09-08）**：三文件落盘——`internal/codegen/m10b_test.go`（六函数：TestM10bBuildSlotsAndEntry 槽全局+真体符号+间接调用+入口例外 / TestM10bMainDowngrade test 模式 main 槽化+驱动占 __we_main / TestM10bStdEntrySlots io·test·time 三源槽+channel 构造面不槽化断言 / TestM10bFnAbiMatrix **D2 定形契约**六族[i64/float/string/gc record/sum/valueless]——define 行+调用实参形逐字 / TestM10bHarnessSynthesis 驱动序（mock 装/恢复、begin/end、wrap spawn 零捕获、await、report、summary）/ TestM10bBndStops 两新词）；`internal/typecheck/m10b_test.go`（三函数：签名与效果面[声明净/纯 E1401/实参 E0501/别名到达/裸名 E1304/task 自段/mock 字面绿]/用户同名 time 共存[装载门于模块键]/段序 E0105 锁定）；`internal/cli/m10b_test.go`（四函数：发现排序[path 序+文件内源序+tests/ 外不入+无 tests/ 空集]/filter 三形+非法 pattern usage 2/退出码映射[0/1/abort→1]）。
  **red 证据**：codegen 编译红 `undefined: ProgModule`（m10b_test.go:47）；cli 编译红 `undefined: discoverTests`（m10b_test.go:26）；typecheck 断言红两函数——TestStdTimeSignatures/TestStdTimeUserSameName 皆 E1302 std 形（`no standard-library module "std.time" exists in this build yet`——模块未注册的直接红证），TestStdTimeEffectSegmentOrder **锁定绿**（E0105 是 parse 期诊断先于装载，与注册无关——枚举时改判锁定绿记录）。
  **测试期勘误（D13 披露）**：段序枚期望列 3:28 数错（`effect` token 在 3:21）——先红揭出即修（黄金 test-fn-body-bnd 无此面）。**D2 表修正预告（T3 兑现 design 披露）**：value record 行 sret 形不启用——M9b 运行时统一堆表示下 value record 与 gc record 同走 ptr ABI，sret 行属过度设计（ABI 矩阵 gc record 行钉 ptr，注释载明）；sum 返回 `{ i64, i64 }` 聚合值返回维持。

- [x] T3 codegen 程序级发射：D1 EmitProgram + 模块限定符号 + fn 表并图、bndOtherFns/bndTestModule 删 + bndGenericFns/bndFnBody 新、D2 调用 ABI 族、D3 槽形（std fn 条目入槽/构造面与特型面无槽/入口例外）、build.go EmitProgram 接线与 M10a 单文件 test 路由删
  来源：proposal 目标 1 / design D1 D2 D3 D8
  验证：`go test ./internal/codegen/` 绿（T2 套件 + 既有 IR 对拍族更新面逐枚披露）；conformance build 族黄金翻绿对账（D8 表逐枚——翻绿三/回落二）；`we build .` 既有绿黄金零回归

  **完成记录（2026-09-08）**：`internal/codegen/codegen.go` 程序级面全落——EmitProgram 两遍（pass one 逐模块收 sums/records/fns/import 面；pass two 入口体先行[fresh 从 v0 起，单 main 黄金字节不变]后逐 fn define，快照-恢复协议镜像 emitTask）；per-module import 面（modImports/modStd/modConc + enterModule 换装 + resolveQual/resolveStd 双解析序，程序模块键优先于 std 键）；fn ABI 六族（fnAbiKind + fnAbiOf 分类器 + fnDef 惰性 ABI 缓存[classify 在被调模块分类后回 caller 语境]）；槽物化 slotFor 首次调用点（镜像 strPool），fn 槽 `@<key>.<name>`、std 槽 `@__we_*`，根 build main 不入槽（入口例外）；emitCall 分派序落定（裸 Ident：panic/cancel → record 构造桥 → fn 表 → bnd；Member：concAlias primCtor → prims → caps → resolveQual fn 表 → resolveStd std 表 → bnd）；record/sum 一律带模块键（`%struct.main.Greeter`/`@.map.main.Greeter`）。
  **测试对账（既有面逐枚披露）**：codegen 包 25 既有测试全绿——m10a_test.go **删除**（39 行退役，引用 bndTestModule/bndOtherFns 两已删边界词）；m8 五快照更新（hello/print 键形 + `@slot.io.println` 槽 + `%v0 = load ptr, ptr @slot.io.println` 间接调用，披露注置于 m8HelloIR 前；record/nested/err 三快照 record 键化 + 槽化 load；TestM8LocalImportBoundary 重写为擦除等价断言[local import 后 io 回退解析 → IR == m8HelloIR]）；m9a 一例翻面（"fn containing concurrent forms" bndOtherFns → **bndFnBody**，值形 fn 无尾 Return 的停点换词）；codegen_test 一例翻绿（"extra module function"——值形空体 helper 定义 `define void @main.helper()` + 槽，what 空）；m10b float pin 修正（`(double 0x3FF8000000000000)` 裸 hex 形）。modules_test TestLoadGraphBuildBoundary → **TestLoadGraphBuild 翻新**（多模块 + helper fn 构建成功：exit 0 + artifact + .ll 含 `define void @util.helper()`/`@slot.util.helper`/`define i32 @__we_main()`——bndOtherFns 退役的 cli 面对账）。
  **conformance 黄金随批更新（D8 表逐枚）**：**翻绿三**——build-bnd-fn/build-bnd-conc-fn/build-bnd-multi-module exit 70 → 0、stderr 行撤（conc-fn 的 files pin 同撤——成功构建产二进制工件树不可字节 pin，与 build-json 等成功黄金的无 files 惯例对齐）；**回落二**——build-bnd-test-module/build-bnd-test-fns-first stderr 换 `we: single-file builds (spec gap; roadmap follow-up)…`（bndTestModule/bndOtherFns 行退役后单文件 test 模块 build 统一落工件命名边界，exit 70 不变，files pin 保持源集）。五枚 case 名保留为 D8 对账锚点。全量 conformance：**红 44 = T1 红名单全员，既有绿黄金零回归**（543 基线无新失败；当前绿 544）。
  **cli 接线**：loadProject 第四返回值 count → `[]typecheck.Module`（调用方 len()+1）；buildProject 组装 `[]codegen.ProgModule`（滤 std. 键，根 `{Key:"main", ID:name}` 末位）→ `codegen.EmitProgram(ModeBuild, …)`；runBuild 单文件 IsTestModule 路由删（:36-49，M10a 中间态退役——单文件 test 模块 build 面即命名边界）。**三纯函数前移 T3**（T2 打破 cli 包编译的修复）：新文件 `internal/cli/test.go` 落 discoverTests（tests/ 下 *_test.we、path 序+源序、无目录空集）/filterTests（regexp 非锚、非法 pattern exitUsage）/mapTestExit（nil→0、余→1）+ discoveredTest——cli 包全绿（T2 四函数翻绿）。
  **实现缺陷修复（D13 披露，均先红后修）**：① numImmediate 浮点双前缀——原返 `"double 0x…"` 而一切消费者（store/fadd/fcmp）再拼类型 → 潜在坏 IR，无任何既有黄金 pin 过浮点路径（首次暴露即 M10b float pin）；修为裸 `0x%016X`，消费者不变。② emitLetBinding Construct 绑定存裸记录名 → fieldSlotOf 键化查表 miss → 字段链误停边界（m8 record 两快照红）；修：emitConstruct 改返 (reg, recKey, ni)，绑定处存模块键。③ layout key 语义混用——fieldSlotOf/render 传完整 recKey 而 fkRef 解析需模块 key（拼出 `main.Wrapper.Point` 查表 miss 拖垮整 layout）；修：recModKey 切分（记录名无点，LastIndex 安全）。④ m10b_test FnAbiMatrix gc record 例 RecordDecl 缺 `Cat: "gc"`（T2 测试笔误——构造协议只收 gc record，M8 面如此；测试侧补正）。
  **T2 预告兑现（D2 披露）**：value record 同 gc record 走 ptr ABI（sret 行不启用）——FnAbiMatrix gc record 行钉 `define ptr @main.same(ptr %u)`，注释载明；sum 返回 `{ i64, i64 }` 聚合值返回维持（Result prelude 键 Ok/Err、用户 sum 按 sumsOrd 序 idx）。
  **中间态预期红（T5 材料）**：codegen m10b 三函数诚实红——TestM10bMainDowngrade（缺 `call void @__we_test_begin()`——driver 未合成）、TestM10bStdEntrySlots（TestDecl 体不发射 → 无槽物化）、TestM10bHarnessSynthesis（缺 test/mock defines）；typecheck 两函数红为 T4 面（StdModule("time") 未注册）。`go vet ./...` 清、`go build ./...` 过。

- [x] T4 运行时虚拟钟：D4 test.c（钟状态机/__we_test_begin/end/__we_advance FIFO/__we_time_now/__we_time_sleep）+ sched.c deadline 双源分派与虚拟域死锁 + std.time 第四枚（typecheck StdModule("time")）+ D7 断言助手（__we_assert_true/__we_assert_eq_i64/__we_assert_eq_str）
  来源：proposal 目标 3 4 6 / design D4 D7
  验证：runtime C harness 套件绿（钟状态机/FIFO/双源/作废/助手报文）；`go test ./internal/typecheck/` std.time 套件绿；既有 runtime 套件零回归（M9b 真实钟路径 real 模式原样）

  **完成记录（2026-09-08）**：`runtime/c/test.c` 新建（~230 行：钟状态机三全局 + sleep 等待源[FIFO 单链、幂等 unlink、唤醒者摘链属主清理] + begin/end 边界 + advance + now 双源 + 四断言助手）；sched.c 五触点（we_scope 增 virtual 位、`__we_scope_enter` 按 `__we_test_virtual()` 分派 deadline 时钟、`__we_expire_deadlines` 转公开每 scope 读己钟、`earliest_deadline` 跳过虚拟 scope、空闲循环折入 sleep deadline + 虚拟域死锁 exit 1）；sched.h 双段跨文件声明（test.c 建于其上的任务账面 + 钟查询面）；runtime.go embed TestSource；sched_test 5 例 + conc_test 6 例编译集补 test.c（链 sched.c 即链 test.c）；build.go 三清单加 rt-test.c（`we build .` 烟测过——rt-test.o 入链接、二进制跑绿）；typecheck StdModule("std.time") 第四枚（now 带 M8 尾产值体、sleep 空体，装载门于模块键）。
  **运行时两策略决议（design D4/D9 随批增补）**：① **越刻屏障**——advanceTime 拨钟前先排空就绪队列（`__we_sched_drain`、新任务态 WE_DRAIN 的 FIFO 屏障队列、嵌套 advance 按到达序应答）：ch20:121 场景「a task … waits … registered on the virtual clock and the test calls advanceTime past it」读 = 等待须先登记；无屏障时 spawn 后未及运行的任务把 sleep 登记到已拨后的钟上、永不到期（T1 黄金 advance-release/fifo/scope-timeout 三枚源序皆 spawn→advance，无屏障必死锁——黄金矩阵倒逼的策略面）。② **残留作废到达面**——合法 We 源中 passing test 留不下未 join 任务（E1618 任务必在 scope 内 + E1607 路径分析 + scope 退出即 join）——弃置的到达形 = 失败测试（panic 中途穿域、scope 无人 leave）。
  **T1 黄金源缺陷与随批修正（D13 披露，四枚全为 T1 自有新增、实现前修正）**：① **E1618 裸 task**——四枚钟黄金测试体顶层裸 task 触 ch18:101 无条件 E1618（ch20 测试体是 function body context 非 scope block，无豁免表述——规范裁决后修源不修 checker）：advance-release/same-tick-fifo 加 scope 包裹、scope-timeout-virtual 补 adv 外包裹，三枚期望 stdout 不变。② **residual-discard 语义不可满足**——原源（passing test + 未 await 慢任务）在 E1618+E1607 下不可达；重构为失败测试形（scope 内 spawn 慢任务 + assert(false,"boom") + return 穿域 discharged）：exit 0→**1**、stdout 改 `fail … quick (0ms)\n  boom\npass … next (0ms)\ntotal 2, passed 1, failed 1`——「late 不打印」语义由弃置兑现（下一测试不受扰面不变）。③ **初版清扫语义偏差（本任务内自纠）**——T4 首版 `__we_test_end` 用 `__we_handle_cancel`：取消唤醒任务、剩余语句仍执行（「late」会打印），违设计 D4「永久弃置」与黄金；改 `__we_task_abandon`（新任务态 WE_ABANDONED：等待链标 dead + READY 摘就绪队列 + 屏障位摘屏障队列 + PARKED 停原处永不再调度）。typecheck m10b task-sleep 例同步 scope 包裹翻绿。
  **实现细节披露**：sleep 等待节点走扩展节点协议（calloc 自家节点 + `__we_link_attach`——`__we_link_new` 只配裸 40 字节 link，ASan 实证越界 8 字节后即修）；虚拟域死锁计数 = 活跃虚拟 sleep 注册数（`__we_virtual_parked`），0 时回落 count_parked()（纯通道死锁面诚实计数），报文括注两态皆悬；断言报文四形经 `__we_task_fail` 存消息、驱动 await 读出（D7 词面、实现自有）；std.time now 声明体带尾产值 `{ 0 }`（FnDecl.Body 值类型——空体 + Ret 触 E0501，M8「合成模块是可检查的 We-writable 源」纪律）。
  **T2 盲写测试修正（D13 披露）**：① E0501 期望列 19→24（检查器锚实参表达式 exprPos 惯例）；② TestStdTimeUserSameName 直调 CheckProject 须自带 `{Key:"std.time"}` deps（装载器 loadGraph 会放、直调不会）；③ main 调 probe 须声明 effect time 段（E1401 是正确行为——测试源补段）。
  **验证对账**：runtime 套件全绿（M10b 八测试：状态机/FIFO/双源/scope 虚拟 deadline/弃置/越刻屏障/助手报文/死锁 + M9b 零回归）；typecheck 全绿（std.time 三函数 + 既有面）；conformance 544 绿 / **红 44 = T1 红名单全员**（名单 diff 双向核对）；`go build ./...`、`go vet ./...`、`gofmt -l` 清；`we build .` 烟测绿（rt-test.o 链接 + 运行）。**中间态预期红（T5 材料）**：codegen m10b 三函数仍诚实红（TestM10bMainDowngrade/TestM10bStdEntrySlots/TestM10bHarnessSynthesis——候 T5 发射面）。

- [x] T5 harness 与 test 边界：D5 合成驱动（mock 槽装填/恢复、per-test spawn+await、TaskPanic 判败、report/summary 接线）、TestDecl/MockDecl 发射（test fn + mock fn 同 ABI）、emitBody fn 语境参数化
  来源：proposal 目标 2 5 / design D5 D3
  验证：`go test ./internal/codegen/` harness 套件绿；conformance 边界/mock/钟族黄金部分翻绿对账（we test CLI 面候 T6——中间态预期记档）

  **完成记录（2026-09-08）**：`internal/codegen/codegen.go` 塔全落——pass one 逐模块收集 testRef（ModeTest、模块序+源序、Path 空时 `ReplaceAll(key,".","/")+".we"` 反推）；emitTestTower 每测试三 define（test fn `define void @<key>.test.<n>`、mock fn `define <retTyp> @<key>.mock.<n>` 目标 ABI 无槽、wrapper `define internal i64 @<key>.wrap.<n>(ptr %env)` 直调返 0 入 e.thunks）；emitDriver 渲染 driveStep 序作 `__we_main`（mock 源序装填 `store ptr @…mock.N, ptr @slot.<target>` → begin → task_new → alloca payload → await → end → 槽恢复真体 → icmp 分支双 report[pass 六参 reason null / fail 读 Err payload inttoptr] → 汇合 → summary + `ret i32 0`）；render ModeTest/build 统一 `define i32 @__we_main()` 真体（占位注释退役）。**mock ABI 双路**：程序 fn 目标 enterModule(目标键) 后 classify(fd)（重述签名在测试模块解不了 qualified record 名）；std 条目目标用 mock 自身重述签名 fnAbiOf（基标量/String 任何模块可分类）。**test 体语境**：checker 视 test 体为 valueless fn 语境（fnRet=nil、"(test)"、任意深度 valueless return 合法）——emitter 以 testBody 标志镜像（emitTestReturn：`ret void` + 死续块 tr<n>），emitBlockStmts/emitTestDefine 顶层/emitScope 块语句三处判 Return，fn 体纪律零拓宽（!testBody → e.bnd()）；emitTask 不快照 testBody（task 体自为 thunk 协议语境）。**裸名分派**：panic/todo/assert/advanceTime 四形（assert 双参 condI64→br→失败分支 NUL 串 task_fail+unreachable；advanceTime 直呼 __we_advance 无槽）；st.assertEqual 形状判族（string Literal/strEnv Ident/Member 字串链 vs 数值对）双助手分派。**declareLines** 追加七行（两 eq 助手/advance/begin/end/report 六参/summary）。
  **位置局部调用面拓宽（披露，四处——M10b 黄金硬需求 `st.assertEqual(add(2,3),5)`/`return double(double(n))` 等倒逼；M9b 表达式发射器保持零调用[调用仍须经 let 绑定入表达式位]，解析在各位置内部）**：① assertEqual 比较位——Call 参数先 emitCall 按结果族归路（ckI64→数值助手、ckStr→字串助手、Float64/其他→停点）；② fn 尾返回值（fnRetVal）——i64/double/str 三族收直接调用（str 走 insertvalue 配对），gc/sum 族维持形状钉死；③ emitFnCall 实参位——参数族自身的调用结果直传（i64/double/str，嵌套调用 `double(double(n))` 面）；④ test 体内 scope 块的 valueless return（emitScope 语句环镜像 emitBlockStmts 纪律）。黄金对账：全语料扫 `return f(` 与嵌套调用形——**零黄金以调用入表达式位为停点**（build-bnd-test-fns-first 的 exit 70 是单文件构建命名边界非嵌套调用；ch12 系命中皆 check 模式；Err(Failed(…)) 系是入口 Result 面不走 emitFnCall）；test-fn-generic-bnd 停点经 emitCall 解析层仍触发（TestM10bBndStops 钉）。
  **实现缺陷修复（D13 披露，先红后修）**：① **str 参数返回非法 IR**——fnRetVal abiStr Ident 尾产出 `ret { ptr, i64 } { ptr %s0, i64 %s1 }`：LLVM 的 ret 聚合常量不容 SSA 操作数，须 insertvalue 构建寄存器；T2 ABI 矩阵只钉 define/调用行，此路径从未编译过（e2e 探针 clang 链接首揭）——修：SSA 对（Ident 绑定与 Call 结果两路）走 `insertvalue undef` 两步、字面量路径（全局指针+常量长度）合法聚合维持；回归钉 TestM10bStrParamRet。② **emitDriver 固定寄存器名 %c**——多测试同函数多块重定义非法 IR；修：走 e.value() fresh 计数器。③ **T2 盲写两枚（测试侧缺陷，非实现缺陷，随批修正）**——TestM10bMainDowngrade build 子测 wantNoIR 锚 `@slot.helper.main` 与紧邻 wantIR 同串自相矛盾（意图是根 main 入口无槽，修锚 `@slot.main.main`）；TestM10bStdEntrySlots 期望 `@slot.time.sleep` 但测试体从未调 sleep（槽按首引用物化，修测试体补 letDiscard 调用）。
  **端到端预验（SCRATCH 探针，已删）**：`internal/codegen/scratch_e2e_test.go` 临时探针——EmitProgram(ModeTest) → prog.ll + 六 runtime 源 → pinned clang -c/链接 → 跑、对 T1 黄金逐字节（探针头注明 conformance 于 T6 接管后面）；**14 场景全绿**（单过/双败续跑/JSON 三行+stderr 原因/钟推进释放 100ms/残留弃置/mock 同模块 helper/mock println 拦截/死锁 abort 退出 1/空集/chain 嵌套调用/跨模块 pub fn/字串参数/record 参数+Member 字串链/真跨模块 import util 限定调用）——探针正是①②④三缺陷的首揭面。删前新增四枚**永久 IR 契约钉**（m10b_test.go）：TestM10bStrParamRet（insertvalue 形）/ TestM10bNestedCallArg（三槽调用计数）/ TestM10bAssertEqualCallComparands（双助手分派形）/ TestM10bReturnInScope（scope 内 ret void）。
  **验证对账**：`go test ./internal/codegen/` 全绿（T2 六函数翻绿 + 新四钉 + 既有 25 零回归）；`go build ./...`/`go vet ./...`/`gofmt -l` 清；conformance **红 44 不变**（stash 前后双向核对——全部 test-* 前缀，T6 前预期红名单全员；`we test` 未接线无新翻绿，既有绿零回归）。

- [x] T6 we test CLI：D6 test.go 全链（发现排序/并图编译/单二进制构建/执行/转达/退出码 0-1-2 与 abort→1/--filter 烘进综合/--json argv 透传/单文件临时工件）+ cli.go 路由
  来源：proposal 目标 7 / design D6
  完成（2026-09-08）：
  **发现一走（一事实一位置）**：`collectTests(dir)` 是唯一的 tests/ 走法——WalkDir 路径序收 `*_test.we`、解析后以 `Rel(dir,p)` 斜杠形挂 `testFile{Path, File}`；失败面外携（parse 诊断 / parse 边界 What / 走法自身 error，三者皆非即成功）。T2 纯面 `discoverTests` 改为委托（`testNames` 映射同享），行为零变（T2 钉全绿）。
  **编译败映射 exit 2**：新常量 `exitCompileFailure = 2`（cli.go——与 exitUsage 同数异名：两者皆「从未跑」类，ch21:113 编译败从不跑测试）。run 链内三类失败面重映射：loadManifest/loadGraph 的 exitDiagnostic → 2（诊断已渲染）；CheckTestRoot 诊断 → report+2；collectTests parse 诊断 → report+2。**边界（70）与依赖边界原样透传**；E1907 维持 dispatch 前 exit 1（黄金钉）。manifest 校验共享（loadManifest(dir,false)）而**库项目不停**——D6 未列库边界于 test 面（其可执行是合成 harness 非工件类型之别），字面读披露。
  **--filter 烘进综合**：`applyFilter` CLI 侧匹配（filterTests 既有面）后**剥除非匹配 TestDecl 再进编译**——被滤出的坏测试不扰滤后运行（空匹配 = 零测试驱动、`total 0`、exit 0，黄金钉）；剥除走 File 浅拷贝换 Items（Docs 索引可悬垂——检查面在剥除后跑于保留集、codegen 不读 Docs，披露）。
  **单文件面**：`runTestSingle`——loadFile（std.*-only 既有单文件面）→ 同 applyFilter → 单模块 `EmitProgram(ModeTest)`（Key "main"、Path = 所给路径斜杠形——报告行即 `timer_test.we: clock` 黄金形）→ `MkdirTemp` 临时目录编译（base+".test"）用后 RemoveAll——单文件无 manifest 不立 build/；非 `_test.we` 单文件 = 该文件 test 块为零 = 空跑 exit 0（D6 字面读，黄金钉）。
  **项目面**：每 test 根 `loadGraph(dir, key, path, file)`（key = path 去 .we 斜杠转点；导入映射 src/ 复用）+ `CheckTestRoot`（无 main 约定）；dep 跨根**按键去重**后组装（滤 std. 键——发射器 std 表自有调用面）；test 模块 ID = manifest 名（`; ModuleID` 头是项目自己的程序）；**零测试集仍编译运行**——裸根 `{Key:"main", File:&ast.File{}}` 保程序非空，驱动零步 → `total 0` exit 0（黄金钉两枚：空 tests/ 与滤空）。
  **构建执行**：build.go 提取 `compileProgram(buildDir, base, ir)`（写 .ll + 六 runtime 源 + 钉版 clang 三步序——buildProject 逐字迁移改调，行为零变）；项目形工件 `build/<name>.test`（中间物留检视，同 build 面）；子进程 `runTestBinary`——stdio 接注入流、`--json` 经 argv（startup.c main 读之切 test.c 双形渲染）、cmd.Dir=项目目录（单文件形继承 cwd）、ExitError → mapTestExit（0→0、1→1、abort→1）、余 error → fsError。
  **T1 黄金修正一枚（披露）**：test-fn-body-bnd 源文 `fn looped()` 调 io.println 未声明 `effect io`——检查阶段（先于 codegen）合法先发 E1401，黄金原期 70/bndFnBody 不可达。**修源不修期**：fn 声明补 `effect io`，黄金仍钉 bndFnBody@70（矩阵意图——generic/domain 两枚同族边界的运行面钉保留）；E1401 检查面另有既有覆盖。此为 T1 自有新增黄金的实现前修正，按纪律披露。
  **验证对账**：`go test ./internal/cli/` 全绿（T2 三纯面钉零回归 + 既有 build/check/modules 套件）；conformance **588/588 全绿**（44 红全员翻绿：45 枚 test-* 含 e1907 先绿——红因分类归位：报告/时序/断言/mock/边界/过滤/JSON/单文件各族尽绿）；`go test ./...` 九包全绿；`go build ./...`、`go vet ./...`、`gofmt -l` 清。
  验证：`go test ./internal/cli/` 绿；conformance runner 面黄金翻绿对账；红名单逐枚清点（T1 红因分类归位）

- [x] T7 黑盒电池：真二进制逐面——`we test .` 全链（过/败/混/空/编译败四退码）、虚拟钟时序探针（虚拟 sleep 大值瞬回 vs 真实墙钟）、mock 拦截真跑（cache 形 + println 拦截）、`--json` 逐字段（ch21 R7 schema）、`--filter` 两形、abort 转达、`we build .` 与 `we check .` 零回归
  来源：proposal 目标 1–8 / design D12 阶③
  完成（2026-09-08）：真二进制 `go build ./cmd/we`（we 0.1.0 / spec 0.9.0，pinned clang 20.x）逐探，14 探针全绿，逐字记档：
  **①全链四退码**：过（exit 0，`pass  tests/a_test.we: one (0ms)` + `total 1, passed 1, failed 0 (0ms)`）；败（exit 1，`fail  …: bad (0ms)` + 缩进原因行 `  boom`）；混（exit 1，先败后过续跑——`fail … first fails` → `pass … second ok` → `total 2, passed 1, failed 1`）；空 tests/（exit 0，`total 0, passed 0, failed 0 (0ms)`）；编译败（exit 2，stdout 空、stderr `tests/bad_test.we:4:23: error[E0501]: mixed types — …`——零测试跑）。
  **②虚拟钟时序**：测试域内 `time.sleep(1000000000)` 由兄弟任务 `advanceTime(1000000000)` 释放——全程墙钟 **397ms**（含编译链接；纯执行亚毫秒级瞬回），报告行 `pass  tests/clock_test.we: virtual big sleep (1000000000ms)`——duration_ms 是虚拟钟差（10⁹），非墙钟：域内域外两个时间世界各守其位。
  **③域外真实钟（T1 递延探针归位）**：`we run .` 普通程序（非 harness）`time.now() → time.sleep(50) → time.now()`——**真实 CLOCK_MONOTONIC 睡眠**：程序内 `assert(b - a >= 50, "clock did not move")` 过 → exit 0；工件裸执行墙钟 56ms ≥ 50ms。虚拟化只在测试边界内（`__we_test_begin/end` 之间 `test_virtual` 置位），域外 time 面走真实 deadline——T1 记档的「≥ 断言无法钉字节」以黑盒形承接闭合。
  **④mock 拦截真跑**：值形（`mock read() -> Int64 { return 7 }` + `assertEqual(read(), 7)` → exit 0）；println 形（`mock io.println(s: String) effect io { return }` 后 `io.println("REAL")`——stdout **无 REAL 行**，mock 吞掉真调用，exit 0 过）。
  **⑤--json 逐字段**：三行 `test-result`{type,file,name,status,duration_ms} × 2 + `test-summary`{type,total,passed,failed,duration_ms}——字段集与 ch21 R7 schema 逐字相等（python 断言 set 相等 + 值相等 + duration_ms 整型），失败原因走 stderr 人形（`tests/m_test.we: bad: nope`）。
  **⑥--filter 两形**：`--filter beta` 与 `--filter=beta` 输出 diff 空（逐字节同）：`pass  tests/m_test.we: beta (0ms)` + total 1。
  **⑦abort 转达**：虚拟域死锁（test 内裸 `time.sleep(100)` 无 advanceTime）——运行时 `we: deadlock: 1 tasks parked with no wake source (the virtual clock only advances when a runnable task calls advanceTime)` stderr 原样转达，CLI **exit 1**（abort 归运行失败非编译失败，D6 决议）。
  **⑧零回归**：`we build .`（p7 真实钟项目）exit 0 工件可执行；`we check .` exit 0（`we: check passed (2 modules)` --verbose 形）；单文件三面——`we test timer_test.we`（报告行 `timer_test.we: clock (0ms)` exit 0）、`we test plain.we`（零测试集空跑 exit 0）、非 .we 既有文件（`we: test wants a .we file, got "notes.txt"` exit 2）；不存在的路径 E1907 exit 1（dispatch 前解析，黄金钉）。
  验证：电池逐探结果记于完成记录（exit 码与 stdout/stderr 逐字）

- [x] T8 全量终验：`go clean -testcache && go test ./...` 全绿、gofmt/vet 清、`validate --all --strict` 过、docs_sync 对数不变、T1 红名单逐枚翻绿零回归、conformance 总数对账（543 + N）
  来源：proposal 目标 8 / design D12
  验证：命令输出记于完成记录

  **完成记录（2026-09-08）**：
  **clean-cache 全量**：`go clean -testcache && go test ./...` 九包全绿（cli/codegen/conformance/runtime/typecheck/parser/diag/version/cmd——conformance 38.7s 冷跑 588 黄金，runtime 9.0s C harness 编译集）；`gofmt -l`（滤 refr/）空、`go vet ./...` 清、`go build ./...` 过。
  **openspec 校验**：`validate.py testing-run --strict` → `OK: 1 change(s) valid; registry clean; mode=strict`；`--all --strict` 同 OK。
  **docs_sync 对数不变（基线歧义判明记档）**：变更创建态基线与实现后工作树两态相等——`239 failure(s) across 31 paired document(s)` 两跑逐字同（stash 仅跟踪面对基线、pop 后全工作树复量）。先前量得的 +3（236 vs 239）成因：`git stash --include-untracked` 把**未跟踪**的 testing-run 三工件（变更脚手架，按用户约定中文单语）整体卷走，量出的是「无活动变更」错误基线；HEAD 的 openspec/changes/ 只含 archive 可证。正确基线（变更创建态 = HEAD 跟踪面 + 脚手架在位）即 239，三枚 `missing paired translation` 自变更创建即在册，实现周期零扰动——对数不变成立。AGENTS.md 修改（前窗口编译器定位段落）为无配对单语 md，两态计数贡献恒 1，不入差异。
  **T1 红名单归位**：44 枚全员翻绿（T6 记录在案；红名单 diff 双向核对——红 ∖ 新 = 空、翻绿后 test-* 前缀失败为零），既有 543 基线零回归。
  **conformance 总数对账**：543 + **45** = **588**，588/588 全绿；45 分布 runner 14 / 边界与断言 9 / mock 7 / 虚拟钟 7 / 多函数 8（T1 定数，含 e1907 锁定绿一枚）。
  **proposal 计数对齐（T1 承诺兑现）**：三处 ~38 → 定数 45（目标 8 行 / 实施面 tests 行 / 对账预期行，543 → 588 一并载明）。

- [x] T9 实现审查：welang-code-review 7 条（真二进制证据），发现项修复或披露（行文原误写 welang-change-review——七条实现审查技能是 welang-code-review，M10a 同款；审查记录内披露）
  来源：研发流程 + design D12
  验证：审查记录（7 条逐条 + 发现项处置）追加于 proposal.md

- [x] T10 归档与 roadmap 手术：archive-sync 五步（status → archived、目录带日期前缀）；M10b 行翻 done（双语）；follow-up 登记：channel mock 检查/运行张力重分类裁决（proposal 风险节 / design D3）
  来源：proposal 目标 8 + 研发流程
  验证：gofmt -l 空、go build/vet 过、validate --all --strict 过、docs_sync 对数不变、git diff --check 干净；归档目录存在、roadmap M10b 行 done、follow-up 行在册

  **完成记录（2026-09-08）**：archive-sync 五步——① 规范提升零（纯实现路径，无 specs/ 增量可并）；② 决策提升不设新 ADR（M9b/M10a 先例：槽形取舍与虚拟钟双世界的长期载体是 roadmap B1 行 + 本变更 design/审查记录，未达 ADR 级架构承诺——ADR-0002/0003 先例是宿主策略与 GC 策略级）；③ roadmap M10b 行翻 `done 2026-09-08`（en/zh 两文件）；④ `mv openspec/changes/testing-run openspec/changes/archive/2026-09-08-testing-run`（目录未跟踪，mv 非 git mv——git mv 只作用于已跟踪路径）+ change.yaml `status: archived`；⑤ 复验全套过。
  **follow-up #14 登记（双语）**：「并发构造面为 mock 目标」——E1804 不可 mock 集未点名并发构造器：M10a 检查面字面接受 `mock conc.channel`、运行面无从拦截（期望类型派生实参非签名可复述），M10b 维持检查面零改 + channel 不入槽；重分类裁决点（E1804 点名原始构造器 vs 章文固定拦截面）随册，发现于 M10a 审查 F1、design D3 披露。
  **验证对账**：`validate.py --all --strict` → `OK: no changes found (nothing to validate); registry clean`（active 目录清空之谓）；docs_sync `239 failure(s) across 31 paired document(s)` 对数不变（md 集仅移位）；gofmt -l 空、`go build ./...`/`go vet ./...` 过、`git diff --check` 干净；归档目录 `archive/2026-09-08-testing-run/` 在位、roadmap M10b 行 done、follow-up #14 行在册（双语）。

- [x] T11 记忆更新与提交：project-overview 增 M10b 条目（下一里程碑指向 M10c testing-explore）、MEMORY.md 索引行更新；英文提交信息（无署名 trailer；refr/ 不入提交；等用户明示提交）
  来源：研发流程 + 用户红线
  验证：`git log -1` 消息为英文且无 Co-Authored-By；`git show --stat` 无 refr/ 路径

  **完成记录（2026-09-08）**：project-overview.md 增 testing-run (M10b) 条目（程序级 codegen/槽拦截/虚拟钟双世界/harness/we test CLI 四裁决、conformance 543→588、实现期修正披露、T7 电池 14 探针、T8 基线判明、T9 F1/F2、follow-up #14、下一里程碑 M10c）+ MEMORY.md 索引行更新（M0–M10b、conformance 588）。**提交前实况（`git status` 对账）**：跟踪改动 24（改 23 + 删 1[m10a_test.go]——AGENTS.md 除外 23 项）+ 未跟踪 52（黄金 45 + m10b 单测 ×4 + test.go + test.c + 本归档目录）+ AGENTS.md（+14 行——前窗口编译器定位讨论产物，与本变更无关，单独提交），`git status --porcelain | grep refr/` 零命中——与 proposal 涉触码盘点（含审查 F1 补行）逐项对齐。用户明示提交（2026-09-08），提交信息 `Land M10b testing-run: chapter 20/21 run towers`（英文、无署名 trailer）；AGENTS.md 定位节另笔提交。
