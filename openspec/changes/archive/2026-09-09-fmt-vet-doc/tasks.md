# tasks — fmt-vet-doc（M11）

- [x] T1 测试先行 A：D7 黄金矩阵落盘（预计 ~35，fmt-*/vet-*/doc-* 全 byte-exact），对当前构建运行必须先红
  来源：proposal 目标 1–8 / design D7
  验证：`go test ./internal/conformance/ -run TestGoldenCases` 红（新增失败清单，既有 606 枚零意外回归——48 枚随批更新属 T4 后的预期红，非 T1 面），红因分类记于完成记录；D7 各组逐枚对账落点
  完成记录（2026-09-09）：**35 枚落盘（fmt 16 / vet 14 / doc 5），34 红 + 1 绿哨兵，既有 606 枚零回归**（`git status internal/conformance/testdata/` 零既有文件改动，35 枚全为新增未跟踪——负面对账过）。
  **红因三类**：① **exit 70 not implemented**（不带旗标的 fmt-*/vet-*/doc-*——`we: {cmd} is not implemented in this reference build yet`）；② **exit 2 旗标前置**（doc --check/--output 枚——选项解析先于子命令分发，`unknown option "--check"`，M10c ① 同类）；③ **exit 0 vs want 1**（vet-e1903-check-face——`[vet]` 表今日无读取面，check 照常过，E1903 死文字直证，M10c ② 同类）。
  **绿哨兵一枚（预期）**：fmt-bad-flag——`we fmt . --width 80` 今日即 usage 2（cli.go:94 parseOptions → :117 implemented 检查的分发序），T3 后恒绿，钉死「fmt 零选项」非目标。
  **D7 逐组对账**：fmt 基础 4/4（pass 综合脏→净[脏源含 160+ 字符长行 + 手工紧贴邻接——净输出单行不折行不对齐的负向证明]/tabs+CRLF+BOM[单文件面]/间距/already-clean）；fmt 结构 5/5（空行坍缩/空块 `{}`/import 分组[std 组前置 + 未解析本地 import 照常——fmt 不跑模块解析的隐式负钉]/注释随行/闭包管 vs or 管）；fmt 边界 6/6（解析失败逐文件独立/E1905/单文件面/--verbose[stdout `we: formatted`，build 先例形]/坏旗标/空文件保持空）；fmt 定点 1/1（setup=fmt-pass 净输出断言零改写——与 pass 枚传递合成 fmt(fmt(x))==fmt(x)；真双跑归 T7）；vet 触发 6/6（w1910 发/mocked 静默/w1911 绑定穿参/w1912 五操作一坑[trySend/tryReceive 不入面同钉]/跨码锚位与消息逐字[位置序 11:9→12:5]/helps=注册表 remediation 逐字）；vet 配置 5/5（提升停 build 无工件/提升 test exit 2[同项目 W1912 提升为 error + W1910 留 warning 的混排]/ignore 抑制/E1903 非法值[规范场景 W1912="strict"]/check 面共享）；vet 运行 3/3（净项目 0/W 行入 check 输出 exit 0[R2 直证]/--json W 事件 schema 含 help）；doc 5/5（双模块页形[围栏签名 ch6 序 + `///` 原文]/pub-only[私有 fn/record 不入页]/--check 缺口行 exit 1 无页面/--check 净/--output 重定向）。
  **源验证（黄金源须过编译器——M10c 教训）**：全部 35 枚 setup 源对 base 二进制（we 0.1.0）跑过——vet/doc 源 check/test 全绿；fmt 脏源逐枚 check 探针：12 枚 GREEN、3 枚 E1302（fmt-pass/fixpoint/import-* 系未解析 `import util`——**解析后**的模块解析失败，parse 面净）、1 枚 E1905（fmt-e1905 本意）、fmt-parse-error 的坏文件单文件探针 E0105 报文逐字捕获（`pub fn (` → 1:8）。
  **锚位口径**：W 码锚 = 被调方法名/函数名 token 位（D4）；锚位逐枚人工重核（`    sem.acquire()` → col 9 等首算笔误当场改）。消息与 helps 从 diagnostics.toml :1496/:1505/:1514 逐字转录；usage 四行从 m10c 黄金逐字对齐。
  **设计期三处补全（先于实现，非实现后追认）**：D3 间距表闭括号/开括号通则补行 + `#[` 死行删（零批准面）+ 规则优先级（括号零 > 管一）；D7 定点枚改对偶传递证明（零 runner 改动）；verbose 形定 `we: formatted {path}`/`we: wrote {path}` stdout（build `we: built` 先例）。计数 ~33 → 35（坏旗标 + 空文件两枚审查期补入），proposal 目标 8 / design D7/D8 三处同步。
- [x] T2 测试先行 B：单测先红——lex keep 模式（注释 trivia/词类不变/BOM-CRLF 同 Scan）、fmt 重排（深度/间距表/空行/import 组/注释随行/定点性）、advisory 收集（三触发正负例）、cli m11（vet/doc 旗标、[vet] 门、fmt 文件集、doc 选项解析）
  来源：proposal 目标 1–6 / design D1–D6
  验证：`go test ./internal/lex/ ./internal/fmt/ ./internal/typecheck/ ./internal/cli/` 红（新用例引用未定义符号/断言不匹配），red 证据记于完成记录
  完成记录（2026-09-09）：四测试文件落盘、四包全编译红，红因 = 符号未定义恰为设计面——
  **lex m11_test**（4 用例）：`Keep`/`KindComment` 未定义。钉死面：注释 token 三枚（`// lead` 1:1 / `// trailing` 2:11 / `/// doc` 3:1——含 `///` 在 Keep 面同为 comment 的 D1 裁决）；非注释流与 `Scan` 逐 token 全等（kind/text/line/col）；BOM 剥除 + CRLF 行账同 Scan；词法错报文同码同位穿透。
  **fmt fmt_test**（8 用例，新包）：`Format` 未定义。表驱动 8 组（间距/空行坍缩/空块/闭包管/import 组/注释随行/tabs+CRLF+尾白/字符串逐字）逐组断言脏→净 + **定点性直测**（Format(Format(x))==Format(x)——比黄金对偶传递更强）+ **token 行数不变量**（非空行数守恒——import 组分隔空行是空白策略非 token，裸行数不变量不成立，测试口径当场修正披露）+ 解析错原样返回 E0105@1:8 + 空文件保持空 + 注释内文逐字。
  **typecheck m11_test**（3 用例）：`Advisories` 未定义。W1910 正（save 8:5 消息含 tag）/mock 负/test 外调用负；W1911 正（touch(m,v) 12:26）/异绑定负/E1613 直访形不负（不重复发）；W1912 五操作逐锚（16:19/18:9/19:8/20:8/21:16——m11Head 三行使行号较黄金移一，首写照抄黄金 17–22 笔误当场改）+ 恰五枚断言（trySend/tryReceive 不入面的同测负钉）。
  **cli m11_test**（4 用例）：`docOutput`/`docCheck`/`vetPosture`/`collectWeFiles` 未定义。doc 双旗标两形式+last-wins+缺值 usage（`we: --output wants a directory`）+ doc 专属域（check 面仍 unknown option）；[vet] 表 8 案（三合法值/三键并置/strict 与裸值 E1903 报文逐字/无表/杂键不校验——键面 `vet.W19xx` 同 [test] 前缀机制）；posture 映射含 nil 缺省 warning；collectWeFiles 文件集（build/ 子树全排、散置 .we 收、非 .we 排、字典序）。
  **测试源侧两笔误当场修**：Go 源中字符串内裸 BOM 非法（`illegal byte order mark`——仅文件首字符可裸）→ \uFEFF 转义；W1912 锚位行号偏移。源验证（黄金源须过编译器）：W1912 单测源以 base 二进制 check 绿（`effect io` 裸标签无需 import std.io 的判据顺带实证）。
- [x] T3 fmt 塔：lex.Keep 第二入口 + internal/fmt 行保结构重排器 + cli runFmt（项目面 WalkDir 排除 build/、单文件面、逐文件独立、差异写回、E1905 一致门）
  来源：proposal 目标 1 / design D1 D2 D3
  验证：`go test ./internal/lex/ ./internal/fmt/ ./internal/cli/` 绿；黄金 fmt-* 组翻绿对账；既有 606 枚零回归（fmt 不触管线命令）
  完成记录（2026-09-09）：三面全落——lex.Keep（注释 KindComment trivia、`///` 同为 comment、非注释流与 Scan 逐 token 全等、BOM/CRLF 同账）；internal/fmt（Keep 面 + parser AST 分类 + 行保结构重排：深度模型/邻接表/import 提升/空行策略，从不折行从不对齐）；cli runFmt（目录 = 项目 `loadManifest(dir,false)` 同 E1905 门 + `collectWeFiles` 排除根 build/ 子树字典序、`.we` 单文件面无门、逐文件独立 parser.Parse 预门 d→report+continue / ni→exit 70、差异写回 0o644、`--verbose` 仅改写时 `we: formatted {path}`）。验证全过：三包单测绿、fmt-* 16 枚黄金全绿（含 fmt-bad-flag 哨兵恒绿）、既有 606 零回归（全量失败集恰 18 = 13 新 vet + 5 新 doc，皆 T4/T5 面）。
  **计数对账**：盘上 fmt/vet/doc 前缀 36 枚 = T1 新 35 + 既有 vet-e1907-human（M10c 遗产、属 606、E1907 于 implemented 检查前触发故 T1 起即绿——T1 完成记录的 34 红未单列此枚，如实补记）；conformance 总数 641 = 606 + 35 ✓。
  **D13 缺陷披露（黄金笔误修复）**：fmt-pass 的 want 于 add 体写 `return Ok()`——脏源两处皆 `Ok(())`、main 体 want 亦 `Ok(())`、重排器设计上不删 token；T1 手写 want 笔误，同步污染 fmt-fixpoint 的 setup（自该 want 抄录）。两枚修复为 `return Ok(())` 后 fmt-* 组全绿；非静默重写——T1 本里程碑新黄金（未提交）+ 此处披露授权源。
  **顶层空行策略定形（两钉冲突的实现内化解，零测试改写）**：T2 单测钉顶层相邻 let 不插空行（closure_pipe 用例）、fmt-pass 黄金钉相邻顶层声明（effect→type、fn→fn）插空行——初版「项间恒插」实现使单测红。裁决为「**声明项分隔、let/var 连排聚簇**」（与 import 块聚簇同构；unitStarts/headKind 机制 + 注释 run 边界永落 run 上方）——两钉同过，黄金与单测皆未动。
  **邻接表两处机械闭合披露**：① `<`/`>`/`>>` 取「Binary Op 位 = 比较，否则 typeAngle 零贴」的默认闭合分类（黄金 `Result<(), AppError>` 零贴角为必需；嵌套 `Vec<Vec<Int64>>` 的 `>>` 单 token 非 Binary 位整体零贴天然覆盖）；② 关键字贴 `(`（`return (x)` 面）规范与黄金皆未钉——按 D3 括号行「恒零附加」的机械读法统一取零（uniform 调用形），此面留待后续规范钉名。
  **cli 双解析披露**：fmtFiles 以 parser.Parse 作 ni 边界预门（Format 包签名无 ni 面，文档化为调用方自门）→ 每文件两遍解析，R11 重正确轻性能接受。
  **小 API 件前移 + 提前绿披露**：cli 包 m11_test 的 T4/T5 API 面（docOutput/docCheck 字段、doc 专属 `--output`/`--check` 解析、vetPosture、[vet] 门）随 T3 落地使 cli 包可编译全绿；附带效应：vet-e1903-check-face（T1 归 T4 组）**提前翻绿**（[vet] 门落 loadManifest，check 面直证共享）。
  **parseManifest 引号元数据**：T2 钉的裸值拒绝面（`W1910 = error` → E1903 got error）需区分引号性——reader 增第三返回值 bare 集（值面语义零变：裸值照样剥引号入 map，M10c `= 5`/`"many"` 行为原样，m10c 测试全绿佐证）；[vet] 门以 bare 或值非法拒绝，报文/At/help 同黄金逐字。
  **遗留披露**：`gofmt -l` 修 T2 遗留 fmt_test.go 字段对齐；`go vet ./...` 此刻仍红于 typecheck 包（T2 先红件 `Advisories` 未定义属 T4 面）——T4 落地后复验。
- [x] T4 advisory 塔：diag.Warning 构造器 + typecheck advisory.go 三触发收集 + loadManifest [vet] 门（E1903 三键三值）+ 全管线接线（check/build/run/test 报 W 不停/提升停/抑制）+ cli runVet
  来源：proposal 目标 2–5 / design D4 D5
  验证：`go test ./internal/typecheck/ ./internal/cli/` 绿；黄金 vet-* 组翻绿对账；48 枚既有黄金转红（预期红——Q4a 载明，逐枚红因属 W1912/W1910 新行，清单落 T6）
  完成记录（2026-09-09）：**五面全落**——diag.Warning/AsError（severity 渲染层：提升改渲染层、码保持 W）；typecheck/advisory.go（helps 三常量 = 注册表 remediation 逐字 :1500/:1509/:1518、pendingW1910、adviseUnmocked/settleW1910、adviseIndirect 穿透走查、adviseBlocking、Advisories/AdvisoriesProject/AdvisoriesTestRoot 三入口）；checker 四字段（advisories/advRoot/advInTest/advPend）；五钩点（fnCall 加 mod 参数（""=活模块；importCall 传 canonical 键——W1910 身份机与 E1805 一处两用）、checkTestDecl advInTest/advPend 换新+块尾 settle、callType 尾 adviseBlocking、nestedAccessCheck 内 adviseIndirect、三入口于根 ingest 前置 advRoot——std 体/组合子体/依赖模块同钩静默）；cli advise（posture 三态渲染：ignore 弃置/warning 如实/error 渲染 error 码保持 W——**渲染全部后才停**，promoted+shown 双轴）+ projectAdvisories + loadProject 增回传 manifest + check/build/run/test 四面接线 + vet.go 两面（目录/单文件；无 toolchainGate，设计 D5 与 check 同形）+ subcommands 翻位。
  **[vet] 门与 E1903 面已于 T3 前移落地**（T3 记录披露，vet-e1903-check-face 提前绿）；T4 补齐的是收集机与全管线。
  **D13 披露（T2 测试件两笔误，实现期揭出）**：① advisoriesOf 以 "test.we" 名 parse——parser 的 test 块文件名门（E1801 须 `*_test.we`）使 W1910 用例不可解析（T2 时包编译红不可运行故未察）；改 "m11_test.we"。② wantAdvisory 首码匹配语义——同码多枚（W1912 五枚）下逐元组断言自第二枚起必 Fatal；改为码+消息部件联合定位。两修皆测试期工件笔误，钉的意图（码/消息/锚位/严重度）零变。
  **advisory 段范围定形披露**：check/build/run/vet = src 根图先 + collectTests 发现的 test 模块后（每模块自身 root 过自身依赖图；src+test 混排次序无黄金钉，取 src 先）；test = 各 test root（其循环内 deps 已在手零重载）+ 单文件面（过滤后集合——advisory 段随编译面）；check/vet/test 单文件面无 manifest → posture 恒缺省 warning、无一可提升；build/run 单文件面停于 boundary（exit 70）先无 advisory 段（该面无工件管线，披露）。
  **双走查披露**：Advisories* 与 Check/CheckProject/CheckTestRoot 同 walk 各跑一遍（AST/依赖图复用零重复 parse/load；R11 重正确轻性能）。
  **W1911 两处机制披露**：① 直访面（`m.update` 于 m 回调内）无需显式 carve-out——checkNestedAccess 先行、E1613 先炸停查，adviseIndirect 自然不至（嵌套闭包内 E1613 边界外则照发——advisory 正是补其所不能见）；② 穿透范围 = 回调闭包子树全量函数体（嵌套闭包/task 体/scope 体/select case 体，参数遮蔽逐层继承）——设计点名嵌套闭包，其余同理由延展，无黄金反证。
  **48→46 对账**：proposal Q4a 的 48 为 grep 五操作拼写的量面预估（run-conc 27/check-green 11/test 10）；T4 落地实红 **46**（run-conc 33 / check-\*green 9 / test-\* 8 / build-bnd 1——build 面为预估分类外新见）。全量 diff 机械校验：46 枚的每条 diff 行非空者皆含 W 码（110× W1912 + 1× W1910），无 W 码的 diff 行与 exit 70 全属 doc-\* 5 枚（T5 面）——**零意外回归**；逐枚清单与偏差定数落 T6。
  **验证**：`go test ./internal/typecheck/ ./internal/cli/` 绿（T2 四文件全绿）；`go vet ./...` 绿（T3 遗留复验清账）；`gofmt -l` 零；conformance 失败集恰 51 = 46 预期红 + 5 doc（T5 面），vet-\* 组 15 枚（14 新 + 既有 vet-e1907-human）全绿、fmt-\* 16 枚保持绿、既有 606 除 46 预期红外零回归。
- [x] T5 doc 塔：cli runDoc（check 管线先行 + pub-only 渲染 + 每模块一 Markdown + --output/--check + 旗标解析）
  来源：proposal 目标 6 / design D6
  验证：`go test ./internal/cli/` 绿；黄金 doc-* 组翻绿对账；--output/--check 面 usage 2 形钉死
  完成记录（2026-09-09）：**两面全落**——internal/cli/doc.go（~560 行：runDoc 分派形同 check、docModule 文档集、runDocProject = loadProject + projectAdvisories + advise（promoted→1 无页面，doc 也是管线命令 Q4）+ 根图滤 std.\*（与 build 程序面同滤，根 main 先 deps 后）；runDocSingle = loadFile + advise(nil) + 该文件自身集；docSet = --check 零生成缺口行 stderr 平文 `we: undocumented pub declaration {name} at {file}:{line}`（不入册不受 --json）exit 0/1 / 默认面 docs/{key}.md 平坦点分写盘 + --output 重定向根 + --verbose `we: wrote {path}`；MkdirAll 仅写面）；cli.go doc 翻位 implemented + dispatch 组。
  **渲染机**：pubDocItems 六类 pub 源序（fn/let/record/newtype/sum/interface；pub effect 与 impl 不入面——design D6 载明）；页形 H1 `# Module {key}` + 每项 `\n### {name}\n\n` + 围栏规范签名 + `///` 原文围栏外（lex 侧已剥 `/// ` 前缀，`strings.Join(lines, "\n")` 直用——Docs 索引按 Item 序号对位）；fn 形无体 ch6 序（effect 段在返回类型前、tag 空格分隔——`effect db net` ch16 源形）；接口成员两空格缩进一员一行、默认体丢弃（页载契约非实现）；record 空字段 `{}`、byval/byres 前缀、derives 尾、where 子句（bound `Subject: Iface + Iface2` / equality `Subject.Assoc == TypeRef`）、构造更新 `with &base` 形（ch8 黄金源实证）。
  **渲染披露面（无黄金钉的机械形）**：① fn 类型引用的 tag 段 = 裸形（ch7 `fn(String) db -> ()` 无 effect 关键字——check-ch16-closure-arg 源实证，与 fn 声明的 `effect db` 段两形）；② effect tag 显示走 displayTag 同规则（末段）——typecheck 的 displayTag 未导出，cli 侧同判据镜像（小重复，R11 取）；③ 接口 assocs 先于 methods（AST 两列分置，源内交错不保真）；④ renderExpr 全量覆盖 17 种表达式 + 全部语句/模式（pub let 初始化式任意合法表达式可渲染、多行体折叠 `{ a; b }` 单行形）——页脸永不见无可拼写形。
  **单文件面三探针（真二进制）**：默认面页落 {file} 所在目录 docs/{基名去 .we}.md、--check 缺口行 exit 1、非 .we usage 2 `we: doc wants a .we file, got %q`（与 check/vet/test 同形）。
  **验证**：`go test ./internal/cli/` 绿（T2 四文件 + 既有全绿）；`go test ./...` 除 conformance 外全绿；conformance 失败集恰 **46** = T4 预期红（doc-\* 5 枚全部离开失败集翻绿，fmt-\* 16 / vet-\* 15 保持绿）。今日桶分精确定数：run-conc 29 / check-\* 8 / test-\* 8 / build-bnd 1 = 46——**T4 记录桶分（33/9/8/1，和 51）混入了当时失败总数（46+5doc）**，逐枚清单与偏差披露落 T6。
- [x] T6 随批更新：48 枚既有黄金的 W1912/W1910 新行（WE_UPDATE_GOLDEN 重生成 + 逐枚 diff 过目；W 行与源内五操作调用位/未 mock 自定义效果对账）
  来源：proposal 裁决 Q4a / design D5
  验证：conformance 全量绿；48 枚清单与 proposal 载明数逐一对齐（偏差定数披露）；零预期外文件改动
  完成记录（2026-09-09）：**实红 46 枚重写（非 48——偏差见下），conformance 全量 641 枚绿**。重生成前 46 枚原文备份、重生成后机械 diff 校验：**每处差异皆纯 W 行新增——W1912 110 / W1910 1 / W1911 0，共 111 行**；human 面落 stderr、--json 面落 stdout 事件（test-explore-json 6 条 W1912 warning 事件 schema = vet-json-w-event 钉形：type/severity/code/message/file/line/column/help）；**零 exit 改动、零非事件 stdout 改动、零 files 改动、零行删除**。
  **逐枚对账（源 ↔ W 行）**：每枚 setup 源内五操作拼写数（`.acquire(`/`.wait(`/`.send(`/`.receive(`/`.await(`——trySend/tryReceive 拼写天然不撞）== 该枚新增 W1912 行数，46 枚零失配；W1910 唯一枚 = test-explore-e1902（tests/gate_test.we test 体调 `save()`（effect db）无 mock——恰为该枚场景本意）；W1911 零枚（既有黄金无穿参场景，触发面由 T1 新黄金 + T2 单测钉）。
  **逐枚清单（46 枚 / 新增 W 数）**：build-bnd-conc-fn 1；check-advancetime-task-scope-green 1、check-conc-cancel-signal-green 1、check-conc-cond-sem-green 2、check-conc-select-await-green 2、check-conc-select-green 4、check-conc-task-pure-main-green 1、check-conc-task-scope-green 1、check-conc-taskhandle-annot-green 1（check 桶 8）；run-conc-cancel-coop 1、chan-close-drain 3、chan-full-park 5、chan-rendezvous 4、chan-send-after-close 2、chan-send-recv 4、chan-tryrecv 1、cond-broadcast 5、cond-signal 3、deadlock 6、defer-task 1、gc-park-survival 4、interleave 4、match-option 2、scope-collectall 2、scope-plain 2、scope-stmt 1、scope-timeout-err 2、scope-timeout-ok 1、select-await-source 2、select-int 3、select-two-chans 3、sem-basic 1、sem-blocking 3、sem-overrelease 2、sig-await 1、taskpanic-await 1、timeout-question 2、two-tasks 2（run-conc 桶 29）；test-clock-advance-release 1、test-clock-same-tick-fifo 2、test-clock-scope-timeout-virtual 2、test-explore-e1902 1（W1910）、test-explore-json 6、test-explore-no-reduce 6、test-explore-order-found 6、test-explore-reduce-merges 6（test 桶 8）。
  **偏差定数披露（48 → 46）**：proposal Q4a 的 48 为 grep 五操作拼写的量面预估（run-conc 27 / check-green 11 / test 10）；落地实红 **46**（run-conc 29 / check-\*green 8 / test-\* 8 / build-bnd 1）——预估为手工桶分粗计，非逐枚清单；build 桶为预估分类外新见（build-bnd-conc-fn——advisory 段先于 boundary）。另：**T4 记录的桶分（33/9/8/1，和 51）有误**——其计数混入了当时失败总数（51 = 46 预期红 + 5 doc 面本里程碑新黄金），本记录的 46 分桶为备份-重生成机械清点定数。
  **git 对账**：`git status` 既有 cases 改动恰 46 枚 M、零预期外文件；35 枚 T1 新黄金保持未跟踪待提交。
- [x] T7 黑盒电池：真二进制逐面探针——fmt 脏→净真机复现与定点性、vet 三触发真机行形、[vet]=error 停 build 无工件、doc 页面真机生成与 --check 缺口行、--json W 事件 python 断言、既有无告警面回归抽检
  来源：proposal 目标 1–6 / design D5 D6
  验证：电池探针全过，逐探针输出记于完成记录
  完成记录（2026-09-09）：`go build ./cmd/we` 真二进制逐面探针，六面全过——
  **① fmt 脏→净与定点性**：tabs+多空格脏源 fmt pass 1 归净（import 提升、间距归一、体缩进 2 空格）；pass 2 输出与 pass 1 **diff identical**（真机双跑定点性）；already-clean pass 3 exit 0 静默、--verbose 无改写零输出。电池探针源三处当场笔误（裸 `println`（须 io 限定）致 check 探针 E1304、`m.update` 返回未绑定致 E0605、python 断言行号差一）——探针工件不入库，逐处修正后复跑，非实现缺陷。
  **② vet 三触发真机行形**：同项目三触发齐发——`src/main.we:8:26: warning[W1911]: possible indirect nested access — m is passed to helper inside its own callback` / `src/main.we:9:8: error[W1912]: Channel.send may block`（[vet] W1912="error" 提升渲染 error、码保持 W） / `tests/gate_test.we:8:5: warning[W1910]: unmocked custom effect — save carries custom effect db`；渲染序 = src 根图先 test 模块后（advisory 段范围定形的真机直证）。
  **③ [vet]=error 停 build 无工件**：同上项目 `we build .` exit 1、**无 build/ 目录**（提升停于工件产出前）。
  **④ doc 页面真机（项目面）**：含 W1912 warning 的项目 doc 照常生成页面（warning 不停 doc——Q4 一致）；页形 `# Module main` + AppError/main（无 ///）+ add（`/// Adds one.` 围栏外）；`doc --check` 先渲 W 行再报缺口行 `we: undocumented pub declaration AppError at src/main.we:3` / `main at src/main.we:5`（add 有 /// 不报）exit 1。单文件面三探针已记 T5（默认页/缺口行/非 .we usage 2）。
  **⑤ --json W 事件 python 断言**：恰一枚事件，`type=diagnostic / severity=warning / code=W1912 / file / line=7 / column=8 / message 含 Channel.send / help 非空` 七面全断言过。
  **⑥ 既有无告警面回归抽检**：纯项目（无五操作/无 test）`check / build / test` 三面 exit 0 零输出（test 为空集 total 0 形）。
- [x] T8 全量终验：D8 阶梯固定序全绿 + 对账（refr/ 零触碰、既有黄金改动恰 T6 清单、conformance 总数对齐 proposal 计数）
  来源：proposal 目标 7 8 / design D8
  验证：`go build ./...`、`go vet ./...`、`go test ./...`、runtime C harness、conformance 全量、validate --all --strict、docs_sync、gofmt -l、git status 对账——全绿记录
  完成记录（2026-09-09）：**九步全绿**——① `go build ./...` OK；② `go vet ./...` OK；③ `go test ./...` 全绿（唯 conformance 包亦绿，见⑤）；④ runtime C harness `go test ./runtime/ -count=1` ok（12.6s，`git status runtime/` 零改——确认性重跑）；⑤ conformance 全量 fresh `-count=1` ok（**641 枚 = 606 既有 + 35 新**，总数对齐 proposal 目标 8；46 枚在位更新不增减总数）；⑥ `validate.py --all --strict` OK（1 change valid; registry clean）；⑦ `docs_sync.py` OK（31 对齐）；⑧ `gofmt -l` 零；⑨ git status 对账——**refr/ 零触碰**；modified 54 = **46 黄金（恰 T6 清单）** + 8 代码（cli/build+check+cli+test、diag/diag、lex/lex、typecheck/typecheck——钩点四字段五钩位）+ 1 归档遗留（2026-09-09-testing-explore/tasks.md——M10c T11 完成记录归档后追加，T4 已披露，随本变更提交带走）；未跟踪 = M11 全新工件（cli fmt/vet/doc/advisory/m11_test、lex m11_test、typecheck advisory/m11_test、internal/fmt/ 包、35 新黄金、本变更目录）——与 proposal「涉触码盘点」逐项对齐（proposal 载 48 枚更新、实 46——T6 偏差定数披露在案）。
- [x] T9 实现审查：welang-code-review 7 条（规范符合性/验证诚实性/测试先行证据/诊断协议稳定/单一权威/红线复核/最小可信验证已跑），产出 = proposal.md「## 审查记录」+ status → complete
  来源：仓库节奏（M10a/b/c 先例）
  验证：7 条逐条过，发现当场处置或披露，审查记录落盘
- [x] T10 归档：welang-archive-sync 五步（本变更零规范提升零新 ADR——机制载体 = 变更记录 + roadmap）+ roadmap M11 行 pending → done（双语）
  来源：仓库节奏
  验证：validate --all --strict 复验绿；归档目录带日期前缀
  完成记录（2026-09-09）：前置确认过（status complete、tasks 全勾、validate --strict OK、审查记录在 proposal.md）。① 规范提升——零增量（工具链实现 slice，三 W 码与 E1903 皆在册复用，docs/spec/ 零触碰）；② 决策提升——零新 ADR（四裁决与渲染层提升语义由变更记录承载，M10 先例）；③ 状态提升——roadmap 双语 M11 行 pending → done 2026-09-09（行述扩为落地实貌：行保结构重排/advisory 层 [vet] 姿态/we vet/we doc pub-only 页 --check）；④ 移动归档 `archive/2026-09-09-fmt-vet-doc` + status → archived；⑤ 复验 validate --all --strict OK（no changes found; registry clean）+ docs_sync 31 对齐。
- [x] T11 记忆与提交：project-overview 增 M11 全量条 + MEMORY.md 索引行更新；提交信息草案呈报，**候用户明示**
  来源：仓库节奏（提交须明示）
  验证：记忆回写；提交草案就绪；未提交状态确认
  完成记录（2026-09-09）：project-overview.md 增 M11 全量条（四裁决、三塔机制、顶层空行策略/parseManifest bare 集两处实现内裁决、渲染披露面四条、46 随批 111 行 breakdown 与 48→46 偏差定数、T4 桶分勘误、六面探针、conformance 606→641）+ M10c 条尾补提交对账（7021aec）+ MEMORY.md 索引行更新（M0–M11、下一里程碑 M12 ffi）。提交信息草案呈报（`Land M11 fmt-vet-doc: chapter 21 formatter, advisory layer, we doc`），**候用户明示**。未提交状态确认：git status = 56 M + 45 untracked（46 黄金 + 8 代码 + 归档遗留 2 + roadmap 2；新工件 = fmt/vet/doc/advisory 代码 + 测试 + internal/fmt/ 包 + 35 新黄金 + 本归档目录），refr/ 零触碰。提交对账候明示后随补（M10c 先例形）。**用户明示提交+推送（2026-09-09）**。提交对账：**4bc15e3** `Land M11 fmt-vet-doc: chapter 21 formatter, advisory layer, we doc`——105 文件 +4016/−218；`git log -1 --format='%(trailers)'` 空（无署名 trailer）；staged 集零 refr/ 命中（pre-commit 钩子同拦）；push origin main `7021aec..4bc15e3`。
