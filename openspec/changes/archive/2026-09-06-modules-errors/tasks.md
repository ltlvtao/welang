# tasks — modules-errors

- [x] T1 测试先行 A：D13 黄金表落盘（预估 ~55 新增 + 2 改写），对当前构建运行必须先红
  来源：proposal 目标 1 2 3 4 5 6 7 / design D4 D5 D6 D7 D8 D9 D13 D14
  验证：`go test ./internal/conformance/ -run TestGoldenCases` 红（新增失败清单 + 改写枚失败，其余 298 枚既有全绿），red 证据与改写逐枚清点记于完成记录

  **完成记录（2026-09-06）**：60 案例落盘（生成器 `/tmp/m6b-goldens/gen.py`，/tmp 惯例同 M3–M6a 先例；锚点全部由标记子串程序化定位，非手算）。conformance 对账：总数 356（298 既有文件 + 58 新文件），**失败 56 = 54 预期红 + 2 改写**，通过 300 = 296 未触碰既有（零回归）+ 4 锁定枚；逐枚 want/got 审计无假红（无因生成器笔误造成的意外红）。红因分布：bndScope 解析边界 70（E1103×2/E0202/E0204/E1104 四形/E1105-assign/六绿例）、bndErr `?` 边界 70（E1201×2/E1202×3/E1203/ch14 三绿例/build-bnd-main-body）、bndTermination 70（panic 两绿例 + check-bnd-panic 改写）、bndMultiModule 70（E1301/E1302 链式/E1303 三位/E1304/ch15 三绿例 + check-bnd-multi-module 改写 + build-bnd-multi-module）、E1304-Releasable 前置（E1102×2/E0808/E1104 两形/E1105 两形/E1106 七位——今日 Releasable 无名，实现后消失）、静默 exit 0（E1101、m6a-impl-where）、E0808 误报形（m6a-precise-map-override want 0 got E0808——D10(b) 实锤）。
  **4 锁定枚披露**（今日已绿的既有行为钉住，非新行为）：check-e0105-unwrapped-bang（`f()!` 今日已发 E0105 锚 `!` 位）、check-e0601-byval-resource-field（byval 字段资源位今日已发 E0601 注册表原文——M6b 后 Releasable 有名，E1304 前置消失，报文不变）、check-m6a-cross-clause-collision（D10(a) 推翻「静默」假设：checkArgs 兜底今日已发 E0501 于实参位）、check-m6a-generic-map-override-mismatch（D10(b) 错签名形今日已发 E0808 于方法名位——需 `> >` 分隔形程序）。
  **2 改写逐枚披露**（proposal D12 预告）：check-bnd-multi-module（bndMultiModule 边界 70 → 多模块 pub 面绿例：import util + util.helper() 静默过）、check-bnd-panic（bndTermination 边界 70 → panic 真调用绿例：`fn die() -> Never { panic("boom") }` 静默过）。
  **过程修正披露**：(1) 三处程序语法笔误（`gc record` 前缀不存在——record 默认即 gc；`newtype X = X(T)` → `newtype X(T)`；E0601 案例误用 gc record——byval 字段才是 own-rejection 位）已改；(2) PRELUDE 手算行数偏一（10 行误作 9）致全部 PRELUDE 基锚点偏一——废弃手算，生成器重构为标记子串定位（posof + 列偏移）；(3) E0601 黄金曾自造报文尾 `a copy would duplicate the one handle`——对照注册表与今日实发修正为原文 `a copy is only honest when everything in it is copyable by value`，该枚随之从预期红重分类为锁定枚（今日已发）；(4) 锁定枚 m6a-generic-map 锚点曾误置 `fn` 关键字位（9:5），今日实锚方法名位（9:8），已修正。E1002 资源捕获黄金（M5 落）已存在，无需新增。


- [x] T2 测试先行 B：parser 单测扩展（scope resource 语句矩阵、`?` 后缀结构、bndScope 拆行后的 ch18 形独立停）与 typecheck 单测（Releasable 判定序矩阵、活跃性分析矩阵——三转移/落空/转移后使用/条件双臂/循环穿透/break-continue 穿透、别名三形、组合位六位、`?` 三码与语境枚举、短闭包定型、panic 族签名、多模块装载/E1301/E1302/E1303/E1304 合格名）先写先红
  来源：proposal 目标 1 2 3 4 5 6 7 / design D1 D2 D3 D4 D5 D6 D7 D8 D9
  验证：`go test ./internal/parser/ ./internal/typecheck/` 红（新用例引用未定义节点/分支失败），red 证据记于完成记录

  **完成记录（2026-09-06）**：`internal/parser/m6b_test.go`（3 函数）+ `internal/typecheck/m6b_test.go`（7 函数）落盘，源与锚点逐字镜像 T1 黄金表。red 证据：parser 编译红——`undefined: ast.ScopeRes` ×2、`undefined: ast.Prop` ×3（恰为 D1 新节点，其余全编译过）；typecheck 7 函数全红且红因正确——TestReleasableDecl/TestScopeResourceStmt/TestResourceLiveness 停 bndScope 解析边界、TestResourceAlias/TestResourceComposite 停 E1304-Releasable（今日无名）、TestPropOperator 停 bndErr、TestPanicFamily 停 bndTermination。**多模块拆分披露**：ch15 多模块（E1301/E1302 项目形/E1303/E1304 合格名）是 CLI 装载面（design D9：装载层在 internal/cli/check.go，跨模块符号表 API 形状由 T7 落定）——包级单测镜像不适用单文件 helper（wantOK/wantDiag 走单 *ast.File），其契约由 T1 项目形黄金（已红）+ T7 装载层单测双钉；本任务不写引用臆造 API 的占位测试。过程修正：字段访问节点实名 `ast.Member`（Recv 字段，非臆造的 ast.Field/X）。

- [x] T3 parser ch13：D1 ScopeRes 产生式（scope+resource 双关键字、绑定列表、块）、Prop 节点（`?` 真节点）、bndErr 行删除、bndScope 拆行（ch18 形独立 bndConcScope）
  来源：proposal 目标 2 5 / design D1
  验证：`go test ./internal/parser/` ch13/ch14 套件绿；既有套件零回归

  **完成记录（2026-09-06）**：ast 增 `ScopeRes{Binds []ScopeBind, Body, Line, Col}`/`ScopeBind{Name, Val, Line, Col}`/`Prop{X, Line, Col}`（stmt/expr 接口登记）；`parseScopeRes`——`scope` 后非逐字 `resource` → bndConcScope（`chapter 18 (scope) forms`），`(` 前 E0105（锚意外 token）、绑定名 E0012/头内重名 E0404（沿用 pattern-binding 措辞形 `the scope resource head binds %q twice`）、body 为普通嵌套块（loopDepth 穿透、defer 非直接层走既有 E0204）；`?` 换 `ast.Prop` 节点（postfix 链级 1，锚 token 位），bndErr 常量删；表达式位 `scope` 收编 E0202 值位族；既有测试更新三处（dispatch 表 scope 行 stmt→bndConcScope/expr→E0202、boundary 探针换 scope timeout 形、`x?` 探针从 bndErr 改 clean）。**临时停点披露**：typecheck walk 对 ScopeRes/Prop 加两行临时边界（bndResDiscipline `resource discipline (chapter 13)`、bndPropOp `the ? operator (chapter 14)`）——防 panic 中断 conformance 运行，T4/T6 以真行为替换（同里程碑内消亡，不入任何黄金）。**提前翻绿 2 枚**：check-e0202-scope-value（E0202 值位族收编 `scope`——解析期兑现）、check-e0204-defer-in-scope（E0204 defer 直接层判定在解析期）——原 56 红中属 parser 层的 2 枚于本任务兑现。验证：parser 全绿；typecheck 既有套件零回归（唯 M6b 7 函数红且红因正确：bndResDiscipline/E1304-Releasable/bndPropOp/bndTermination）；conformance 54 失败（52 新行为 + 2 改写）全部红因正确。

- [x] T4 typecheck ch13 声明层：releasableIface 登记、E1101（模块尾 byres 完备性）、E1102（非 resource 实现者）、catOf 资源类别接线、scope resource 语句检查（E1103 头判定 + 块作用域 + E0204 内嵌 defer 核对）
  来源：proposal 目标 1 2 / design D2 D3
  验证：`go test ./internal/typecheck/` 声明层与 scope 语句套件绿

  **完成记录（2026-09-06）**：四处落地——(1) releasableIface 登记（var 块 + init 设 `release/recvMut:true`（ret nil = unit）+ builtinIface case，resolveIfaceRef 走既有 builtin 兜底）；(2) E1102 于 checkImplDecl（E0822 块后，`inf == releasableIface && catOf(head) != "resource"`，锚 impl 头——gc 记录与 sum 头两形均验）；(3) E1101 于 checkModule **体遍历之后**的真模块尾（遍 byres 声明，impls 中 sameType 精确命中 `recordType.decl` 指针，锚记录名 token NameLine/NameCol——impl 后置文件满足义务）；(4) checkScopeRes 替换 T3 临时停点（bndResDiscipline 行删，仅余 bndPropOp 待 T6）——头表达式定型后 implementsFace 判 Releasable（E1103 锚 exprPos 头首 token），绑定入独立 locals 层，体 walkControl；E0204 内嵌 defer 为解析期判定（parseDefer 的 direct 位），本任务核对并由 parser 套件钉住。
  **E1101 次序披露（过程修正）**：首版置于 pass 2a2 impl 校验后，即刻回归两枚 M5 锁定黄金 e0606-resource-update / e1002-resource-capture（其源文件 byres 记录无 impl、期望错误在体内 7:13/7:24——E1101 于声明层先行则抢先报错）。修正为体遍历后的真模块尾：体内诊断先行、体干净时 E1101 兑现（e1101-no-impl 体干净 ✓）；两枚锁定黄金零改写复原绿，m5_test 的 E0606/E1002 单测用例原样通过。全含 byres 黄金枚举核对（32 枚）与该次序一致（e0903 在 impl 校验层先行、e1103×2 源无 byres 记录）。
  **黄金锚点修正披露（M6b 自有新增，实现前修正）**：check-e1103-gc 锚 1:8 → 4:24。T1 生成器标记子串定位命中首个 `"User"`（记录声明处），非头表达式首 token（4:24，构造头名 token，与 e1103-base 的 2:24 同规则）；T2 单测镜像 4:20 同误，一并修正为 4:24。T1 记录中该枚红因归类（bndScope 边界）掩盖了此锚点缺陷——审计核的是红而非锚。
  **T3 解析缺口披露（本任务揭出并补）**：scope resource 绑定列表的 `(` 未入 paren 区域（depth 未增），构造式头 `u = User { name: "n" }` 被 headExpr 的 noBrace 挂起拒（E0105 4:29，黄金 e1103-gc 将死于解析期）。补：绑定列表按 call 实参列表同型 depth 包裹（T3 完成记录应载而未载的缺口）。配套：E0204-in-scope 用例从 typecheck 套件移至 parser 套件（E0204 解析期判定，typecheck 的 runCheck helper 在解析诊断处 Fatal 无法承载——T2 落错了文件），parser 套件另增构造式头 clean 用例。
  **既有测试更新披露**：m5_test TestRecordDeclarations 的裸 `byres record Conn` wantOK 夹具补 impl Releasable——ch13 语义落地后裸 byres 记录不再 clean（E1101），这是语言真变更非测试债。
  **对账**：conformance 54 → 42 失败。T4 翻绿 10 枚（e1101-no-impl、e1102-gc、e1102-sum、e1103-base、e1103-gc、e0808-release-sig、ch13-scope-green、ch13-impl-late-green、ch13-transfers-green、ch13-break-green）+ e0606/e1002 经次序修正复原绿（T3 时本绿）；余 42 枚红因逐类核验为正确中间态——T5 17 枚（E1104×6/E1105×3/E1106×8，got clean 或 e1105-assign got E0501 赋值定型先行，T5 需在 checkAssign 内先行纪律判定）、T6 11 枚（bndPropOp/bndTermination 70 及 ch14 绿例）、T7 12 枚（bndMultiModule 70 及 ch15 绿例 + build 两枚；build-bnd-m6b-main-body 现停 bndPropOp，T6 后落入 bndMainBody）、T8 2 枚。typecheck 既有套件零回归（唯 M6b 5 函数红且红因正确：T5×3 + T6×2）；parser 全绿；gofmt/vet 清。

- [x] T5 typecheck ch13 纪律：D4 结构化活跃性分析（三通道转移 kill/落空/转移后使用/顶层/break-continue 穿透/release 直调 → E1104 各形）、D5 别名三形（E1105）、D6 组合位六位（E1106 + resourceInType）
  来源：proposal 目标 3 4 / design D4 D5 D6
  验证：`go test ./internal/typecheck/` 纪律套件绿

  **完成记录（2026-09-06）**：架构分两层——**编织进定型**的两处（次序敏感）：E1105-assign 于 checkAssign 两条路径（E0501 定型之前判两侧资源性，`=` 位锚——ast.Assign 增 OpLine/OpCol，parser 两构造点记录 `=` token）、E1104-release-direct 于 memberOfType recordType 方法支（按规范「接收者资源类型」判，含 impl 自身体内的自递归 release；锚接收者首 token，报文经 recvText 渲染名字链）；**后置结构化 walk**（internal/typecheck/resource.go 新文件，体内定型干净后跑）：三转移 = 活绑定裸名处于 scope 头 / return 值 / 调用实参位（干净程序上非资源参数位必 E0501，故句法判定与类型判定等价）；路径汇合 = 任一路径活即活（「a path with no transfer」）；break/continue = 内层块作用域出口（循环帧 entry 集合归属，break 快照并入环后路径）；while/for 的零迭代路径并入环后（entry 态参与 join；loop 无条件进入不并入）；块尾/return 出口/穿透出口的落空绑定按声明序取最早者单报；scope 头绑定 owned（免落空义务，读与直调仍查）；defer 体按副本走（其中 use-after 仍查，kill 不回流）；闭包体经 closureType 在定型期内跑自己的 resCheck（裸参类型取自期望推导）；影子名 save/restore；resLets 事实表由 walkItems 记录 let 的资源类别。E1106 七位：tuple 元素（resolveTypeRef）、gc 记录字段（byres 字段按规范枚举不禁，byval 字段属 E0601 自有）、sum payload、newtype 底型（pass 2a 三处）、泛型实参（resolveArgs 统一——fn/构造/接口子句 + Result/Option 内联实参环补齐）、Dyn 类型位（resolveNamed Dyn case，face==Releasable）、Dyn 构造位（dynCall 实参资源类别，报文 face 名模板化）。
  **披露**：(1) T2 单测镜像锚修正——TestResourceLiveness use-after 14,13 → 15,13（拼接源逐行核对：prelude 10 + 空行 + fn use 12 + let f 13 + take 14 + f.fd 15；T2 手算偏一，T4 交接时已标记待核）；(2) **T4 完成记录笔误更正**——「T5 17 枚（E1104×6/E1105×3/E1106×8）」应为 **16 枚（E1106×7）**：E1106 黄金实为 7 文件（tuple/gc-field/sum-payload/newtype/generic-arg/dyn-type/dyn-construct），6+3+7=16，42−16=26 与今日对账吻合；(3) **未钉边角的规范一致性裁定**（均按 spec 1300 「每条控制路径恰达转移」文义）：外层 while 含零迭代路径故其体内 kill 不解除外层绑定落空义务（`let f; while c { take(f) }` 拒绝）、推断式泛型实例化（`echo(f)` 无显式实参）不触发 E1106（无锚位——与 spec「绑定位精化留作本章修订」同类留白）但实参转移照常 kill、`let _ = openFile()` 的丢弃绑定照担义务、非 Ident 接收者的 release 直调报文渲染 `<expr>.release()` 占位（锚已在首 token）；(4) 真机边角电池（15 探针全符）：零迭代落空 / loop 无零迭代路径 / 嵌套循环 break 归内层帧 / 影子恢复 / defer 内 use-after / match 臂 join（双臂净、单臂落空）/ var-alias 报别名形（spec 别名形覆盖 var 拼写）/ self.h = f 值侧 E1105 @ `=` / fn 类型参数位资源合法（转移通道）/ byres 嵌 byres 字段不禁 / 最早声明者先报。
  **对账**：conformance 42 → 26 失败。T5 翻绿 16 枚（e1104 六形、e1105 三形、e1106 七位全数）；余 26 逐类核验为正确中间态——T6 12（bndPropOp 9：e1201×2/e1202×3/e1203 + ch14 三绿例；bndTermination 3：panic 两绿例 + check-bnd-panic 改写）、T7 12（bndMultiModule 70 及 ch15 三绿例 + e1301/e1302/e1303×3/e1304 + build 两枚——build-bnd-m6b-main-body 现停 bndPropOp，T6 后落入 bndMainBody）、T8 2（m6a-impl-where got 0 / precise-map want 0 got E0808）。typecheck 包内 M6b 套件五函数绿（Releasable/ScopeResource/Liveness/Alias/Composite），唯 TestPropOperator/TestPanicFamily 红于 bndPropOp/bndTermination（T6 正确红）；parser 全绿；既有套件零回归；gofmt/vet 清。

- [x] T6 typecheck ch14：D7 `?` 传播（E1201/E1202 语境枚举/E1203、Ok 解包、短闭包定型、postfix 链）、D8 panic 族预导入真签名（bndTermination 删、Never 返回位、普通调用检查）
  来源：proposal 目标 5 6 / design D7 D8
  验证：`go test ./internal/typecheck/` ch14 套件绿

  **完成记录（2026-09-06）**：架构两层——**传播语境** `prop *propCtx`（checker 字段，栈式 save/restore）承载 `fnName`（"" = 模块顶层、"(closure)" = 闭包、fn/方法名）、`ret`（nil = valueless 渲染 "()"）、`deferBody`、`short`+`shortErr`；五挂点：Check 构造器零值（=顶层）、checkFnDecl、checkMethodBody（签名增 name 首参）、closureType 三臂（full = 闭包自身 ret；short = 推断模式，体走后值型包 `Result<尾型, shortErr>`；裸参臂 = 闭包语境无 ret）、walkItems Defer 臂（`deferBody: true` **覆盖任何外围 fn**——黄金 e1202-defer 外围 main 本身返回 Result 证明显式覆盖必须）。**propType 判序**（design D7 (a)→(b)→(c)）：操作数先定型（expected 不入）→ 非 resultSum 即 E1201（OptionSum 特注记 Option 不传播）→ deferBody → E1202 defer 形 → short 模式（首 `?` 记 shortErr、后续 sameType 否则 E1203）→ fnName=="" → E1202 顶层形 → ret 非 resultSum → E1202 naming（nil 补 unitType）→ E_src vs E_dst sameType → E1203 → 值 = nt.args[0]（Ok 解包）。**panic 族**：`terminationFnType(name)` helper（assert → `fn(Bool, String) -> ()`；panic/todo → `fn(String) -> Never`）+ identType/callType 两处硬 case（不造 syms 符号——用户同名声明 syms 先解析自然影子，M3 已钉）；调用走 fnValueCall 普通路径——实参 E0501 位置判、Never 满足任意返回位（agree 既有豁免）、Never 表达式不触发 E0605、topLet 初始化器 panic 调用合法。bndTermination/bndPropOp 两常量删。
  **披露**：(1) **规范场景违例（探针电池揭出，已修）**——spec 1400 场景「A closure propagates from itself」钉死 `fn(x: String) -> Result<Int64, AppError> { parse(x)? }` 裸 `?` 尾合法，而首版尾判断按解包 T 对完整声明返回报 E0501；修复：walkItems ExprStmt 尾判断对 `*ast.Prop` 尾取声明返回的 Ok 槽（`Result<U, E>` → 判 U），其余尾照旧判全返回——fn 体与全注解闭包体同经 walkItems(walkFn) 一处覆盖；配套单测正反形钉住（fn 裸 `?` 尾对 U 合法 / 对错 U 报 E0501 锚操作数首 token）。(2) **exprPos 落穿**——`ast.Prop` 无 case，`?` 尾诊断锚 1:1（探针实抓）；补 `case *ast.Prop: return exprPos(x.X)`（postfix 链首 token 语义同 Member）。(3) **边角裁定**：return 位 `?`（`return g()?`）按解包值对完整声明返回判 E0501——Ok 包装是块尾值机制（ch5+ch14），return 表达式自带值，单测钉住该裁定；元组元素位 expected 穿透不达（`let t: (Result<...>, Int64) = (Ok(1), 2)` E0827）为 ch10 既有穿透深度，非 M6b 面。(4) **bndTermination 三钉测试更新**（typecheck_test.go，语言真变更，T4 先例）：panic 调用 wantOK（Never 不触发丢弃律）、todo()/assert(true) 个数缺 wantBnd bndArityGap（design D8 披露的诚实边界维持）。(5) **build-bnd-m6b-main-body 黄金源修正**（M6b 自有，未绿前修正）：原源含 helper `fn g()`，codegen pass-one 源序扫描先报 bndOtherFns（build-bnd-fn 黄金钉死），黄金要的 bndMainBody 不可达；改源为 impl 方法承载（ImplDecl 擦除不触 other-fns 行），用例意图（`?` main 体过定型达主行边界）与黄金名保真；**T5 记录「T6 后落入 bndMainBody」的归侧更正**——该枚红因是 bndPropOp，T6 兑现（非 T7），T5 将其计入 T7 12 属归侧笔误。
  **真机边角电池（16 探针，go run 真二进制）**：全注解闭包 Result 含 `?`（绿，修复后）/ 全注解闭包 Int64 含 `?` → E1202 naming "(closure)" / 短闭包双 `?` 异 E → E1203 @ 第二 `?` / 顶层短闭包绑定（绿）/ if·match·while 臂内 `?`（绿）/ 方法体 Result 含 `?`（绿）· Int64 → E1202 naming 方法名 / defer 内嵌短闭包（闭包自身语境，绿）/ 元组操作数 E1201 / `??` 双传播 → E1201 Int64 / `let x = panic(...)` topLet Never（绿）/ `let ok = assert(true, "x")` unit（绿）/ fn 裸 `?` 尾正反形 / return 位 `?` / postfix 链 `g()?.n`·`unwrap(g()?)`·`add(g()?, g()?)`（绿）。三探针源自身语法修正（非实现问题）：match 臂逗号→换行、零参短闭包不存在（`||` 为逻辑或，parser 既有）、短闭包 if 表达式体 noBrace 拒（parser 既有，换嵌套调用形）。
  **单测增钉**（m6b_test.go TestPropOperator +7 调用）：短闭包双 `?` 异 E 的 E1203、闭包场景绿例 + E1202 "(closure)" 形、顶层短闭包绿例、fn 裸 `?` 尾绿例 + E0501 反例、return 位 `?` E0501 裁定钉——全部为探针电池揭出的规范场景/修复镜像（E1202 行锚手算偏一 8→9 已核，col 19 = `?` token 无误）。
  **对账**：conformance 26 → **13 失败**。T6 翻绿 13 枚（T5 预判 12：e1201×2/e1202×3/e1203 + ch14 三绿例 + panic 两绿例 + check-bnd-panic 改写；+ build-bnd-m6b-main-body——T5 归侧笔误的更正，见披露 (5)）；余 13 逐类核验为正确中间态——T7 11（bndMultiModule 70 及 ch15 三绿例 + e1301/e1302/e1303×3/e1304 + check-bnd-multi-module 改写 + build-bnd-multi-module）、T8 2（m6a-impl-where got 0 / precise-map want 0 got E0808）。typecheck 包全绿（M6b 七函数全过）；parser 零触碰；既有套件零回归；gofmt/vet 清。

- [x] T7 cli 装载层 + typecheck ch15 多模块：D9 路径映射装载、import 图 DFS 环检（E1301）、E1302 项目形（期望路径报文）、跨模块 pub 面（E1303 三位）、合格名 E1304 扩、后序检查次序、初始化序编译期计算、bndMultiModule 删
  来源：proposal 目标 7 / design D9
  验证：`go test ./internal/cli/ ./internal/typecheck/` 多模块套件绿；单模块既有全绿

  **完成记录（2026-09-06）**：架构两层——**装载层**（internal/cli/check.go loadGraph）：根读+parse 先行（根 parse 诊断先于模块解析），DFS 三色图走（import 源序）：std 首段 → bndStdModules 边界；路径映射 `a.b.c` → `src/a/b/c.we`；缺文件 → E1302 项目形（报文含期望路径，锚 import 首段 token PathLine/PathCol，附 WithHelp）；回边（gray 命中）→ E1301（环渲染 = 栈中目标首现到栈顶 + 目标，如 `a -> b -> a`，锚闭合 import 位，附 WithHelp）；白节点读（fsError）+ parse（d → report / ni → boundary）+ 递归；后序 append（非 root 模块，Key = 规范虚线路径）——后序即初始化序的编译期计算（被引先于引用者）。**typecheck 层**（typecheck.go）：入口拆 Check（单模块，ingest key "main"）/CheckProject(root, deps)（deps 后序先 ingest、root 最后，panic-stop 经 stopTo 穿透全部 ingest）；`Module{Key, Path, File}` API；ingest 逐模块换 c.syms 桶并按规范键注册 c.modules、重置全部逐体状态（locals/typeScope/fnRet/fnBounds/recvMut/closures/closureBounds/prop）——AST 指针键映射（fnParams/fnRets/fnWheres/implWheres/resLets）跨模块天然共享，sameType 指针身份成立；symbol 增 mod（import 的规范键）+ pub 位（pass 1 全声明携带）；跨模块解析 helper 组：`importQualifier(name)`（当前模块 import 符号且无局部影子 → 规范键）、`importSym(mod, item)`（E1304 declares-no / E1303 non-pub 门，锚合格名首 token）、importTypeRef/importIface/importMember/importCall（分派进既有机器——泛型确定经 fnCall 免费获得 M6a 定型）；七个合格位接线（resolveNamed 类型位 / boundIface where 界 / dynIfaceRef Dyn 实参 / resolveIfaceRef impl 子句 / memberType 值位 / callType 调用头前置分支 / constructType 构造头）+ checkPattern 限定变体模式（PatVariant 增 Qual 携带，parser 传入）。checkImport 重写：SingleFile 保留 E1302 单文件报文；Project 无操作（E1301/E1302 归装载层，原 os.Stat CWD 相对 hack 删）；bndMultiModule 常量删。codegen Emit 的 `*ast.Import` 臂注释更新（多模块根过定型后可达，bndOtherFns 为诚实行——build-bnd-multi-module 由根模块 import 项兑现）。
  **披露**：(1) **std 边界双守卫**——装载层（管道序先行）与 typecheck checkImport（CheckProject 直接 API 调用的自足诚实）各持一处 `imp.Path[0] == "std"` 判定；同 What 字符串无可分歧行为，CLI 面恒由装载层先行兑现。(2) **helps 裁定**——装载层 E1301/E1302 附 WithHelp（注册表 remediation 原文，helpE1301/helpE1302 常量）；E1303 不附（同 E1101-E1106/E1201-E1203 先例——tHelps 无条目）；E1304 新报文自动获得 tHelps 既有条目。(3) **装载层单测**（T2 承诺兑现，internal/cli/modules_test.go 8 函数）：绿双模块 + verbose 计数面（2/3 modules）、三模块初始化序链、E1302 三形（root/依赖方/嵌套路径映射）、深路径 + 别名绿例、E1301 环渲染、std 边界 exit 70、E1303/E1304 可见性门、多模块 build exit 70——测试先行的红面由 T1 项目形黄金承担（T2 已披露拆分），本任务绿检。(4) **过程修正（首轮构建揭出即修）**——loadProject 内 `deps` 撞名（manifest 依赖计数 int vs 模块图切片，图变量更名 mods）；runCheckProject if-init 作用域（modules 于 if 语句域外不可见，提为普通语句）。(5) **对账口径**——T4/T6 记录的「T7 11」桶与今日 T7 面全绿 12 枚（e1301/e1302-chain/e1302-project/e1303×3/e1304-undeclared-item/ch15×3/check-bnd-multi-module/build-bnd-multi-module）差一：e1302-project 的实现前红因是旧 checkImport Project 模式 os.Stat hack 的报文（非 bndMultiModule 边界），旧桶枚举未单列；本记录以今日全量运行（2 红 = T8 两枚）为权威，不回溯改写已落账记录。
  **对账**：conformance 13 → **2 失败**（唯 T8：m6a-impl-where-instantiation / m6a-precise-map-override）。T7 面 12 枚全绿（见披露 (5) 列举）。cli 套件绿（含新 8 函数装载层单测）；typecheck/parser 套件绿；单模块既有零回归；go clean -testcache 后 `go test ./...` 除 conformance T8 两枚外全绿；gofmt/vet 清。

- [x] T8 M6a 遗留三项精化（D10）+ 收尾：跨子句撞名显式检查、impl 覆写子句默认指标空间、impl where 应用点满足（E0830 新位）；全量 conformance 绿（预估 ~355/355）、`go test ./...` 清缓存全绿
  来源：proposal 目标 8 / design D10
  验证：`go test ./...` 全绿；T1 名单逐枚翻绿零回归

  **完成记录（2026-09-06）**：(a) **无代码**——D10(a) 的「静默」前提被 T1 黄金推翻（checkArgs 兜底今日已发 E0501 于实参位），check-m6a-cross-clause-collision 为锁定枚（T1 披露），全量运行绿。(b) **rebaseClause helper**（镜像 subst 形状的 paramRef idx 平移 walk：idx >= from 位移 to-from，全 Type 构造子覆盖）+ checkImplMethodSig 签名增 `clause` 参：接口侧签名经 ifaceArgs/assocs 替换后，方法子句位从接口子句偏移 `len(inf.params)` 重基到 impl 方法子句偏移 `len(params)`——同一方法子句在两侧不同偏移下解析（M6a T5 的 paramScopeOffset 机制），恒等子句名（checkMethodClause 先行钉死）重基后 sameType 成立；check-m6a-precise-map-override 绿（泛型 impl 覆写 `map<U>` 签名不再假报 E0808）。(c) **checkImplWhereAtConstruct**（constructType 实参确定后、checkFieldInstantiation 前置）：遍 c.impls 泛型 impl（generic 位）中头为同 record 声明者，头实参位 paramRef 与构造实参同位绑定 impl 子句，逐 where bound implementsFace 判定，不满足 → E0830 既有码构造位新报文（消息尾 `the where bound of the impl for %q holds at every instantiation`，headStr 渲染 impl 头如 "Wrap<T>"），锚构造头 token；for-impl 与 inherent impl 同判（where 是 impl 的声明事实，与接口使用与否无关——构造即实例化）；check-m6a-impl-where-instantiation 绿。
  **披露**：(1) D10(c) 设计文句「constructType 显式实参路径」与黄金实际形态不符——黄金为推断实参（`Wrap { v: u }` 无子句），实现统一于实参确定之后（显式与推断两路径同覆盖），以黄金为准。(2) 三项以黄金钉死（D10 自文「三项各 1–2 枚黄金钉死」），无单测镜像（T2 未规划此三形，红面由 T1 黄金先行承担）。(3) 实现期间两枚红因同时消（rebase 修 (b)、构造检查修 (c)），无中间对账。
  **对账**：conformance **0 失败——356/356 全绿**（T1 名单 54 预期红 + 2 改写全数翻绿零回归）；`go clean -testcache && go test ./...` 全绿；gofmt/vet 清。

- [x] T9 全量验证与真机电池：既有 298 枚零改写核对（唯 T1 披露 2 枚）；真机黑盒——资源三转移综合绿程序静默、`?` 链综合绿程序、多模块项目（3+ 模块含环负例）真机 check、逐码负例真机报文核对（11 首发码全覆盖）、新形式 main 体 build 停 bndMainBody 70、多模块 build 停 bndOtherFns 70；D14 不可达清单逐条实证记录
  来源：proposal 目标 1 2 3 4 5 6 7 / design D11 D14
  验证：输出与不可达实证记于完成记录

  **完成记录（2026-09-06）**：**既有黄金零改写核对**——`git diff --stat internal/conformance/testdata/` 恰 2 枚修改（check-bnd-multi-module/check-bnd-panic，T1 披露改写）+ 58 枚新文件全 untracked：298 既有零改写成立。**真机电池**（真二进制 `go build -o /tmp/we-bin ./cmd/we`，/tmp 惯例）分两部——**Part A 全量黄金对等**（脚本 /tmp/m6b-t9/run_goldens.py：逐枚 tempdir 物化 setup + 子进程运行 + exit/stdout/stderr 字节级比对）：**356/356**——既有进程内 runner（cli.Run 直调）与编译二进制零行为差；覆盖 11 首发码逐枚负例真机报文核对（E1101×1/E1102×2/E1103×2/E1104 六形/E1105 三形/E1106 七位/E1201×2/E1202×3/E1203/E1301/E1303 三位）、资源三转移综合绿（check-ch13-transfers-green 静默）、`?` 链综合绿（check-ch14-chain-green）、多模块真机 check（check-ch15-init-order-green 三模块链 + check-e1301-cycle 三模块环负例）、build 双停（build-bnd-m6b-main-body → bndMainBody 70、build-bnd-multi-module → bndOtherFns 70，clang 21.1.8 真链）。**Part B D14 专属探针**（脚本 /tmp/m6b-t9/probes.py，10 探针全符）+ 多模块加深刻：三环渲染 a -> b -> c -> a（四模块，闭合 import 锚 src/c.we:1:8——黄金只钉二环）、无环四模块链 `we: check passed (4 modules)`。
  **D14 逐条实证**：(1) E1106 byval 字段位归 E0601——check-e0601-byval-resource-field 真机复现（E1106 黄金七位无 byval 形）；(2) E1202 task/test 语境不可达——`task`/`test` 块停于解析（真机 exit 70 chapter 18 (concurrency) forms / chapter 20 (testing) forms，体内 token 不达类型期）；(3) E1302 std 段不发——`import std.io` 真机 exit 70 standard-library modules (chapter 15)，先于任何模块解析；(4) E1303 接口值到达合法——check-ch15-interface-arrival-green 真机静默（Dyn 箱 + bound 调用）；(5) E1608/E1806 名——currentCancelSignal/advanceTime 真机 exit 70 task-scope and time-control functions (chapters 18 and 20)，bndTaskTime 维持；(6) while 回边保守性——真机探针：外层绑定体内转移→循环外读→再转移，静默 exit 0（零迭代路径并入环后：kill 不出环、多重转移不报）；(7) 初始化序运行时/panic 中止非目标——panic 程序 check 绿 + build 停 bndMainBody 70；topLet 项目 check 绿（序编译期已算）+ build 停 bndTopLets 70（codegen 诚实行）；(8) E0808 于 release 签名——check-e0808-release-sig 真机复现（ch10 既有码）。
  **披露**：(1) D14-6 文句「『体内转移后循环外使用』形黄金锁定接受」——黄金集实锁的是体内转移形（check-ch13-loop-green），「循环外使用」半形无对应黄金；以本任务真机探针补实证（静默接受面即保守性本身）。(2) 探针脚本首版一处流期望笔误（--verbose 成功行在 stdout 而非 stderr）——脚本自修正后 10/10，非产品面。(3) D14-2 探针体内放资源绑定文本——解析停于块关键字，体内任何 token 不可达即实证机制（`?` 同理由，无需求其真达）。

- [x] T10 验证阶梯 + 实现审查（welang-code-review 7 条，真二进制证据）+ 归档（status → archived，归档目录 2026-09-06-modules-errors）+ roadmap M6b → done 双语同步
  来源：研发流程 + design D14 披露义务
  验证：gofmt -l 空、go build/vet 过、validate --all --strict 过、docs_sync 对数不变（30）、git diff --check 干净；归档目录存在

  **完成记录（2026-09-06）**：**验证阶梯**——gofmt -l 空；go build ./... / go vet ./... 过；`python3 openspec/tools/validate.py modules-errors --strict` 与 `--all --strict` OK（1 change valid, registry clean）；`docs_sync.py` 30 对齐（对数不变）；`git diff --check` 干净；`go test ./...` 全绿。**实现审查**（welang-code-review 7 条，全部通过，结论落 proposal.md「## 审查记录（active → complete 关卡）」）：规范符合性（11 首发码 title 与注册表逐字、E1104 直调形 = ch13:113 逐字、356 黄金字节级对等、D14 八条真机实证引用 T9）、验证诚实性（T1–T9 逐项 + 今日全量重跑 + /tmp 证据在卷）、测试先行（T1/T2 red 在卷 + 58 新黄金 §52 格式零违规——既有非诊断行均属 M0 工具面黄金）、诊断协议稳定（internal/diag/ 零触及、零新增码）、单一权威（零规范增量、注释纯英文、M6a 归档先例）、红线复核（refr/ 双重隔离核对、diff 范围与目标一致）、最小可信验证（阶梯全项 + 真二进制 356/356 + 探针 10/10）。**归档**——roadmap M6b → done 双语同步（en/zh 行 22）；零 specs/ 提升面（三章规范先行已批）；无 ADR 级政策变更（M6a 先例，长期取舍随 design.md 归档留存）；`git mv openspec/changes/modules-errors openspec/changes/archive/2026-09-06-modules-errors` + status → archived；复验 `validate.py --all --strict` OK（no changes found——active 集空即净）。

- [ ] T11 记忆更新与提交：project-overview 增 M6b 条目（M6 拆分收尾 + 下一里程碑指向）、MEMORY.md 索引行更新；英文提交信息（无署名 trailer；refr/ 不入提交）
  来源：研发流程 + 用户红线
  验证：`git log -1` 消息为英文且无 Co-Authored-By；`git show --stat` 无 refr/ 路径
