# tasks — concurrency-check（M9a）

- [x] T1 测试先行 A：D12 黄金表落盘（~58 新增），对当前构建运行必须先红
  来源：proposal 目标 1–8 / design D1 D2 D3 D4 D5 D6 D7 D8 D11 D12
  验证：`go test ./internal/conformance/ -run TestGoldenCases` 红（新增失败清单，既有 431 枚零回归），红因分类（边界红/正确类型期红/锁定绿）记于完成记录

  **完成记录（2026-09-07）**：44 案例落盘（生成器 `/tmp/m9a-goldens/gen.py`，/tmp 惯例；锚点 marker 子串程序化定位——E1401 于 `io { fetch()`（首现 `fetch()` 是声明位）、捕获码于捕获名 token、构造码于调用头首 token、E1616 于实参 `0`）。conformance 对账：总数 475（431 既有 + 44 新），**红 42 = 全部新枚减 2 锁定**（双向 `comm` 对账：红 ∖ 新 = 空、新 ∖ 红 = 恰两锁定枚），绿 433 = 431 既有（零回归）+ 2 锁定绿。分布：负例 22（E1601–E1618 逐枚，E1602 双形[裸 List/元组含 List]、E1607 双形[未消费/双消费]；+ E1401 任务自身 extent 面 + E0830 Shareable bound 应用面[实参 List<Int64>，闭包实参形归 T2 单测]）；绿面 14（裸装载/别名装载/共享四型+read+跨绑定嵌套访问绿/Cond+Semaphore 全成员/任务 await 绿/main 纯净零贡献锁/currentCancelSignal 体内绿/scope 四形含 timeout×collectAll 复合+双 sum match/select 双源+通配/select 体内 awaitCancelled 单位臂/方向视图经参数注解/TaskHandle 注解+Panicked 限定模式/三命名 sum 限定模式 match/Shareable bound 应用 Int64+Bool[无 import——prelude 可见钉]）；锁定 2（裸 `Mutex` 无 import → E1304 4:13 维持；`advanceTime` → bndTaskTime exit 70 维持——Q3 裁决的回归钉）；边界 What 6（scope 语句位/scope 值位/构造/双任务/io 实参位链/含并发 fn → bndOtherFns）。
  **红因分类**：parse 边界 exit 70（一切首触 `scope`/`task`/`select` 的文件——parse 先于装载与类型检查，bndConc/bndConcScope 先停）；E1302 std 形（无并发语法形、仅 import 的文件直达装载边界：e1606/e1608）；E0829（shareable bound 两枚——今日 bound 位空转，翻绿面）；E1304（e0830-list 修正后仍红于 E0829 首停）。**锁定绿 2 枚即「正确今日行为」类**。
  **黄金措辞修正（实现前，自有新增自由修正，披露）**：check-conc-shareable-bound-green 与 check-conc-shareable-e0830-list 初稿漏 `pub type AppError` 声明——落盘红跑揭示（今日红因错为 E1304 AppError 而非目标面 E0829）；补声明后红因归位。
  **D12 对账**：预估 ~58 → 实落 44（E1604 的 release-only 资源绿孪生并入 e1604 负例文件的类型面即隐含、timeout 子句 E0501 归既有机器不留黄金、闭包实参 E0830 归 T2 单测——覆盖面不减）。**0 改写**：`git status` 既有 testdata 零触碰。
  **design 修正（D5，规范示例对照揭出）**：句柄纪律分析对象由「let 初始化位绑定」扩为「scope 体内一切 TaskExpr 出现位」——规范示例「scope { task effect net { fetchA() } } → E1607」是裸语句形（句柄弃置 = 未消费极端形）；另 D4 补记捕获 pre-pass 先于体走查（捕获诊断先于体内容诊断，panic-stop 次序确定）。

- [x] T2 测试先行 B：parser 单测（三产生式形/锚点/意外 token 清单收缩）与 typecheck 单测（装载/成员集/Shareable 闭集/捕获纪律/句柄纪律/select 定型）先写先红
  来源：proposal 目标 1–8 / design D1 D2 D3 D4 D5 D6 D7 D8 D9
  验证：`go test ./internal/parser/ ./internal/typecheck/` 红（新用例引用未定义符号/停点断言不匹配），red 证据记于完成记录

  **完成记录（2026-09-07）**：`internal/parser/m9a_test.go` 三测试函数（TestTaskProduction/TestScopeCompoundProduction/TestSelectProduction——AST 字段断言按 D1 节点名 `TaskExpr{EffectTags,Body}`/`ScopeExpr{Timeout,CollectAll,Body}`/`SelectExpr{Cases}`、`SelectCase{Wildcard,Name,Source,Body}`；E1601 双形于 task 关键字、E1611 双形（Some(x)/字面量）于 case 首 token、E1612 双形（1 臂/0 臂）于 select 关键字、逆序 `collectAll timeout(...)` → E0105 "where the scope body opens"、`scope resource` 回归卫、语句位=ExprStmt 卫）；`internal/typecheck/m9a_test.go` 八测试函数（TestStdConcurrentLoading/TestConcMemberFaces/TestShareableMarker/TestTaskBlockFaces/TestTaskCaptureDiscipline/TestHandleDiscipline/TestScopeSelectTyping/TestConcConstructors——含 T1 记录预告的 E1604 release-only 资源绿孪生与闭包实参 E0830、句柄纪律环内创建/单臂 join 缺失/嵌套分帧半形、currentCancelSignal 闭包穿透绿、E1613 双向绿[同绑定红/跨绑定绿]、E1616 负字面量形、E1617 期望来自返回位绿）。
  **红证据**：parser 包编译失败——恰为 `undefined: ast.TaskExpr/ScopeExpr/SelectExpr`（新节点未落，预期形式；包级编译失败使既有套件一并不可跑，M8 T2 先例）；typecheck 8/8 函数红且红因逐类吻合 T1 分类——E1302 std.concurrent 未装载（装载/成员/构造三族）、E0829 Shareable bound 空转（:3:36 首停先于 E0830 断言）、parse 边界 exit 70 bndConc/bndConcScope（task/capture/handle 三族）、`scope` 值位 E0202（select 定型族——今日 scope 是 valueless 语句形）。
  **测试自修正（落盘后、对既有 AST 习俗对齐，非停点断言）**：`ExprStmt.Value`→`.Expr`、`*ast.Block`→`ast.Block`（FnDecl.Body 是值类型）、补 `testing` import——三处是测试拼写错，非 T3 待实现面。
  **design 修正（D1/D11，T1 分析已预告）**：原稿称 `case/timeout/collectAll` 从 E0105 意外 token 清单移除——实核 parser.go:1655 它们仍无独立语句产生式（位置关键字，先导 `scope`/`select` 消费后进入），清单不变、报文零变化；D1/D11 两处已改。

- [x] T3 parser 面：D1 三产生式与 AST 节点（TaskExpr/ScopeExpr/SelectExpr/SelectCase）、E1601/E1611/E1612 parse 期、四处 bnd 位删、意外 token 清单收缩、既有 parser/m6b 测试更新（披露）
  来源：proposal 目标 1 / design D1 D9 D11
  验证：`go test ./internal/parser/` 绿；既有套件更新面逐处披露

  **完成记录（2026-09-07）**：ast.go 落四节点（TaskExpr{EffectTags,Body}/ScopeExpr{Timeout,CollectAll,Body}/SelectExpr{Cases}/SelectCase{Wildcard,Name,Source,Body}，expr() 标记入列）；parser.go 三产生式（parseTask——`effect` 段强制，缺段 E1601 于 task 关键字，段走既有 parseEffectSegment，体走 parseFnBlock(&fnCtx{name:"(task)", hasRet:true})——return 携值合法、环深归零；parseScopeCompound——timeout 子句 headExpr（while/if/scope-resource 头同惯例）→ 可选 collectAll → 体 parseBlock(nil)，逆序/非子句 token → E0105 "where the scope body opens"；parseSelect——臂换行分隔（match 臂先例）、`}` 直收、<2 臂 E1612 于 select 关键字；parseSelectCase——`_`/camel Ident 二形，其余（含 Pascal+`(` 变体形、字面量）E1611 于 case 关键字）。语句位分派：`scope` 先 peek `resource` 走 parseScopeRes，否则并入关键字先导表达式组（task/select 同入）；表达式位：`scope` 移出 E0202 valueless 清单（resource 形在值位仍 E0202——m6b 锁行不动），task/select 建真产生式。**bnd 位删四处**（:1651 task/select、:1986 parseScopeRes 守卫、:2927 表达式位）+ bndConc/bndConcScope 常量删——边界组表缩至 FFI/Test/mutPar 三行。E0105 意外 token 清单**零变化**（D1 已修——case/timeout/collectAll 仍无独立语句产生式）。helps 增 E1601/E1611/E1612 三行（registry remediation 压缩）。
  **既有测试更新（逐处披露）**：dispatchTable 三行改钉真行为——scope 行 rest 改 `{ 1 }`（裸形语句/值位皆净解析），task 行 rest 改 `effect net { 1 }`，select 行 rest 改双臂多行文本（`{`/`case`/`}` 换行拼）——三行 top 列维持 E0105；TestBoundaryForms 删四行 conc 探针（边界组表缩行——该表的既定语义）；m6b_test TestScopeSplit 两行 wantBnd → wantClean（M9a 落地表，m9a_test.go 承接真行为探针：`scope x {}` E0105 2:11、空体净解析）；m9a_test 逆序锚点自修 2:33→2:30（初算列数笔误——token `timeout` 实际列 30，非停点设计变更）。
  **验证**：`go test ./internal/parser/` 绿（含 m9a_test.go 三函数全过——AST 字段/锚点/E0105/E1601/E1611/E1612 全中）；gofmt/vet 清；conformance 42 红 → **39 红**（e1601/e1611/e1612 三枚 parse 期黄金**报文与锚点逐字首跑即中**翻绿——黄金契约验证成立），余 39 红全部为 T4+ 检查期面（E1302 装载族为主）。

- [x] T4 装载与内建类型：D2 StdModule("std.concurrent") 注册（四命名 sum 真声明 + channel 标记 FnDecl、注册表单例）、11 内建类型 checker 侧注册（泛型/品类/封闭成员集）、限定解析路径、构造面（T 从实参/期望驱动 E1617）、D8 杂项码（E1614/E1615/E1616）
  来源：proposal 目标 2 8 / design D2 D8
  验证：`go test ./internal/typecheck/` 装载与类型面绿；conformance 对应黄金翻绿

  **完成记录（2026-09-07）**：typecheck.go 落五面。(1) 注册表：11 个 sumInfo 单例（mutexSum…cancelSignalSum）+ concurrentTypes map + concurrentType 谓词；(2) StdModule("std.concurrent") 合成文件——四 SumDecl 真声明（Pub、ReceiveResult 带 `<T>` 子句、Panicked 载荷 String、Received 载荷 T）+ channel 标记 FnDecl（Pub、Params [n: Int64]、无 Ret 无体——M8 println 先例），包级单例 stdConcurrentFile（一 run 一 ingest 下桶内指针稳定）；(3) 成员面：concurrentMembers checker 方法（D2 成员签名表全量；SendResult/ReceiveResult/TaskPanic 经 stdConcSum 桶取——与限定模式同一 sumInfo 指针；RwLock.read 的 U=paramRef{idx:1} 留给调用位定）+ memberOfType namedType 分支 collectionSum 后插 concurrent 插槽（命中 methodUse+substFn(view, t.args)；未中 E0816——封闭成员集非 stdlib 边界）；(4) 解析路径：importTypeRef/importCall 各加 mod=="std.concurrent" 先行分支（concurrentTypes 命中→构造/类型；channel 名→channelCall；gate 于模块键——用户模块同名 Mutex 不受扰）；importCall 增 expected 参数（callType 调用点同步）；(5) 构造面：concurrentCtorCall（Mutex/RwLock/Atomic/AtomicRef 实参定型 T——E1614 判八整型||Bool||Float32/64、E1615 判 recordType 且 cat=="gc"；Cond 实参须 Mutex<X> 否则 E0501（参数视图渲染 Mutex<T>）→Cond<X>；Semaphore：E1616 常量判断先行（整字面量/一元负整字面量经 intLiteral 取值，值入报文）、容量 Int64 走 checkArgs E0501；元数≠1 → bndArityGap）+ channelCall（期望 Channel<T> 定 T；非 Channel 期望同落 E1617——元素类型唯一源是 Channel 期望，design D2 边界；无期望报文含 letName 线程的绑定名——checkBinding 设/checkLetPattern 清空，TopLet 经 checkBinding 同享）+ letName 字段；tHelps 增 E1614–E1617 四码（diagnostics.toml remediation）。
  **D13 缺陷修复披露（两处前置缺陷，std.concurrent 黄金揭出，非静默绕过）**：(a) 泛型 sum 的 variant 模式载荷绑定未替换 scrutinee 实参——`match r { Ok(n) => n, Err(e) => 0 }`（r: Result<Int64,AppError>）绑 n 为 paramRef T、臂间 E0501；用户域独立复现证实先于修复存在；修：checkPattern PatVariant 载荷位 subst(pt, nt.args)。(b) 有用性矩阵 finiteCtors 载荷未替换——嵌套构造臂 `Err(conc.Panicked(m))` 在 paramRef 域上无法特化、被误判死臂 E0307；修：finiteCtors namedType 分支 substArgs(v.payloads, s.args)。两修复使 check-conc-sums-green 与 check-conc-taskhandle-annot-green 翻绿，同时是既有泛型匹配面的正确性修复。
  **测试自修正披露（黄金锚点为权威，m9aHead 实为 4 行）**：E1614 3,21→6,17；E1615 3,17→6,13；E1616 3,15→6,31（正负两形）；E1617 3,19→6,16；E0501 m.set 4,11→7,11；E0816 m.unlock 4,8→7,7；E0816 so.receive 5,15→8,18——T2 手算行号普遍按更短头计，非停点设计变更。装载族 alias-less 案（import std.concurrent 无 as）初稿漏 `pub type AppError` 声明——T2 红跑时被上一行 E1302 先停掩盖，补声明（用例本意：无别名装载）。
  **实现面自修正披露**：concurrentMembers 初版 unit 返回成员漏 ret: unitType{}（fnType.ret nil → 表达式语句 t.String() 段错误）——既有 retOrUnit/unitType{} 惯例对齐；成员视图初版未替换 t.args（裸闭包参数落 paramRef、`v > 0` 报 T×Int64）——memberOfType 插槽改 substFn(view, t.args, nil)。
  **中间态披露（测试先行预期红）**：task/scope/select 表达式在 typeOf 的既有默认守卫 panic（unreachable expr）——T6–T9 各自落真定型时逐族消化；当前 `go test ./internal/typecheck/` 全包中止于 TestTaskBlockFaces（panic 非 Fatalf），选中跑（除 T5–T9 五族）54 套件全绿零回归。
  **验证**：conformance 对账（475）：**绿 448**（431 既有零回归 + 2 锁定绿维持 + T3 已绿 e1601/e1611/e1612/e0824 + **T4 新翻 11**：check-conc-loading/alias/sums/shared/cond-sem/directions/taskhandle-annot 七绿 + check-e1614/e1615/e1616/e1617 四码）+ 红 5（shareable-bound-green/shareable-e0830-list/e1606=T5、e1608=T6、e1613=T9）+ panic 21（任务族 build/check 黄金——T6–T9 面）；parser/ast/diag/cli/codegen 全绿；gofmt/vet 清。

- [x] T5 Shareable 标记：D3 shareableOf 闭集计算、bound 位真分派（E0829 消失）、E1606、prelude 可见
  来源：proposal 目标 3 / design D3
  验证：`go test ./internal/typecheck/` Shareable 套件绿；typecheck_test.go :311-313 更新披露

  **完成记录（2026-09-07）**：typecheck.go 落五面。(1) 注册面：`shareableMarker` 单例（ifaceInfo{name:"Shareable"}——无 clause/无 assoc/无方法集，bound 位分派到形状判定而非 implements 走查）+ `shareableOf(Type) bool` 闭集计算（基础型 14 名/unit/value 记录全字段/value sum 全载荷/tuple 全元/同步 gc 十一类 declarer 判定、实参不下钻）+ `shareableBoundArg`（paramRef 位查 fnBounds 的 Shareable bound 携带——M6a 授集机器同 implementsFace 形，其余落 shareableOf）。(2) bound 位：boundIface 加 Shareable 先行分支（syms/子句参数阴影优先维持——用户自声明同名 interface 合法遮蔽、同名非 interface 仍 E0829；checkArity(nil) 拒 `Shareable<Int64>`），:3485 E0829 清单摘名——`T: Shareable` 今日红翻真分派，prelude 可见无 import 门。(3) 应用位：checkWhereSatisfies 与 impl-where 构造位（:5229 族）各加 Shareable 特支——两处同是 E0830 判断族（"holds at every call / at every instantiation"），design D3 点名前者、后者同型并入（披露）；E0830 报文经 face.String() 渲染 `"Shareable"` 与黄金逐字吻合。(4) E1606：checkImplDecl 头位在 resolveIfaceRef **之前**拦截（否则 marker 被 E1304 先停）——裸名 Iface 命中即报，锚 impl 关键字，报文与黄金逐字对齐（含 ` - ` 连字符段）；tHelps 增 E1606（registry remediation）。(5) 无值面收口：值位（:3810）`x: Shareable`/类型实参位 bnd 换 **E0821**（registry title 领衔，续文准确化「compiler-attached marker …stands in bounds, never in a value type slot」）；Dyn 位（dynIfaceRef）换 **E0820**（同型准确化「never as a box」）——spec ch18 原文「it occupies no value surface: …never a parameter or a box」落两码；bndShareable 常量与两处 bnd 调用删（边界组表缩至 TaskTime/StdModules/DomainGap/ArityGap/CalleeGap/Ctl/Comp 七行）。
  **实现面披露（两处）**：(a) newtype 不在闭集——design D3 清单穷尽（基础/unit/value 记录/value sum/tuple/同步十一类）未列 newtype，catOf 的派生规则不自动适用于成员判定；byval newtype 想入集须 spec 修订。(b) 泛型 value 记录/sum 的字段/载荷在实参替换后判定（`subst(ft, x.args, nil)`）——否则 `byval record Box<T>{v:T}` 的载荷停在 paramRef 域恒假；与 T4 的 checkPattern/finiteCtors 两修复同一形状。
  **既有测试更新披露**：typecheck_test.go :311 `wantBnd(bndShareable)` → `wantDiag(E0821, "compiler-attached marker of chapter 18", 1,9)`（marker 值位真诊断，M9a 落地面）；:312 currentCancelSignal bndTaskTime 维持（T6 拆半归位）、:313 advanceTime bndTaskTime 维持（Q3 回归钉）。
  **锚点债务修正披露（黄金/实际行为为权威）**：TestShareableMarker 的 E0829 用例 5:33 → **5:36**——锚在 bound 的接口名 token（boundIface 既有惯例「anchored at the bound's first token」= 名 token），T2 手算误按 where 主语位；其余五用例首跑即中。
  **验证**：TestShareableMarker 六用例绿；三枚黄金翻绿（check-conc-shareable-bound-green / check-conc-shareable-e0830-list / check-e1606-impl-shareable）；conformance 475 对账 **绿 451**（448 + T5 新翻 3）+ 红 2（e1608=T6、e1613=T9 面）+ panic 22（任务族，T6–T9 消化；较 T4 记账 21 多一枚系安全集关键词加宽的划分差异，非行为变化）+ 两锁定绿维持（bare-mutex-e1304、advancetime-bnd）；typecheck 选中套件绿（排除 T6–T9 四族与 TestConcConstructors 的 E1613 两用例——T9 面测试先行红维持）；parser/ast/diag/cli/codegen 全绿；gofmt/vet 清；真机黑盒三探（E0820 `Dyn<Shareable>` 7:15 锚名 token；泛型 caller 携 bound 递手 exit 0；缺 bound 递手 E0830 `"T" implements no "Shareable"` 8:12）。

- [x] T6 task 块检查：D6 TaskHandle<T> 定型、E1618 词法包含深度、E1106 体值资源型、效果 extent（bodyTags 换任务声明集）、函数体语境并入、currentCancelSignal 归位（E1608）
  来源：proposal 目标 4 / design D6
  验证：`go test ./internal/typecheck/` task 套件绿；advanceTime 半边维持为证

  **完成记录（2026-09-07）**：typecheck.go 落六面。(1) checker 五字段（scopeDepth——compound scope 体深，E1618 的门；taskDepth——task 体深，E1608 的门，均穿透闭包；taskVal/inTaskBody——task 体推断值机器，闭包体复位；bodyTask——E1401 报文的 task extent 标记）。(2) **taskType**：E1618 先行（scopeDepth==0，锚 task 关键字，报文与黄金逐字）；效果 extent 五件套换入（bodyTags=task 自声明集经 resolveEffectTags、bodyName="(task)"、inBody=true、inClosure=false、bodyTask=true）+ fnRet/prop 换入（nil + "(task)" propCtx——task 的值是推断的、非 Result 契约，体内 `?` 是 E1202 的 "(task)" 变体）；taskDepth++ 后 walkPlain 走体；尾值与 taskVal 出口 join（agree 单判定，镜像既有 fn 机器——`if c { return 1 } else { return 2 }` 在今日 fn 体即 E0501 拒，task 同判）；T 为 resource → E1106（锚尾表达式 exprPos 否则 task 关键字；报文入 E1106 家族形 `appears as a task block's value`）；值 = `namedType{taskHandleSum, [T]}`。(3) **taskReturn**（checkReturn 头部 inTaskBody 分派）：valued 贡献其型、bare 贡献 unit、非 agree E0501。(4) **scopeType**：timeout 子句 typeOf(…, Int64) 非 agree → E0501（锚子句表达式）；scopeDepth++/−；值 = 体块值型（bare 与 collectAll 两形）或 timeout 形 `Result<val, TimeoutError>`——**scope 四形的值面随 T6 基础设施落地**（design D7 的 scope 半边提前消化，T8 只余句柄纪律；scope-four-forms-green 随之翻绿，衔接记于 T8）。(5) closureType 体内走查复位 inTaskBody/taskVal（闭包的 return 归闭包自己的 fnRet 机器）；taskDepth 不复位——E1608 词法包含穿透闭包（单测绿）。(6) **currentCancelSignal 拆半**：ident 值位返回 `fnType{ret: CancelSignal}`、call 位 fnValueCall 同型；taskDepth==0 → E1608（锚名字 token，报文 sit 子句经 bodyName——空时 module-top-level 变体）；advanceTime 半边维持 bndTaskTime（Q3 回归钉）。
  **AST/parser 增补披露**：TaskExpr 增 EffectLine/EffectCol（design D6 的 resolveEffectTags 需段首 tag 位作解析锚——T3 节点未记，本步补；parseTask 捕获 parseEffectSegment 的位返回值）。
  **黄金锚点修正披露（实现前，自有新增）**：check-e1401-task-extent 11:29 → **11:34**——T1 生成器 marker 子串置于 `io { fetch()` 首字符（即段 tag token），与既有 E1401 家族纪律（fnCall 锚 exprPos(x.Fn) 调用名）矛盾；按家族纪律修正。
  **单测锚点债务清偿披露**：E1618 3,13→6,13、E1401 8,26→11,34、E1106 15,26→17,35、E1608 3,13→6,13、E0501 timeout 3,28→6,27——T2 手算行号未计 m9aHead/m9aFile 前缀行数，非停点设计变更。
  **单测自修正披露**：closure 穿透用例 `|| { … }` → `fn() -> conc.CancelSignal { … }`（零参短闭包不存在——ch12 规则 E0105 先停；测试拼写非设计面）。
  **实现面修正披露（落盘后、跑测前自审）**：join 初版对 `if c { return 1 }` 后裸 `()` 尾的反向象限漏判（曾静默取 unit）——改 agree 单判定覆盖四象限，黑盒双形实证（正向 8:13、反向 11:13）。
  **既有测试更新披露**：typecheck_test.go :315 currentCancelSignal wantBnd(bndTaskTime) → wantDiag(E1608)（M9a 落地面）；:316 advanceTime 维持 bndTaskTime（Q3 钉）。
  **验证**：TestTaskBlockFaces 全绿（E1618/E1401/E1106/E1608 四负形 + cancel-signal 绿 + 闭包穿透绿 + extent 零贡献绿）；conformance 对账（475）：**绿 462**（安全集 419 整跑 + 嫌疑集 43 逐枚；较 T5 末 451 增 11——e1618/e1401-task-extent/e1608/cancel-signal/task-pure-main/task-scope/scope-four-forms 七枚检查黄金翻绿 + build-bnd-conc-ctor/fn/scope-stmt/scope-value/two-tasks 五枚 build 边界 panic 转 green）+ 红 8（T7 捕获族 5：e1602×2/e1603/e1604/e1605；T8 句柄族 2：e1607×2；T9 select 1：e1613——panic→red，taskType 已走查体、各自诊断待落）+ panic 5（T9 select 族：e1609/e1610/select-green/select-await-green/io-arg——SelectExpr 未定型）；两锁定绿维持（bare-mutex-e1304、advancetime-bnd）；parser/ast/diag/cli/codegen 全绿；gofmt/vet 清；真机黑盒五探（if-return 对 E0501、bare-return 反向 E0501、module 顶层 E1608 变体 5:9、join 同意形 exit 0、task 内 `?` E1202 "(task)" 无 panic）。

- [x] T7 捕获纪律：D4 taskCaptures walk + 判序（E1603→E1604→E1605→E1602）
  来源：proposal 目标 5 / design D4
  验证：`go test ./internal/typecheck/` 捕获套件绿；E1104 资源面零触碰为证

  **完成记录（2026-09-07）**：resource.go 落 chapter 18 纪律段（design D4 单文件定位）。(1) **taskCaptures walk**：自研轻量自由变量走查（未复用 ch12 闭包捕获 ledger——ch18:135 明文分治），位置感知（绑定自其语句起作用、分支绑定留在分支内、guard 不见臂绑定、闭包参数/for/match 模式/scope resource 头各自绑域），收集顺序即源序；Assign 目标名与 `self.field =` 的 self 记为使用；嵌套 task 体传递计入。**判序**（每捕获按规范序首中即停）：① var → E1603（无型视——varScopes 新 checker 字段按块层记录 var 性，仅 walkItems 绑定 case 写[含 var 解构 patBindNames]，参数/模式/scope 头皆 let 形）；② resource 品类 → 类型自身 impls 扫 `release` 外 mut self 方法 → E1604（resourceMutSelf），release-only 资源捕获合法且 `continue`（后序不再适用——释放义务不转移，E1104 机器零触碰）；③ 型提及无 Shareable bound 的泛型参数 → E1605（mentionsUnboundedParam 递归 args/载荷/fn 型位，paramRef 位经 T5 shareableBoundArg 查 fnBounds）；④ Shareable 判定不成立 → E1602。pre-pass 于 body walk 之前（design D4：捕获诊断先于体内容诊断——e1602-list 黄金的 `put(xs)` 未解析调用不先停即证）。**E1602 报文两形**（registry 描述的两种失效面）：gc 绑定 `which no synchronization discipline covers` / value 形状含 gc 部分（tuple、byval record、byval sum）`a value shape whose parts hold gc state the Shareable set excludes`。
  **实现面修正披露（两处）**：(a) ④ 判定用 shareableBoundArg（T5 的 bound-aware 机器）而非裸 shareableOf——裸 paramRef 带 Shareable bound 会被误判 E1602，使 E1605 自身补救文（"declare the parameter with a Shareable bound"）不可达；design D4 措辞「shareableOf 不成立」按此读作「Shareable 判定不成立」。(b) E1602 中段子句按形状分两形（首版单一 `which no synchronization discipline covers`，e1602-tuple 黄金揭出 value 形状形）。
  **单测自修正披露（T2 用例意图修正）**：release-only 资源绿用例体值 `{ f }` → `{ f.fd }`——体值为 resource 触发 T6 的 E1106（spec 1800:101 明文 `T MUST NOT be a resource type`），原用例本意探捕获合法性而非体值面；捕获判断与体值判断是两条规则，绿用例须同时满足两者。
  **单测锚点债务清偿披露**：E1603 5,35→8,35、E1602 7,35→10,35、E1605 4,35→7,35（T2 手算行数未计 m9aHead 前缀；黄金 e1603 8:35 一致）。
  **验证**：TestTaskCaptureDiscipline 全绿（判序 var 先发/同步捕获绿/gc 记录红/release-only 绿[体值改 fd]/无 bound 红·有 bound 绿/体内部绑定非捕获）；ch13/ch12 纪律套件全绿（ScopeResourceStmt/ResourceLiveness/ResourceAlias/ResourceComposite/Closures——E1104 资源面零触碰为证）；conformance 对账（475）：**绿 467**（T6 末 462 + 捕获五枚翻绿 e1602-list/e1602-tuple/e1603/e1604/e1605）+ 红 3（e1607×2=T8、e1613=T9）+ panic 5（select 族=T9）；安全集 419 整跑零回归；parser/cli/codegen 绿；gofmt/vet 清；真机黑盒五探（闭包穿透捕获 E1603 10:24、顺序遮蔽先用后绑 E1603 9:21、var 解构 E1603 8:35、双用首用锚 E1602 11:21、模块级名非捕获 exit 0）。

- [x] T8 scope 块与句柄纪律：D7 scope 四形定型（timeout 形 Result<T,TimeoutError>、E0501）+ D5 句柄路径分析（E1607 双触发形、早出口 discharge、嵌套分帧）
  来源：proposal 目标 6 / design D5 D7
  验证：`go test ./internal/typecheck/` scope/句柄套件绿

  **完成记录（2026-09-07）**：resource.go 落 chapter 18 句柄纪律段（design D5——ch13 活跃性 walk 同骨架、one-shot 消费语义改造），scopeType 于 body 走查洁净后触发（resCheck 同位置）。(1) **hdlState 三态**：pending/fired/foreign——foreign 是本 walk 不拥有的遮蔽名（闭包参数/match 臂与 for 与 tuple-let 模式名/scope resource 头名/select case 绑定名——guard 不见臂绑定），其上的 await/cancel 不计 fire。(2) **判定与消费**：消费 = 绑定名上的 await()/cancel() 调用（Call 头 Member 的 Recv 解析到该绑定）；绑定所在块的块尾 fall-through 仍 pending → E1607 unawaited（锚创建位 task 关键字，报文与黄金逐字含共享补救尾）；同路径第二次 fire → E1607 double-fire（同锚、双触发家族共用「cancel after an await」报文形——registry 单一描述覆盖两向）。早出口（return/break/continue）discharge（design D5 明文）；`?` 不终止路径——传播路径不创建、快乐路径照判，双向健全。(3) **分支与环**：join 语义 pending-on-any（一臂消费一臂穿透 → join 后仍欠，单臂 join 缺失用例证）；环体单遍走查 + entry join（while/for 零迭代路径并入环后判欠、loop 无入口 join）——骨架精度与 resWalk 对齐（跨迭代第二次 await 同句柄不判双触发，resCheck 同粒度近似）。(4) **出现位两形**（D5 修订版「scope 体内一切 TaskExpr 出现位」）：let 直接初始化位绑定入分析（walk 并入 task 体为其自有块——体内创建者体内判，无静默漏判）；其余出现位（裸语句/实参位/if 两臂产句柄）= 弃置形 E1607 于该 task 关键字（报文绑定名以 `<expr>` 占位，recvText 先例）。
  **衔接披露（T6 记录预告）**：D7 scope 四形值面（bare/collectAll = 体块值、timeout 形 Result<val, TimeoutError>、timeout 子句 E0501）已随 T6 基础设施提前落地并翻绿 scope-four-forms-green——本步只落 D5 句柄路径分析面，无重复实现。
  **D13 补齐披露（tHelps 面，修复非绕过）**：T7 落 E1602–E1605 时未同步 tHelps 条目（map 头注声称携带 registry 各码 remediation）——本步补 E1602–E1605 与 E1607 五条（diagnostics.toml 逐字）；Human() 不渲染 help（JSON 面专属），黄金 stderr 零影响（e1608 黄金先例已证）。
  **单测锚点债务清偿披露**：TestHandleDiscipline 五处（4,17→7,17 ×3、4,9→7,9、10,17→13,17）——T2 手算未计 m9aHead 前缀行数，非停点设计变更。
  **实现面决议披露（design 静默处，均保守/字面读）**：(a) 判定点由「scope 体尾」按块泛化——if 臂/环体/BlockExpr/task 体/嵌套 scope 体内创建的句柄在其绑定块尾判（名字死后无可消费位；scope 体即外层块，语义等价）；(b) walk 下降入 task 体与闭包体（创建位皆得判定；外层句柄在其中消费按词法计 fire——静态近似，任务体/闭包体的执行不在 scope 控制路径上）；(c) `let _ = task {...}` 弃名绑定入分析（显式丢弃即未消费，报文绑定名 "_"）；(d) 实参位弃置形即使被调方参数声明句柄型仍判 E1607（ch18 纪律只认直接 fire，无参数位转移形——与 ch13 资源转移规则分治）；(e) Assign 重绑句柄名将状态重置 pending（var 形重赋值后的新句柄仍欠消费）。
  **验证**：TestHandleDiscipline 全绿（8 用例：unawaited/裸语句/双触发三负形 + break discharge/if-else 各消费/环内创建消费/嵌套分帧双绿 + 单臂 join 缺失/嵌套内层缺失两负形）；ch13/ch12 纪律套件全绿（ScopeResourceStmt/ResourceLiveness/ResourceAlias/ResourceComposite/Closures——resCheck 零触碰为证）；TestScopeSelectTyping panic-红与 TestConcConstructors E1613-红维持（T9 面测试先行）。conformance 对账（475）：**绿 469**（安全集 419 整跑 + 嫌疑集 56 逐枚；较 T7 末 467 增 e1607-unawaited/e1607-double-fire 两枚）+ 红 1（e1613=T9）+ panic 5（select 族=T9）；两锁定绿维持（bare-mutex-e1304、advancetime-bnd——安全集内）；parser/ast/diag/cli/codegen 绿；gofmt/vet 清。真机黑盒九探全中：return 出口 discharge exit 0、`?` 后创建仍欠 12:17（线性走查健全性）、零迭代 join 欠 7:17、别名边界 `let u = t` 不解除 t 义务 7:17（D5 字面）、裸语句形 7:9、if 两臂弃置 7:24、双 await 形 7:17、实参位弃置 12:13、弃名绑定形 7:17。

- [x] T9 select 定型与 E1613：D7 四等待源闭集（E1609）、臂一致（E1610）、绑定名作用域、E1613 同绑定嵌套访问
  来源：proposal 目标 7 8 / design D7
  验证：`go test ./internal/typecheck/` select 套件绿

  **完成记录（2026-09-07）**：typecheck.go 落两面。(1) **selectType**（typeOf 增 `case *ast.SelectExpr` 分派）：每 case 先 typeOf(source)（member 调用既有路，其自身诊断先行），再 **waitSourceBind** 判四等待源闭集——`Channel<T>/ReceiveOnly<T>` 的 receive() → 绑定 `Option<T>`、`TaskHandle<T>` 的 await() → `Result<T, TaskPanic>`、`CancelSignal` 的 awaitCancelled() → unit（接收者型 × 方法名双判；AST 非 Call/Member、方法名或接收者型不匹配 → **E1609** 锚 source 表达式首 token，报文与黄金逐字）；绑定名（非 wildcard/`_`）入 case 体 locals 层（体定型后弹出——select 后不可见，E1304 面）；臂体定型无期望，逐臂与首臂 agree 判定 → 不一致 **E1610** 锚该臂 case 关键字（SelectCase 自带 Line/Col——T3 落位），一致者 = select 值型；select 语句位非 unit 落既有 E0605。(2) **E1613**：nestedAccessCheck + checkNestedAccess（resource.go ch18 段）——update/read 调用（接收者为四状态细胞 namedType、实参含闭包）后走查闭包体：Recv 为 Ident、名字等于外层绑定且非闭包参数遮蔽、方法名 ∈ update/get/set/read → **E1613** 锚内层接收者首 token（methodCall 家族锚惯例），报文含外层绑定/外层方法/内层方法三槽（与黄金逐字）。
  **黄金修正披露（实现前，自有新增）**：build-bnd-conc-io-arg 源自相矛盾——select 臂体 `1`/`2` 使 v: Int64（D7「臂型一致者 = select 值型」），`match v { Some(x) => x, None => 0 }` 的 sum 模式落 E0303 先停，永达不了其本意的 build 边界；按 E0305 家族规则（base 型字面臂须通配兜底）改 match 为 `1 => 1, _ => 0`——黄金本意（io 调用 + conc 组合的 main 体 build 边界 exit 70）不变，实跑过。
  **单测自修正披露（T2 用例笔误，黄金为权威）**：TestConcConstructors 的 E1613 绿例源 `a.update(|v| a.get() + b.get())` 恰是同绑定红形（与黄金 check-e1613-nested-access 同形——实现落地即报 8:26）；改 `a.update(|v| v + b.get())`（真跨绑定自由），与 y 行 `b.update(|v| v + a.get())` 成双向自由对。
  **实现面修正披露（落盘后黑盒揭出）**：E1613 挂点首版置于 methodCall 尾——`Mutex<Int64>.update` 成员视图经 substFn 替换实参后不含 paramRef，fnContainsParam 为假，调用走 fnValueCall 旁路（RwLock.read 因 U 位未替换仍走 methodCall）——真机黑盒 clean check 揭出；改挂 callType 的 methodCall/fnValueCall 两路汇合处（参数定型之后统一走查——纪律后置次序与 resCheck/taskCaptures 一致）。
  **实现面决议披露（design 静默处）**：E1613 走查边界照「不追进其他函数」字面——进回调自身的块/if/match/BlockExpr/循环，不进嵌套闭包体、task 体、scope 体、select 体（后三者为并发体，与函数同列边界）；闭包参数同名遮蔽正确（参数集合判，`a.update(|a| a + b.get())` 绿）。
  **单测锚点债务清偿披露**：E1609 6,17→9,18、E1610 6,9→10,9（锚不一致臂的 case 关键字）、E1613 4,29→7,26（黄金 8:26 系黄金源多一行，单测源行号为 7）、E1607 select 消费后 5,17→7,17——T2 手算行号偏差，非停点设计变更。
  **验证**：`go test ./internal/typecheck/` **全包 58 套件绿**（T2 八测试函数全过——M9a 测试先行红全数翻绿）；conformance **475 全绿**（T8 末 469 + e1613 红→绿 + e1609/e1610/select-green/select-await-green 四枚 panic→绿 + build-bnd-conc-io-arg panic→边界绿[源修正后]）；parser/cli/codegen/diag 绿；gofmt/vet 清。真机黑盒七探全中：闭包参数同名遮蔽绿 exit 0、深闭包边界不追（无 update 语境的闭包内 a.get() 绿）、case 绑定名 select 后不可见 E1304 12:13、cancel 作源 E1609 10:22、同句柄双源且先 cancel → double-fire E1607 7:17、ReceiveOnly.receive 作源绿 exit 0、RwLock.read 同绑定嵌套 E1613 7:24（read/read 报文形）。

- [x] T10 收尾全量验证：conformance 全绿（431 + 新增）、`go test ./...` 清缓存全绿、gofmt/vet 清、真机电池（`we check` 诊断面黑盒 + D10 不可达清单逐条实证 + D9 停点逐形）
  来源：proposal 目标 1–9 / design D9 D10 D11
  验证：`go clean -testcache && go test ./...` 全绿；T1/T2 红名单逐枚翻绿零回归

  **完成记录（2026-09-07）**：conformance **475 全绿**（T9 末基线维持）；`go clean -testcache && go test ./...` 十包全绿（修复后重证）；gofmt -l 空、go vet 清。
  **D9 停点单测逐形钉（收尾面补齐披露——T3–T9 未落、D9 明文「新增 Expr 节点在 codegen 各 switch 的 default 停点由单测逐形钉」）**：internal/codegen/m9a_test.go——TestM9aBodyBoundaryWhats 十形（语句位 scope/task/select、let 初始化位 task/scope 值形/select、conc 构造绑定形与弃置形、io 实参位 task 表达式 → 全部 bndMainBody；含并发 fn → bndOtherFns；AST 造形仿 m8_test.go 先例，What 逐字断言）+ TestM9aStdConcurrentImportErases（std.concurrent 走 M8 std 擦除路径零改——hello IR 字节对拍 m8HelloIR）。全部首跑即绿（停点由 T3–T9 的既有 default 臂承载，本步是钉而非建——「显式 case，never vanish」的回归钉）。
  **D10 八条不可达清单真机电池（真二进制，逐条实证）**：①调度与交错——双 task scope 源 check 净、项目模式 build 停 bndMainBody exit 70；②六族原语运行时——Mutex/Cond[wait 谓词形]/Semaphore/Channel 全成员链 check 净、build 停 bndMainBody；③timeout 真实时钟与 Err(TimedOut) 运行值——scope timeout 值形 match check 净、build 停；E1616 运行时陷阱半边由检查期 E1616（Semaphore(0)）先拦，acquire 阻塞不可达；④TaskPanic 边界捕获——task 体 panic("boom")（Never 满足任务体值）+ await + match check 净、build 停；⑤select 运行时取臂——双 channel 源 select check 净、build 停；⑥advanceTime/虚拟钟——check+build 皆停 bndTaskTime exit 70（Q3 回归钉维持）；⑦transaction/WeakRef/内存区域——语法未落，parse 期 E0105（`transaction {` 报文「construction heads are PascalCase」）exit 1；⑧vet 层跨函数嵌套访问——helper 内 a.get() 由 update 回调调用的形 check 净（E1613 走查按「不追进其他函数」边界，D10 判定其属 tooling 章）、build 停 bndOtherFns。结论：八条的运行时行为在 M9a 构建下全部不可达，诚实停在 M8 边界组。
  **D13 缺陷修复披露（真机电池 p3 揭出，既有面缺陷，非静默绕过）**：`scope timeout(n) { … }` 的值型 Result<T, TimeoutError> 在**未 import std.concurrent** 时流经 stdConcSum 的回退分支——返回零变体 sumInfo；finiteCtors 将零变体 sum 当「有限空构造集」，useful 的通配探针遍历零键返回假 → `match r { Ok(n) => …, Err(_) => … }` 假报 E0307（Err 臂判死）。**同一形状是既有面缺陷**：M5 的 listSum/mapSum/setSum/rangeSum 同为零变体——`match xs { _ => 0 }`（List scrutinée）今日同样假 E0307（真机独立复现证实先于本变更存在）。修：finiteCtors namedType 分支零变体 → 非有限域（opaque 域走 default 矩阵）——「零变体 sum 不是合法用户形（parser 把门），只能是检查器的不透明形状」。验证：scope-timeout match 无 import 形 check 净、List 通配形 check 净、Option 对照净、**E0305 穷尽性不受损**（缺 Err 臂仍 E0305 精确报「variant "Err" of "Result" is not covered」）；三枚既有 E0307 黄金皆用户 sum 面（Light）零触碰零改写；typecheck 58 套件 + conformance 475 + 全仓 clean-cache 全绿零回归。
  **电池源自修正披露**：p2 初稿 `c.wait()` 撞 bndArityGap——Cond.wait 带谓词闭包（design D2 签名表 `wait(mut self, f: fn(T)->Bool)`），改 `c.wait(|v| v > 0)`；p3 初稿的 E0307 即上述缺陷本体（非源笔误，缺陷修复后转绿面）。


- [ ] T11 验证阶梯 + 实现审查（welang-code-review 7 条，真二进制证据）+ 归档（status → archived、目录带日期前缀）+ roadmap M9 行拆写 M9a/M9b 两行（双语）
  来源：研发流程 + design D13 披露义务
  验证：gofmt -l 空、go build/vet 过、validate --all --strict 过、docs_sync 对数不变（纯实现零文档对）、git diff --check 干净；归档目录存在

- [ ] T12 记忆更新与提交：project-overview 增 M9a 条目（下一里程碑指向 M9b）、MEMORY.md 索引行更新；英文提交信息（无署名 trailer；refr/ 不入提交）
  来源：研发流程 + 用户红线
  验证：`git log -1` 消息为英文且无 Co-Authored-By；`git show --stat` 无 refr/ 路径
