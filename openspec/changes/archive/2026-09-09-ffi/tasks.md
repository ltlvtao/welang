# tasks — ffi（M12）

- [x] T1 测试先行 A：D6 黄金矩阵落盘（预计 32），对当前构建运行必须先红；既有 641 枚零意外回归（负向对账：bndFFI 行删除后无既有黄金钉 foreign 面）
  来源：proposal 做什么 / design D6
  验证：`go test ./internal/conformance/ -run TestGoldenCases` 红（新增失败清单 + 既有 641 零回归），红因分类记于完成记录；D6 各桶逐枚对账落点；黄金源过编译器（M10c 教训——非法源以 base 二进制探针负例核对）

  完成记录（2026-09-09）：
  - **32 枚落盘**，D6 逐桶对账：check ffi 14（e1701/e1702/e1703/e1704/e1705-composite/e1705-fntype/e1705-never/e1706-string/e1706-bytes/e1707-construct/e1707-update/e0404-dup/green/cross-module）、资源 4（byres-green/byres-e1101/byres-e1106/byres-transfer）、效果 3（eff-e1401/eff-custom-tag/eff-bare-pure）、never 1（never-return check 绿）、build 2（e1906 无 native//e1906-partial 恰报缺者）、run 4（scalar/string/**never**/opaque）、fmt 2（block 脏→净/clean 定点）、doc 1（foreign pub 条目不入页）、vet 1（调用面净）——与 D6 表唯一偏差见披露 3。
  - **先红实证**：32/32 红，红因二分——31 枚顶层 foreign 块停 parser bndFFI 边界（exit 70 `chapter 19 (ffi) forms are not implemented`，负例黄金 want E 码 exit 1 / 绿枚 want 0）；1 枚 check-ffi-e1702 块内 foreign 今日落 E0105 泛形（`"foreign" fits no statement production`，锚 4:5 一致）vs want E1702 专码。零 OTHER。
  - **负向对账**：`git status internal/conformance/testdata/` = 32 新增（`??`）零既有文件触碰；全量跑既有 641 全绿（非-ffi 失败清单为空）——bndFFI 行删除后无既有黄金钉 foreign 面，坐实 proposal 影响范围预判。
  - **黄金源过编译器变通实录**（M10c 教训；foreign 面今日不可编译，等价形探针 + 章文场景逐字）：base 二进制（/tmp/we-base，提交 4bc15e3 构建）等价探针全过——① Never 裸语句位丢弃豁免（`weAbort()` 语句 + return → check 绿，E0605 对 Never 豁免实证）；② 零字段 with 更新 `Socket { with &s }` check 绿（普通 gc record 同形）；③ byres + impl Releasable 空手 return 体 + scope resource 空块绿（候选期）；④ E1106 注解位 detail 以普通 byres record 等价源探针取得逐字（`appears as a generic argument at the instantiation`，锚 = File token 位）；⑤ E1401/E0404 detail 既有黄金逐字引用。新码 detail（E1701–E1707/E1906）黄金定形即实现契约，helps = 注册表 remediation（T4 实现期逐字挂接）。
  - **披露 1（规范层发现，D13 面）**：ch19:39 第二示例拼写 `fn name(params) -> type effect tag1 tag2 ...` 与 ch6:53 + ch16:36 权威（段在参数表与箭头**之间**，「between the parameter list and the arrow or body」）字面矛盾——ch19 同句**散文**部分与两章一致（「between the parameter list and the arrow or the declaration's end」），仅字面示例画反，笔误级。黄金一律按 ch6/ch16 权威拼写（`fn name(params) effect tags -> Ret`；裸段 `fn name(params) effect -> Ret`）。本变更零规范增量立场不修章文；**roadmap follow-up 登记**（spec 变更修 ch19 示例拼写）。
  - **披露 2（Q3 裁决交互定谳）**：ch19:39 「the name under chapter 1's conventions」——foreign 声明名受 E0012 camelCase 门（与 C 符号原样直通不冲突：C 侧符号合法可取 camelCase 名）。黄金 native/ 夹具与声明一律 camelCase（weAdd/boxInt/unboxInt/firstByte/exitWith/absByte/ioWrite/readPin/weAbort/addInt/mulInt）；首批草稿的 we_xxx snake_case 名被探针 E0012 拦下后全量更正（黄金即契约：T3 parser 名字表挂接后 E0012 对 foreign fn 名自然同查，零额外机制）。
  - **披露 3（D6 run 桶偏差定数）**：run 桶 bytes 枚替换为 never 枚——We 侧今日无 Bytes 产生面（无 Bytes 字面量、无构造面、stdlib 未落 B2），端到端 Bytes 参数黄金源不可写；替换枚 run-ffi-never（native `exitWith` 真调 libc exit → 进程 exit 7）端到端直证 Never 声明返回位 + codegen void/unreachable 面。双参展开 (ptr,len) 的运行时实证由 run-ffi-string 承载（C 侧 `firstByte(const char* p, long n)` 真读首字节 'h'=104）。Bytes 缓冲惯用法（章文「copy into a caller-provided buffer」路线）随 B2 stdlib-real 面。
  - **披露 4（黄金源笔误实现前修正，非缺陷）**：update 拼写草稿曾写 `Socket { ... with &s }`——`...` 非 token（ch8:62 场景拼写无省略号，lex maximal munch 下 `..` 是 range 单 token，探针 E0105 拦下），修正为 `Socket { with &s }`；首批 32 枚的效果段位序草稿普遍画在返回类型后，探针 E0105 拦下后按 ch6/ch16 权威全量更正（见披露 1）。
  - 生成方式：/tmp 脚本批量生成（锚位机械断言逐枚内嵌——`src.split("\n")[line-1].index(tok)+1 == col`，手数错误当场拦截，实际抓出 fn 名列号 ×3、行号 ×2、段序改后返回类型锚 ×2 共 7 处）。
- [x] T2 测试先行 B：单测先红——parser ffi（产生式/E1701–E1704/E0105 位/E0404/裸段仅块内）、typecheck ffi（跨界集正负/opaque/E1707/byres 纪律/效果三面）、codegen ffi（declare 形/ABI 映射/直呼无槽）、cli m12（native/ 发现/llvm-nm 查证/E1906 报文与无工件/gate 扩查）
  来源：proposal 做什么 / design D1–D5
  验证：`go test ./internal/parser/ ./internal/typecheck/ ./internal/codegen/ ./internal/cli/` 红（新用例引用未定义符号/断言不匹配），red 证据记于完成记录

  完成记录（2026-09-09）：
  - 四文件落盘：`internal/parser/ffi_test.go`（9 函数：产生式/ABI/条目闭包/pub 形/E1701–E1704/裸段作用域/E0404 两形/pub-face）、`internal/typecheck/ffi_test.go`（6 函数：跨界集参数位/返回位/opaque 纪律/byres 纪律/效果三面/mock 排除）、`internal/codegen/ffi_test.go`（5 函数：declare/ABI 全映射/直呼无槽/Never unreachable/ForeignNames 收集）、`internal/cli/m12_test.go`（4 函数：nativeSources 字典序与空集/checkNmVersion gate/definedSymbols 解析/missingForeignNames 差集）。
  - **red 证据**：`go vet` 编译红三包——parser `undefined: ast.ForeignBlock`（ffi_test.go:24）、codegen `undefined: ast.ForeignBlock`（ffi_test.go:26）、cli `undefined: nativeSources`（m12_test.go:23）；typecheck 行为红 6/6（`parse boundary: chapter 19 (ffi) forms`——wantOK/wantDiag 于 bndFFI 边界）。既有测试文件零触碰。
  - **API 契约随测试定形**（T3–T6 按此兑现）：`ast.ForeignBlock{ABI string; Items []Item; Line, Col int}` + `FnDecl.Foreign bool` + `RecordDecl.Opaque bool`（D1）；`codegen.ForeignName{Module, Name string; Line, Col int}` + `codegen.ForeignNames([]ProgModule) []ForeignName`（D5 链接查证面）；`cli.nativeSources/checkNmVersion/definedSymbols/missingForeignNames` 四函数（D5）。E1804 foreign 类目 detail 定形 `— abs is a foreign function; only a module-level monomorphic fn is mockable`（既有三形同构，D9-6 披露面）。
  - **段序契约钉死**：全部测试源按 ch6/ch16 权威拼写（`effect tags -> Ret`、裸段 `effect -> Ret`）——T1 披露 1 的章文矛盾在测试层只按权威拼写。顶层裸段拒绝报文以真机探针取得逐字（`"{" where an effect segment names at least one effect`，M7 既有零改）。
- [x] T3 parser 塔：ast.ForeignBlock + FnDecl.Foreign/RecordDecl.Opaque + parseForeignDecl（E1701/E1702 挂接/E1703 段语境参数/E1704/E0105 位）+ 名字表 + 派发表翻位
  来源：proposal 做什么 1 / design D1
  验证：`go test ./internal/parser/` 绿；check ffi 黄金字码位翻绿对账（E17xx 码形部分）；既有黄金零回归

  完成记录（2026-09-09）：
  - **实现**：ast 三增量（`ForeignBlock{ABI, Items, Line, Col}` + item 标记、`FnDecl.Foreign`、`RecordDecl.Opaque`——标记位优于回溯父节点，遍历器免携带上下文）；parser 五面——① 派发表 `case "foreign"` → `parseForeignDecl`（bndFFI 常量删除，边界表仅余 mut parameters）；② `parseForeignDecl`：ABI 位非串或 ≠"c" → E1701（detail `— got ` + token 原文，串形带引号即黄金形）；条目循环 head 锚（pub 含入）；③ `foreignFnItem`：名位 dupCheck+checkCamel（E0404/E0012 自然同查零额外机制）→ 泛型探测 `<` 先于参数表 → E1704（锚名）→ 参数表复用 → 非 `effect` → E1703（锚名）→ `parseEffectSegment(true)` → 可选 `->` Ret → `{` 体探测 → E0105 无体专形（锚 head）→ 名字表登记；④ `foreignRecordItem`：`parseRecordDecl` 全量复用（E0404/E0011/类别前缀照常）+ `Opaque` 标记 + 字段数非零 → E0105 带字段专形（锚 head，解析后判——无半树逃逸，parseTestDecl 同型）；⑤ `parseEffectSegment(inForeign bool)` 语境参数：裸段（`effect` 后非 ident）仅此语境放行，tags nil、line/col = effect 关键字位（tagged 形维持首 tag 位不变）；五调用点传 false。块内 `foreign` 关键字从 parseStmt E0105 泛形清单提出 → E1702 专码（锚关键字，黄金 4:5 一致）。helps 补 E1701–E1704 四条（注册表 remediation 逐字）。
  - **翻绿对账**：conformance ffi 32 枚中 **8 枚绿**——check 5（e1701/e1702/e1703/e1704/e0404-dup，全部 parser 级码）+ fmt 2（block/clean）+ doc 1（excluded——doc 走查 type switch 无 ForeignBlock case 天然排除，零 doc 改动）；红 24 枚全 ffi（check 17 待 T4、build 2 待 T6、run 4 待 T5/T6、vet 1 待 T4），**既有 641 零回归**（非 ffi 红清单为空）。`go test ./internal/parser/` 全绿（含 T2 九函数）；cli/codegen 包编译红 = T2 测试先行待兑（nativeSources 四函数待 T6、ForeignNames 待 T5），`go build ./...` 净——CLI 面 conformance 673 枚照跑。
  - **披露 5（D13 缺陷修复：fmt 前置负号空格）**：M12 fmt 黄金揭出 M11 缺陷——`gap` 里二元空格行先于一元行，`-` 的一元位被 `isBinaryKind("-")` 先捕，`-3` 全形被空格化为 `- 3`（`!`/`~` 非二元 kind 故幸免）；与 0200-grammar 规范自身示例 `let n = -x` 紧贴拼写矛盾（fmt 章只钉二元运算符空格，一元未列 = 不空格）。修复 = 一元行提前于二元行（一元位定义上非二元位）+ fmt_test "prefix operators tight" 用例钉（`-3`/`1 - -3`/`--a` → `- -a`/`!flag`/`~a` 五形 + 定点断言）。既有 fmt 黄金零含此形面（机械扫描定谳，无静默重写面）；fmt-ffi 两枚随后翻绿。
  - **披露 6（T1 黄金锚位笔误更正）**：e1703/e1704/e0404-dup 三枚 stderr 锚位手写钉在 fn 关键字（2:5/6:5），与既有 E0404 机器（dupCheck 于名 token 发码——既有黄金全部名位锚，如 check-e0404-interface-method 3:8）及 T2 单测契约（「锚 fn 名」2:8/6:8）矛盾；黄金位反而要求 foreign 专用 dupCheck 变体 = 新机制，违背 D1「零额外机制」。更正为名位（2:8/2:8/6:8），E17xx 族锚法与报文主语一致（E1703 报 `"dial"`、E1704 报 `"id"`）。
  - **披露 7（T1 fmt-ffi-block 期望与 fmt-blank 先例矛盾更正）**：黄金期望块内空行删除，但 fmt-blank 先例（M11 已落）钉块内源空行保留一条（reformatter 不做跨行删除判断——空行分段是作者判断，与「never wraps」同理），且定点成立。按先例更正黄金期望（块内每源空档保留一条 + `abs(-3)` 紧贴），机器输出与先例同构。
  - 遗留观察（无需动作）：spec `fn name(params) effect tags` 段序拼写下 `->` 在段后，`parseTypeRef` 消费返回类型——泛型返回 `-> Box<T>` 的 `<`/`>` 走类型角度分类默认紧贴（分类走查只标比较位），foreign 块内类型位无需 walkItem case（无表达式、无闭包管）。
- [x] T4 typecheck 塔：foreign 条目 ingest + 跨界集检查器（E1705/E1706 分位）+ E1707 构造/更新位 + byres 全纪律既有机器直证（E1101/E1105/E1106/三通道）+ mock 面 E1804 排除 + 效果零新机制直证
  来源：proposal 做什么 2–3 / design D2 D3
  验证：`go test ./internal/typecheck/` 绿；check ffi/资源/效果/never 桶黄金翻绿对账；既有黄金零回归

  完成记录（2026-09-09）：
  - **实现七面**（全在 `internal/typecheck/typecheck.go`）：① `recordInfo.opaque` 位（pass1 从 `RecordDecl.Opaque` 填）；② pass1 加 `case *ast.ForeignBlock`——条目按自身种类入命名空间（fn → symFn、record → symRecord，无专有 symbol kind：边界是声明位不是命名空间类目）；③ pass2a 加 ForeignBlock case——fn 条目填 fnParams/fnTags/fnRets（与顶层声明同形，调用机器零改动直读）+ `checkCrossingSet`；record 条目走 record 机器同形（零字段循环零次、derives 若写照常查——D2「ch19 此面零新规则」）；④ `checkCrossingSet` + `crosses`：跨界集 = baseNames 全集（8 整型/Float32/Float64/Bool/Rune/String/Bytes——基类型表即跨界标量表，零新表）+ opaque record 裸引用（单指针句柄）；参数位 E1705（Never 形尾专句 `Never is a declared return position only`）、返回位 String/Bytes → E1706 先报、Never 放行（唯一信任点）、其余 E1705 返回形；锚一律 refPos（类型 ref 位）；⑤ Never 主权裁决：resolveTypeRef 的 slotAnn 位拒裸 Never（E0703）会先拦参数位 Never——foreign 签名两 slot 传 slotRet（slot 三态的唯一消费者就是 Never 判定，借位零副作用；E1705 的跨界集主权不被 E0703 抢先，注释钉明）；⑥ E1101 模块尾配对查扩入 foreign 条目（byres opaque 走 ch13 既有机器全纪律，报文逐字同）；⑦ mockableTarget symFn 加 Foreign 先拒（E1804 `a foreign function` 类目，锚 target 位）+ constructType 的 `rec := sym.rec` 后 E1707（先于 E0606/E0604——空真构造关死，构造与更新同头同码）。tHelps 补 E1705/E1706/E1707 三条（注册表 remediation 逐字）。
  - **翻绿对账**：typecheck 包全绿（T2 六函数 + 既有）；conformance ffi 32 枚中 **26 枚绿**——T4 新翻 18 枚（check ffi 9：e1705×3/e1706×2/e1707×2/green/cross-module + 资源 4：byres-green/e1101/e1106/transfer + 效果 3 + never 1 + vet 1），红 6 = build 2（待 T6）+ run 4（待 T5/T6），**既有 641 零回归**（非 ffi 红清单为空）。效果零新机制直证成立：E1401 两枚（io 内建 tag + gpio 自定义 tag）走 fnTags 既有机器、裸段解析 nil = 纯（eff-bare-pure 绿）；E1105/E1106/三通道由 byres-transfer（scope resource 包裹 + take 传参转移）全绿直证。
  - **披露 8（T2 单测源笔误，D13 面实现期真机拦截）**：T2 写作时全部停在 bndFFI 边界红，源拼写从未被真 parser/checker 验证；T4 实现后真机逐面拦出六处——① byres-green 源的 `fn close` 误带空体块（ch19 权威：条目 = 无体声明，parser E0105 无体专形拦下）；② `scope resource f = open(...)` 误无括号（ch13 产生式括号形 `scope resource(f = ...)`，既有黄金同形）；③ mock 测试源误用 ch6 段序（`effect -> Int64`）——ch20 mock 产生式返回在前（既有 mock 黄金钉 `-> String effect io`），且裸段在 mock 语法位不存在（省略段 = 空集复述）；④ mock 测试走 wantDiag 缺 `_test.we` 文件名（E1801 拦下——test 块的模块身份事实），改 wantDiagAs + `tests/t_test.we`；⑤ byres-e1106 例 impl 误带 `effect io` 段（E1404：Releasable.release 接口无段，impl 逐字复述——既有 ch13 黄金全无段）；⑥ byres-green 的 take 体缺尾转移（E1104：资源参数按值接收即获所有权，函数尾必须转移——`scope resource(g = f)` 兜底，ch13 既有先例形）。全部按权威拼写修正，修正后断言码/词/位不变。
  - **披露 9（T1 黄金笔误同类，九枚修正）**：同根因（bndFFI 拦截、黄金从未真跑；T1 的「等价形探针」形正确但落盘拼写偏离探针）——锚位手数错四枚（e1706-string 2:20→2:30、e1706-bytes 2:18→2:28、eff-e1401 7:13→8:13、eff-custom-tag 9:13→10:13，机器 refPos/调用位为准）；报文一词之差一枚（byres-e1101 手写多 `own`——既有 check-e1101-no-impl 黄金钉 `in the record's module` 为权威）；源笔误四枚（green/byres-green/byres-transfer/byres-e1106 的 impl release 误带 `effect io` 段 → E1404；green/byres-transfer 的 `scope resource` 误无括号；byres-transfer 的 take 体缺转移）。全部真机输出定点核对后写盘。
  - **ch19×ch13 交界语义事实（黄金已示范）**：Releasable.release 接口无段（纯），impl 逐字（E1404），故 release 体内下调带 `effect io` 段的 foreign close 必撞 E1401——合法拼写 = close 声明裸段（`fn close(fd: File) effect`，foreign 块内独有的显式纯断言：释放语义在效果系统之外）。check-ffi-green 以此形示范 byres opaque 完整生命周期（open mint → 转移 → release 调 close）。
  - 效果面零改直证补记：fnCall 的 checkCallEffect 对 fnTags nil（裸段）空集放行、泛型分支对单态 foreign 条目天然绕过（E1704 已在 parser 关死泛型）——fnCall/检查器调用路径零行改动。
- [x] T5 codegen 塔：declare 发射 + 位置 ABI 映射（String/Bytes 双参展开）+ 调用位直呼 + Never 发散 + opaque 单指针槽
  来源：proposal codegen 塔 / design D4
  验证：`go test ./internal/codegen/` 绿；never 桶 check 绿面翻绿

  完成记录（2026-09-09）：
  - **实现**（全在 `internal/codegen/codegen.go`）：① pass one 加 ForeignBlock case——record 条目先收集入 `opaques` 表（`key.Name`，条目可引用块内后声明的 opaque），fn 条目入 fnTable（`foreign: true`）但**不入 define list**（体在 native 侧），declare 行全发（对齐「every We fn is defined and slotted, called or not」先例）；opaque 不入 records/order——无 layout 无构造（E1707 已在 check 关死 We 侧构造面）。② `foreignKinds` 十二行映射表（Int8/UInt8/Bool→i8、Int16/UInt16→i16、Int32/UInt32/Rune→i32、Int64/UInt64→i64、Float32→float、Float64→double；每行带 narrow/widen 转换指令名——trunc/sext、trunc/zext、fptrunc/fpext，i64/double 恒等行空转换）；String/Bytes 非表行（仅参数位 (ptr, i64) 展开）、Never 非表行（void 返回位专有）、opaque 走表查（单 ptr）。③ `emitFnCall` 头路由 `fd.foreign → emitForeignCall`——先于 classify（opaque 类型不在 We fnAbiOf 的族内，foreign 走自有 ABI）。④ `emitForeignCall`：无槽无 key 前缀直呼 `@name`（Q3 原样直通）；参数位标量窄化出 We 域（trunc i64→i8/i16/i32、fptrunc double→float；嵌套调用结果同径）、String 双字（字面量与嵌套 ckStr 结果两径）、Bytes 值面待 B2（declare 照发）、opaque 单 ptr（绑定环境 prims 与嵌套 ckPrim 两径）；返回位 sext/zext/fpext 扩回 We 域、opaque → ckPrim（bindResult 既有路径直入 prims——opaque 句柄完美契合）、Never → `call void` + `unreachable` + `diverged`。⑤ **diverged 短路七面**：emitStmt 入口统一 return（后续语句词法死码不发射）、emitTail 入口（main 尾免 ret）、emitFnDefine 语句循环顶 break + 尾分支免 ret/defers/pops（fxgate 照发——gate 自身调用不发散）、任务 thunk 同形（diverged 尾免 ret i64）、mock define 同形、test define 同形（diverged 分支在 defers/pops 前——pops 是裸 inst 不走 emitStmt 短路链）、回调谓词值契约拒散（`bndCallback`——谓词欠调用方一个值）；五处 body 交换位全部 save/restore/reset diverged（不依赖短路链的隐式惰性）。⑥ render 组装 foreignDecls 独立组（slots 组后、运行时 declare 组前）。⑦ `ForeignName{Module,Name,Line,Col}` + `ForeignNames(mods)`——链接塔（T6）的 E1906 查证面，模块序 + 块内源序，opaque 无符号。
  - **翻绿对账**：`go test ./internal/codegen/` 全绿（T2 五函数 + 既有）；conformance ffi 32 枚中 26 枚绿不变，红 6 = build 2（待 T6）+ run 4（**check+codegen 阶段已全过**，卡点恰为链接缺 native 对象：`undefined reference to weAdd`——T6 面）；**既有 641 零回归**（失败清单仅 ffi 6 枚）。cli 包编译红维持 = T2 测试先行待兑（nativeSources 四函数 = T6 面，设计内）。
  - **真机端到端直证（T7 电池预演）**：手工 `clang demo.ll rt-*.o native/*.c` 补链四形全过——scalar（`five`，exit 0）、string（`first-h`，exit 0——(ptr,len) 双参展开真读首字节 'h'=104）、never（exit 7——unreachable + diverged 短路，进程以声明码退出）、opaque（`unboxed`，exit 0——ptr 句柄往返 g_val）。四形的 exit 码与 stdout 逐字符合黄金。
  - **披露 10（T1 黄金笔误三枚，D13 面）**：run-ffi-scalar/string/opaque 三枚源用 `io.println` 却缺 `import std.io`——既有 run 黄金纪律全量核对钉死（用 io 必带 import，如 run-conc-* 全带）；同根因（bndFFI 时代源从未过真 checker，E1304 真机拦截）。补 `import std.io` 后三枚推进至链接面。run-ffi-never 无 io 使用、无此笔误。
  - **披露 11（T2 夹具笔误两处，D13 面）**：TestForeignDeclareEmission/TestForeignABIMap 的 `foreignMod(fb)` 零参——夹具无 main，撞 `entry == nil` 防御边界（真管线 E1305 主权在 check 阶段先拦，代码层防御保留不动）；补 `okReturn()` 尾。夹具断言零变化。
  - never 桶 check 绿面（check-ffi-never-return）已在 T4 翻绿；T5 的 never 验证面 = codegen 单测 TestForeignNeverCall（unreachable 断言）+ run-ffi-never 真机 exit 7。
- [x] T6 链接塔：build native/ 发现与编译 + llvm-nm 符号查证 E1906（全渲染后 exit 1 无工件）+ 链接实参链入 + toolchainGate 扩查 + check/test/vet/doc 零链接直证
  来源：proposal 链接塔 / design D5
  验证：`go test ./internal/cli/` 绿；build/run 桶黄金翻绿对账；既有黄金零回归

  完成记录（2026-09-09）：
  - **实现**（`internal/cli/build.go` + codegen ForeignNames 锚位修正）：① `nativeSources(dir)`——`os.ReadDir(native/)` 收 `.c` 基名字典序，目录缺席 = 空集非错（ok 位报目录存在性）；② `checkNmVersion(path)`——沿 clang gate 先例，解析 `LLVM version x.y.z` 行比 pin（真机 `llvm-nm --version` 第二行 `Ubuntu LLVM version 21.1.8` 恰为 T2 钉形），`toolchainGate` 扩查第二行报文 `want llvm-nm %s`；③ `definedSymbols(nm, objs)`——`llvm-nm --defined-only` 全对象一次调用，行尾字段为名（空对象集免调用）；④ `missingForeignNames(declared, defined)`——声明序保持的差集；⑤ `nativeObjects(dir, buildDir, mods, prog)`——native/*.c 逐个 `clang -c` 入 `build/native-<f>.o`，全量 prog（deps+root——**初版 bug**：误传 `programModules(mods)` 只含依赖，根模块 foreign 块漏收、E1906 恒不发射，真机 exit 0 拦下后修正）取 ForeignNames，差集逐条 E1906（`unresolved native symbol — {name} (declared at {file}:{line})`，锚 = 名字位 NameLine/NameCol——ForeignNames 修正：初取 head 位 col 5，黄金 2:8 拦下），全部渲染后 exit 1、compileProgram 不跑（**无 demo 工件**，真机 rm 后复验——demo.ll 亦不落盘，E1906 时点在 IR 写盘前）；modPaths 从 mods（typecheck.Module.Path）+ root `src/main.we` 取诊断锚路径；helps 挂注册表 remediation 逐字；⑥ 链接实参——compileProgram 增 `nativeObjs []string` 形参，native .o 追加在运行时对象后、`-o` 前（**初版 bug**：append 在整个命令表循环里，`-c` 编译步也被追加上对象——重构成 linkArgs 显式组装）；test 面两调用点传 nil（设计 D5：check/test/vet/doc 零链接面）；⑦ `programModules(mods)` 提出为共享组装（buildProject 消重）。
  - **翻绿对账**：`go test ./internal/cli/` 全绿（T2 四函数 + 既有）；conformance **全量 673 枚绿**——ffi 32 枚全绿（本塔新翻 6：build 2 + run 4——run 桶端到端真跑 native/ 链接），既有 641 零回归；`go vet ./internal/...` 净、`go test ./internal/...` 全包绿。
  - **披露 12（T1 黄金源笔误三处，D13 面）**：同根因（bndFFI 时代源从未过真 codegen）——① 三枚 run 黄金缺 `import std.io`（披露 10，T5 期拦下）；② build 两枚的 `let n = ...; let _ = n` 复绑弃用形超出 M9b main 语句集（check 黄金的弃用惯用法误带入 build 桶；改弃用直呼形 `let _ = abs(3)`）；③ build-ffi-e1906 的 `abs(-3)` 一元负不在 main 语句集（T2 单测 `intLit("-3")` 是字面量拼写非真 parse——真 parse 是 Unary 节点，main 集无此形；改 `abs(3)`）。三处修正后锚位/报文/exit 逐字符合黄金零变化。
  - check/test/vet/doc 零链接直证：check-ffi-cross-module（同源项目 check 绿）+ vet/doc 黄金在全量绿里过——四面不触 buildProject/nativeObjects，E1906 章文「we check 不链接、E1906 不可能发射」由结构直证（check 路径无 nativeObjects 调用点）。
- [x] T7 黑盒电池：真二进制逐面探针——native/ 端到端真跑（标量/String/Bytes/opaque 四形 stdout）、E1906 真机报文与无 build/、llvm-nm 缺 gate 报文、check 永不链接（同源项目 check 绿 + build E1906）、fmt/doc/vet 真机面、回归抽检
  来源：proposal 三塔 / design D4–D6 D9
  验证：电池探针全过，逐探针输出记于完成记录

  完成记录（2026-09-09）：
  - 电池载体：当前树真构建 `/tmp/we-m12`（`go build -o /tmp/we-m12 ./cmd/we`），项目 staging 复刻黄金 setup 逐字；七面探针全过：
  - **探针 1（llvm-nm 缺位 gate）**：`PATH=/tmp/t7/fakebin`（仅 clang 符号链接，无 llvm-nm）`build p1` → exit 1 + `we: LLVM toolchain not available (want llvm-nm 21.1.8): running llvm-nm --version: exec: "llvm-nm": executable file not found in $PATH`（clang gate 恰不触发、nm gate 触发——扩查面独立直证；首版 PATH 含 /usr/bin 时真 llvm-nm 被找到 gate 不发，修正 staging 复验）。
  - **探针 2（check 永不链接）**：同源 p1 `check` → exit 0 静默（无 llvm-nm 依赖、无 build/ 副作用）；同源 `build` → E1906 exit 1。check/test/vet/doc 四面零链接结构直证（路径无 nativeObjects 调用点）+ 真机行为直证。
  - **探针 3（E1906 partial 恰报缺者）**：p2（native/ 只供 abs，声明 abs+mulInt）`build` → `p2/src/main.we:3:8: error[E1906]: unresolved native symbol — "mulInt" (declared at p2/src/main.we:3)` exit 1，`ls build/` = **无工件**（demo.ll 亦不落——E1906 时点在 IR 写盘前）。
  - **探针 4（E1906 全缺 + 相对路径锚）**：p1（无 native/）`build` → `p1/src/main.we:2:8: error[E1906]: unresolved native symbol — "abs" (declared at p1/src/main.we:2)` exit 1（锚 = 名字位 2:8、报文带声明处、路径相对调用方 cwd——与黄金逐字同）。
  - **探针 5（run 四形端到端真跑）**：四项目自父目录 `run r-<name>` → scalar exit 0 stdout `five`；string exit 0 stdout `first-h`（(ptr,len) 真读 'h'=104）；never **exit 7** stdout 空（unreachable+diverged，进程以声明码退出）；opaque exit 0 stdout `unboxed`（ptr 句柄往返）。四形 exit/stdout 逐字符合黄金。
  - **探针 6（fmt/doc/vet ffi 面）**：fmt-ffi-block（脏→净）、fmt-ffi-clean（净定点）、doc-ffi-excluded（foreign pub 条目不入页）、vet-ffi-clean（调用面净）四枚真机全 PASS（exit/stdout/stderr 逐字合黄金；fmt 定点 = 二次运行输出不再变化由 fmt-ffi-clean 黄金钉）。
  - **探针 7（回归抽检）**：跨面散点七枚真机 PASS——build-skeleton、run-conc-chan-send-recv、fmt-blank、check-e0604-duplicate-field、test-explore-default-iterations、doc-page-shape、vet-anchor-message（每命令族至少一枚）。
  - **披露 13（D13 缺陷修复：run 面相对路径工件双重解析）**：探针 5 首跑四枚全报 `we: fork/exec r-run-ffi-scalar/build/demo: no such file or directory` exit 1——`runRun` 的工件路径携带调用方命名的项目前缀（`proj/build/demo`），而子进程 `cmd.Dir = proj`，exec 在 Dir **内**解析相对 Path → 双重前缀 `proj/proj/build/demo` 不存在。M9b 起潜伏：conformance runner 全骑绝对 temp 路径（runner.go workdir = `t.TempDir()`），且既有 run 黄金全 `we run .` 形（`build/demo` 在 Dir="." 下解析正常）——双重掩盖；黑盒电池的相对子目录形（用户真实用法）首次揭出。修复 = exec 前 `filepath.Abs(artifact)`（`internal/cli/build.go` runRun）。**回归钉** = 新黄金 `run-ffi-scalar-subdir`（`we run proj` 自父目录，scalar 同源程序）：先红实证——临时还原缺陷形跑单枚，exit want 0 got 1、stdout want `"five\n"` got `""`、stderr want `""` got `"we: fork/exec proj/build/demo: no such file or directory\n"`；修复后绿。conformance 总数 673 → **674**（D6 计数 32 → 33 的唯一偏差，回归钉性质）。
- [x] T8 全量终验：D7 阶梯固定序全绿 + 对账（refr/ 零触碰、既有黄金零改写、conformance 总数对齐 D6 计数）
  来源：proposal 影响 / design D7
  验证：`go build ./...`、`go vet ./...`、`go test ./...`、runtime C harness、conformance 全量、validate --all --strict、docs_sync、gofmt -l、git status 对账——全绿记录

  完成记录（2026-09-09）：
  - 阶梯九步全绿：`go build ./...` OK；`go vet ./...` OK；`go test ./...` 全包 ok（cli/codegen/conformance/diag/fmt/lex/parser/typecheck/version/runtime——runtime 包测试即 C harness 面）；conformance 全量 **674 枚绿**（D6 计数 32 + T7 披露 13 回归钉 1 = 33 偏差已记）；`validate.py --all --strict` OK（1 change valid; registry clean）；`docs_sync.py` OK（31 对齐）；`gofmt -l` 零输出；`git diff --check` CLEAN。
  - 对账：`git status` refr/ **零触碰**；既有黄金零改写（改动面 = 12 修改文件 + 新增 ffi 工件——修改文件里 parser_test/fmt_test 是披露 5 的 M11 fmt 缺陷修复用例 + T2 测试，无既有黄金 JSON 改写面——32+1 枚 ffi 黄金全部 `??` 新增）；openspec/changes/ffi/ 全工件在场。
- [x] T9 实现审查：welang-code-review 7 条（规范符合性/验证诚实性/测试先行证据/诊断协议稳定/单一权威/红线复核/最小可信验证已跑），产出 = proposal.md「## 审查记录」+ status → complete
  来源：仓库节奏（M10a/b/c、M11 先例）
  验证：7 条逐条过，发现当场处置或披露，审查记录落盘

  完成记录（2026-09-09）：七条全过落 proposal.md「## 审查记录」；status → complete；validate --all --strict 复验绿。要点：八码预注册零认领（diag 包零改动）、run 四形端到端直证、披露 13 为本变更期唯一新增缺陷面（D13 闭环）；fmt.go 面 = 披露 5 的 M11 缺陷 D13 修复（范围内）。
- [x] T10 归档：welang-archive-sync 五步（本变更零规范提升零新 ADR——机制载体 = 变更记录 + roadmap）+ roadmap M12 行 pending → done（双语）
  来源：仓库节奏
  验证：validate --all --strict 复验绿；归档目录带日期前缀

  完成记录（2026-09-09）：前置确认过（status complete、tasks 全勾、validate --strict OK、审查记录在 proposal.md）。① 规范提升——零增量（纯实现 slice，八码预注册在册复用，docs/spec/ 零触碰）；② 决策提升——零新 ADR（四裁决与 ABI 映射由变更记录承载，M10/M11 先例）；③ 状态提升——roadmap 双语 M12 行 pending → done 2026-09-09（行述扩为落地实貌）；④ 移动归档 `archive/2026-09-09-ffi` + status → archived；⑤ 复验 validate --all --strict OK + docs_sync 31 对齐。
- [x] T11 记忆与提交：project-overview 增 M12 全量条 + MEMORY.md 索引行更新；提交信息草案呈报，**候用户明示**
  来源：仓库节奏（提交须明示）
  验证：记忆回写；提交草案就绪；未提交状态确认

  完成记录（2026-09-09）：project-overview.md 增 M12 全量条（四裁决、四塔机制、13 披露总账、七探针、conformance 641→674）+ MEMORY.md 索引行更新（M0–M12 已归档、M12 提交候用户明示、下一里程碑 M13 dependencies）。提交信息草案呈报（`Land M12 ffi: chapter 19 foreign blocks and native linkage`），**候用户明示**。未提交状态确认：git status = 12 M + untracked（ffi 黄金 33 + m12_test/ffi_test + 本归档目录）；refr/ 零触碰。M11 归档件提交对账追加句（2026-09-09-fmt-vet-doc/tasks.md）随本变更提交带走（M10c 先例形）。
