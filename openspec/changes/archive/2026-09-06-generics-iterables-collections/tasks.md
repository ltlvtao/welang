# tasks.md — generics-iterables-collections

- [x] T1 测试先行 A：D13 黄金表落盘（约 100 枚新增 + 边界钉死改写 6–10 枚），对当前构建运行必须先红
  来源：proposal 目标 1 2 3 4 5 6 7 8 / design D12 D13 D14
  验证：`go test ./internal/conformance/ -run TestGoldenCases` 红（新增用例失败清单 + 改写枚失败，其余 201 枚既有全绿），red 证据与改写逐枚清点记于完成记录

完成记录（T1，2026-09-05）：
- 实际落盘 **97 新增 + 1 改写 = 98 枚**（D13 估算 ~100）：段内新码负例 55（E0801–E0831 族 48——E0801×2/E0808×3/E0809×2/E0811×2/E0812×2/E0814×3/E0815×2/E0819×2/E0820×2/E0821×2/E0823×2/E0824×3/E0827×2/E0830×2 及单例 14 码，**E0810 除外（D10 单模块不可达，判定实现但不发）**；E0901×2/E0902/E0903/E0904/E1501/E1004 共 7）+ 复用码新位负例 14（E0501×3：for 头元组/方法实参/混界 range；E0601/E0702 实例化位；E0105×3：方法值/接口字段项/接口上 where；E0104/E0202/E1304/E0012/E0404/E0828-Dyn 遮蔽）+ 正例 26（ch10 18：接口+impl/默认方法/Dyn/内禀 mut/泛型 fn/record/嵌套应用/where×3/关联类型/pub 接口/derives/泛型 impl/泛型 newtype/泛型 sum/if 比较；ch11 5：组合子链/for 三源/用户 Iterable/裸 next/Dyn<Iterator>；ch17 3：集合+字节法/Map 迭代/列表期望型）+ build 代表 2（声明擦除 build 绿 / 方法调用 main 停 bndMainBody 70）+ 边界锁定 1（check-bnd-method-arity，**首枚钉 follow-up #5 What 文本全句的黄金**）。生成器 /tmp/gen_goldens_m6a.py（源串 + 子串锚点 + 字符偏移；`->` 含 `>`、`name` 含 `me` 等子串污染以 occ 计数/偏移消解，3 处锚点计数笔误被断言当场拦截）。
- **red 证据**：`go test ./internal/conformance/ -run TestGoldenCases` → **98/298 失败**，失败集合与新增+改写集合脚本逐名比对**完全一致，零计划外失败**；既有 200 枚（201 − 改写 1）全绿——零改写硬门成立。日志 /tmp/t1-red.log。
- **D12 改写清点：实际 1 枚**（check-bnd-list：bndCollections 70 → E0501 binding-vs-annotation 真码，源串不变仅翻转断言；预估 6–10 为估计值，实测唯此一枚钉住被删行——bndGener/bndIter/bndColl/bndDyn/bndRange 各行在既有黄金中无其他钉死探针）。
- **锁定例披露**：check-e0104-method-generic-args（`obj.m<T>(x)` 归比较链，章文明文不可写形）当前因顶层 impl 先撞 bndGener 70 而红，随 T3/T4 落地后转绿；check-e0202-for-value 当前为 E0105「cannot begin an expression」（锚 2:13 与黄金同位），T4 落 for 语句后升级为 E0202 值位族消息——两枚按 M5「既有行为锁定」先例处理，红因正确。

- [x] T2 测试先行 B：parser 单测扩展（interface/impl 声明矩阵、接收者参数、泛型/where/derives 子句、for 语句与头模式、列表字面量、显式泛型实参前瞻、E0801/E0802/E0803/E0804/E0806/E0824/E0825 句法侧）与 typecheck 单测（impl 验证判定序矩阵、成员集四源与碰撞、Dyn 面、单向统一化矩阵、where 与 bound 授集、协议/集合/for、derives 能力传播）先写先红
  来源：proposal 目标 1 2 3 4 5 / design D2 D3 D4 D5 D6 D7 D8 D9
  验证：`go test ./internal/parser/ ./internal/typecheck/` 红（新用例引用未定义节点/分支失败），red 证据记于完成记录

完成记录（T2，2026-09-05）：
- 新增 `internal/parser/m6a_test.go` **10 套件**（TestInterfaceDecls / TestInterfaceAssocLimits / TestImplDecls / TestReceiverParams / TestGenericClauses / TestWhereClauses / TestDerivesClauses / TestForStmt / TestListLit / TestExplicitTypeArgs）：结构探针按 D1 节点形（InterfaceDecl/MethodSig/AssocDecl/ImplDecl/AssocBinding/TypeParam/WhereBound/TypeEq/DerivesClause/ForStmt/ListLit + RecvSelf/RecvMutSelf，及 FnDecl.TypeParams/Where/Recv、Call/Construct.TypeArgs、record/newtype/sum 的 TypeParams/Derives 扩展）；句法负例 E0801×2/E0802/E0803/E0804/E0806/E0824×3/E0825 + E0105 新位三处（接口字段项 2:5/接口上 where 1:20/for 头可驳形 2:9——消息形 `the "for" head takes an irrefutable pattern` 为 T4 实现目标）+ E0202 for 值位（parser 侧 valSite 族）+ 既有行为锁六枚（普通 fn 裸参 E0105、postfix `[` E0105、`>>` 未分隔 E0105、`obj.m<T>()` E0104、`if a < b` 比较保持、空/尾逗号/跨行列表折叠）。**接收者不入 Params**（Recv 独立字段承载，裸 `self`/`mut self` 由 Recv 枚举）——D1 字段形的落定，T3 随测试实现。
- **parser 既有探针翻转 1 枚（披露）**：parser_test.go 原 `Box<Int64> { v: 1 }` → E0104「non-associative」（M2 披露 F1 的行为锁定）翻转为前瞻后行为（wantClean + `Construct.TypeArgs == [Int64]` 断言）——D2 角括号前瞻的必然翻转，与黄金 check-e1304-generic-construct 呼应；红并入包构建失败。
- 新增 `internal/typecheck/m6a_test.go` **9 套件**（TestIfaceValidation / TestReceiverDiscipline / TestMemberSets / TestDynFace / TestDerives / TestGenericDetermination / TestWhereBounds / TestIterProtocol / TestCollectionsFace）：负例源串**逐字镜像 T1 黄金**（码/报文片段/行列与 JSON 契约逐位一致——锚点位置全部机械取自黄金 stderr，杜绝手数漂移）；绿例 m5 极简风格覆盖接口+impl/默认方法/mut self/Dyn/泛型五形/where 三形/for 三源+用户 Iterable+裸 next+Dyn 迭代器/组合子链/列表字面量/String 四法/Map 迭代/Map/Set 参数面；另加 **bndStdModules 收窄锁**（`xs.add(1)` 停诚实边界，不私发 E0816）与 **bndArityGap 锁**（方法调用实参个数，follow-up #5）。
- **red 证据**：`go test ./internal/parser/` → **包构建失败**（`undefined: ast.InterfaceDecl / ast.RecvSelf / ast.RecvMutSelf …` 共 10 行——M5 同构：引用未定义节点即红），日志 /tmp/t2-parser-red.log；`go test ./internal/typecheck/` → **恰 9 套件失败**（= 新增套件全集，既有套件零回归），红因逐套件核对：6 套件首探针撞 bndGener 解析边界、TestIterProtocol 撞 bndIter、TestCollectionsFace 撞 bndColl、TestDerives 撞既有 derives-E0105（`t.Fatalf` 首探针即止），日志 /tmp/t2-typecheck-red.log。gofmt 双文件干净。
- **T1 黄金勘误 1 枚（实现前修正，披露）**：check-e0829-noninterface-bound 锚点 **1:18 → 1:33**——生成器 needle `"Int64"` occ 缺省钉到返回槽的无辜 token，D5/D6 明文「锚 bound 首 token」；生成器补 occ=2 重生成，md5 比对确认 98 枚中**仅此一枚**变化（T1 计数 97 新增 + 1 改写不变）。
- **披露：E0501 列表元素负例为单测专属**（黄金表未设——E0501 属复用码非段内新码，D13 无逐位要求）：报文形按 D9「operands of different types」消息族落为 `the elements are Int64 and String`（列表字面量无运算符 token 可点名；纪律仍为注册表 title `mixed types` 起头组合），锚违例元素首 token 2:18（python 机械计算）。

- [x] T3 AST + parser ch10 声明：D1 节点（InterfaceDecl/ImplDecl/MethodSig/TypeParam/WhereBound/DerivesClause + 各声明 TypeParams/Where 扩展）、D2 产生式（interface 体三形项 + 关联类型 E0803/E0804、impl 头与关联绑定 E0806、接收者参数 E0801/E0802、where 子句、derives 子句 E0824、泛型子句 E0825、方法体语境、pub 派发表补行）；bndGener 死行随产生式删除
  来源：proposal 目标 1 3 / design D1 D2
  验证：`go test ./internal/parser/` ch10 套件绿；既有套件零回归

完成记录（T3，2026-09-05）：
- **D1 全量落地**（T2 已全数定义于 ast.go，本轮补 gofmt 对齐即成）：InterfaceDecl/AssocDecl/MethodSig/ImplDecl/AssocBinding/WhereBound/TypeEq/DerivesClause/TypeParam + RecvKind 三枚举；FnDecl/Sum/Record/Newtype 扩 TypeParams/Derives（Fn 另有 Where/Recv）；Assign.Field 承 self.field 写形（anchor 在 self token，E0813 黄金同位）；ForStmt/ListLit/Call.TypeArgs/Construct.TypeArgs 节点随 D1 同轮落（解析 T4 接线）。
- **D2 产生式**：parseTypeParams（PascalCase E0011 + E0825 于子句 `<`，解析后计数判发）；parseWhereClause/parseBoundRef（bound 形 `T: I[+I2…]` 与等式形 `T.Assoc == Type`，一约束一 WhereBound）；maybeDerives（同行判定 brokeLine 否则留待下一项位 E0105；E0824 三报文形——未知目标「the clause takes "Eq", "Hash", and "Show"」/ Shareable 专项「it is a where bound, not a clause target」/ 重复「appears at most once」，均锚目标名）；parseInterfaceDecl（体项 type/fn 二形一物一行、项间无分隔符；E0105 接口字段项 2:5 与接口上 where 1:20 报文逐字落黄金；E0803 于第五个 `type` 关键字、解析后判发以点名 "E"；E0804 于 bound 首 token）；parseAssocDecl；parseMethodSig（签名名 declare 入模块名空间——E0404/E0012 黄金同锚；effect 段停 bndEffect；where 显式拒绝）；recvParamList(method)（E0801 零参锚 `(`/带注锚参名、E0802 锚裸名，报文逐字；parseParamList 重构为 `p.next()+annotatedParams()` 共享循环）；parseImplDecl（头 Iface/Head 均走 parseTypeRef——元组头/裸参头可析、判留 E0811；for 双头；impl 头 where；E0806 解析后判发、报文点末方法名）；parseAssocBinding；parseImplMethod（[pub] fn 可选前缀、不入名空间、where 显式拒绝）。
- **bndGener 全删**：常量行 + 5 处使用（parseSumDecl/parseRecordDecl 各两处、assignAfterExpr 一处）随产生式替换；边界表余 9 组。**pub 派发表**：仅补 `pub interface` 行（spec 明文 impl 块不携 pub——pub impl 落尾随 E0105，报文已注明「an impl block carries no pub」）。
- **parseStmt 补 self 探针**：Ident(self)+.+Ident+= 同行四 token 位检（`p.pos+3 < len(p.toks)` 界卫），产 Assign{Name:"self", Field:…}；assignAfterExpr 去参去分支，E0105 报文原文不动（既有黄金钉死）。
- **parser_test.go 既有探针翻转 7 处（披露）**：派发表 interface/impl 两行 bd→okRes()；TestStatements :413 self.name 探针 wantBnd→结构钉（Assign{Name:"self",Field:"name"}）；TestDeclarations :513 fn<T> wantBnd→wantClean；TestSumDecls :616 Pair<A,B> wantBnd→wantClean、:617 derives(Eq) wantBnd→E0105（锚 `(` 1:25，derives 取裸名）；TestBoundaryForms 删 interface/impl/fn<T>/self.x 四行（for/[1,2] 两行留 T4）；generic10 常量随用尽删除。
- **m6a_test.go 缺陷修复 3 处（T2 遗留披露 2 + 本轮新发现 1）**：TestImplDecls 四站点 Iface/Head 补 `.(*ast.NamedType)` 断言（D1 落为 TypeRef 后直取字段不可编译）；TestWhereClauses 等式约束 Where[0]→先断言双 WhereBound 再取 [1]（一约束一节点）；TestExplicitTypeArgs 比较保持例 `*ast.If` 为 Expr——语句位包 ExprStmt（编译器 impossible type assertion 当场拦截），补 `.Expr.(*ast.If)` 解包。
- **conformance 98→83（15 枚翻转逐枚清点，全部句法层判发）**：E0801×2/E0802（接收者）、E0803/E0804（关联类型）、E0806（晚绑定）、E0824×3（derives）、E0825（子句计数）、E0105×2（接口字段项/接口上 where）、E0404（接口方法签名入名空间）、E0012（方法名 camelCase）、E0104-method-generic-args（T1 披露锁定例——impl 可析后 obj.m<T>() 归比较链，如期转绿）。既有 200 枚零翻转（green→red 集合为空）；e0827/e0828/e0816 序列（prelude Result/Option）T1 起即绿，非本轮翻转。
- **红保持**：parser 3 套件（TestForStmt/TestListLit/TestExplicitTypeArgs）+ TestTypeRefs Box<Int64> 前瞻探针——T4 范围；typecheck 9 套件红形自「解析边界」演进为「检查器缺口」（TestDerives 现达 E0816、TestWhereBounds 达 E1304——解析已通、判定待 T5–T8），既有 12 套件零回归；conformance 83 枚红全部 T4–T8。
- 验证：`gofmt -l` 空、`go build ./...`/`go vet ./...` 过；`go test ./internal/parser/` 7 ch10 套件绿 + 4 T4 红如上；`go test ./internal/typecheck/` 既有全绿；conformance 215/298 绿（200 既有 + 15 句法层），日志 /tmp/t3-conformance.log。

- [x] T4 parser ch5/ch17 与显式形：D2 for 语句与头模式（绑定/_/元组，可驳形 E0105）、列表字面量 primary `[` 与 postfix `[` 现状核对（若属边界行随 bndColl 收敛）、显式泛型实参角括号前瞻（调用/构造头三形，`Box<Box<Int64> >` 收口维持 E0105）、E0202 值位 for；bndIter/bndColl 死行删除；checker 对新树的 Go-panic 兜底行（What 表临时行，T5–T8 各自删除）
  来源：proposal 目标 3 4 5 / design D2 D9
  验证：`go test ./internal/parser/` ch5/ch17 套件绿；既有套件零回归

完成记录（T4，2026-09-05）：
- **D2 for 语句**：parseFor（`for pat in expr block`，in 关键字必居，迭代式走 headExpr 挂 noBrace，体入 loopDepth——break/continue 深度同 while/loop）；forHead 三形（`_`→PatWildcard、非 Pascal 识符→checkCamel+PatBinding、`(`→**复用 parseLetTuple**——不可驳元组文法唯一权威，嵌套元组/通配/重名检查同源）+ 可驳形 E0105 报文 `the "for" head takes an irrefutable pattern` 锚模式首 token（黄金 2:9 逐位）。
- **D2 列表字面量**：parsePrimary `[` 由 bndColl 死行换 parseListLit（depth++ 折行、统一尾逗号、空表 `[]`、EOF 检查，锚 `[`）；**postfix `[` E0105 原行不动**（`indexing is by named methods` 仍钉）。
- **D2 显式泛型实参前瞻 tryTypeArgs**：parsePostfix 新 `case "<"`，仅裸 *ast.Ident 头进入（Member 头直接归还比较读——`obj.m<T>()` 按章文保持 E0104 于 `>` 2:32，既有锁定零翻转）；试探以 save(pos,last)+recover(stop) 兜底——**未提交形一律还原流**（`if a < b {` 保持比较、`a < b < c` 保持 E0104、小写实参 `f<t>(x)` 还原后归比较链 E0104、`f<Box>()` 形实参后的 `&&`/`,/)/换行` 均还原）；提交二形：`(`→Call{TypeArgs}（锚 `(`，与既有调用一致）、`{`→parseConstruct 扩参（open, path, **typeArgs, hl, hc**）——锚显式传头名 token（前瞻路上 p.last 是 `>` 不再是头名），check-e1304-generic-construct 的 E1304 因此落头名锚 2:13 逐位；`{` 提交带 noBrace>0&&depth==0 卫（控制头体括号永不作构造）与 depth==0 brokeLine 卫（后缀续行规则覆盖子句之后）；空子句 `<>` 还原；小写头 `{` 形 E0105 construction-heads-PascalCase 与既有 `{` 分支同文。
- **E0202 值位 for**：parsePrimary 无值形 case 增 "for"（while/loop/break/continue/defer 同列），`let x = for …` 由 E0105「cannot begin an expression」（T1 披露锁定形）升级为 E0202 值位族报文，锚 for 2:13 与黄金同位——T1 预告的如期翻转。
- **bndIter/bndColl 全删**：常量两行 + parseStmt/parsePrimary 使用点两处；边界表余 7 组。parser_test.go 翻转 3 处（披露）：dispatch for 行 stmt bd→okRes()、expr E0105→E0202；TestLineJoining `[1, 2]` wantBnd→结构钉（ListLit 双元素）；TestBoundaryForms 删 for/[1,2] 两行。
- **checker 兜底行 3 组（What 表临时行，随 T5/T8 删除）**：checkModule pass1 显式 `case *ast.InterfaceDecl, *ast.ImplDecl`→bndIfaceRules（否则两 pass **静默跳过**——违「never a silent skip」）；walkItems 显式 `case *ast.ForStmt`→bndForRules（否则误落 bndCtlForms，章名错报 ch3）；typeOf `case *ast.ListLit`→bndListRules（否则 panic("unreachable expr")——真 Go-panic）。真机探针：interface+impl+for+列表综合程序 `we check` exit 70 报 `interface and impl type rules (chapter 10) are not implemented…`（首个未实现层先停，诚实）。
- **conformance 83→81（2 枚翻转逐枚清点，零计划外）**：check-e0202-for-value（E0105→E0202，T1 锁定例如期升级）、check-e1304-generic-construct（前瞻落地后 Construct{TypeArgs} 流入既有 E1304，头名锚 2:13）；既有 215 枚绿零翻转（green→red 集合为空）。
- **红保持**：parser **全绿**（T2 三套件 TestForStmt/TestListLit/TestExplicitTypeArgs + TestTypeRefs Box 前瞻探针随实现转绿）；typecheck 9 套件红形自「解析边界」演为「检查器缺口」（iface/where 套件停 bndIfaceRules、iter 套件停 bndForRules、collections 套件停 bndStdModules/bndListRules、TestDerives 达 E0816、TestGenericDetermination 达 E1304——T5–T8 各层），既有 12 套件零回归；conformance 81 枚红全部 T5–T8（正例 26 枚停临时边界行，负例待各检查层真码）。
- 验证：`gofmt -l` 空、`go build ./...`/`go vet ./...` 过；`go test ./internal/parser/` 全绿（/tmp/t4-parser.log）；`go test ./internal/typecheck/` 恰 9 套件红（/tmp/t4-typecheck.log）；conformance 217/298 绿，失败集合与 T3 名单双向 diff 仅 2 枚 red→green、0 枚 green→red（/tmp/t4-conformance.log、/tmp/t4-names.txt）。

- [x] T5 typecheck ch10 声明验证：D3 符号层（ifaceInfo/implInfo/成员集登记）、E0803–E0811 判定序、E0812（mut self 于值类别头与实例化）、E0814 碰撞四源、derives 检查（E0822 手工 impl / E0823 能力传播与实例化重查 / D8 生成方法入集）、E0825/E0826 声明侧、内建声明层（D7 Iterator/Iterable/String/Range/List/Map/Set 与 derives {Eq,Hash,Show} 登记）
  来源：proposal 目标 1 2 / design D3 D7 D8
  验证：`go test ./internal/typecheck/` 声明验证与 derives 套件绿

完成记录（T5，2026-09-05）：
- **D3 判定序全落**（checkModule 四遍：pass1 符号登记含 implDecls 收集与各声明 params/derives → pass2a 各声明自子句解析（payload/field/underlying 原位 + E0601/E0702 声明侧 + derives 检查 + fn 签名 where→bndWhereRules + checkInterface）→ pass2a2 impl 源序（checkImplDecl）→ pass2b 绑定初始化与三类体（fn/接口默认体/impl 方法体））。checkImplDecl 序：where 边界→子句 scope→resolveIfaceRef→E0811 头（base/tuple/paramRef/iface 四形+尾句）→E0822（deriveTarget）→E0809（同 iface 指针+同 headName 基名；任一侧泛型（子句非空或头含参数位）→overlap 形）→E0805（缺绑定 golden 形+多余绑定名 E1304 组合）→E0807（defines 闭包）→方法循环（E0826→**E0808 子句比较与「matches no method」先于该方法的签名解析**——名比较不需类型解析，破损签名永不误报，本轮实证后重构）→签名解析（子句+assocs+方法子句偏移；方法子句 idx 加 len(外层子句)，外层应用代换不触及）→E0808（接收者→参数个数→逐参（接口侧 subst(ifaceArgs,assocs)）→返回三形）→E0812→E0814 登记（提供方法 from=接口名锚方法名；继承默认 from=接口名锚 impl 头=责任声明）→implInfo append。
- **四层名字解析**：typeScope 栈（声明子句→impl 的 assocs map 作层→方法子句偏移层），resolveNamed 序：typeScope 命中（带 Args→arityFail 0）→syms（symType/symRecord/symNewtype/symIface 各自 checkArity+resolveArgs；symIface 与 prelude builtinIface 命中即 valueSlotIface **E0821 提前至所有值槽**——Dyn 分支 bndDyn 先停故 Dyn<Describable> 实参永不解析，无误发）→prelude（Never/Result/Option/内建接口/List/Map/Set→bndCollections/Dyn→bndDyn/Shareable/baseNames）→E1304。
- **derives（D8）**：carries 三 visiting map 防循环（record 字段/newtype 底层/sum 载荷各自 subst(args) 后递归；baseType→true；paramRef/assocRef→false=延迟到应用）；声明侧 E0823 field 形锚字段类型 token；生成方法 deriveMethods（Eq→equals(params:[self],ret:Bool)/Hash→hash()->Int64/Show→toDebugString()->String，from="derive:X"，锚子句）经 addMethod 入集；**实例化诚实三处**：constructType 显式实参→checkFieldInstantiation（E0601-inst+E0823-inst 锚实参 token）、ctorCall→checkPayloadInstantiation（E0702-inst+E0823-inst，第一渲染=变体名+\<args\>）、newtypeCall 内联（E0823-inst；newtype 无类别检查——类别随底层）。**嵌套应用形（字段为 Box\<T\> 形）延迟到内部实例化点**（披露：只有直接 paramRef 载荷/字段在应用点查）。
- **内建声明层（D7 部分）**：Iterator\<T\>（fn next(mut self)->Option\<T\>，**仅 next 注册**——十一个组合子默认体签名携集合类型，T8 随 collections 落，披露）/Iterable\<T\>（type Iter+fn iterator(self)->Iter 关联位）/Eq/Hash/Show（deriveTarget 标记）；nameResolvable 增五名。
- **E0813/E0816/E0501 于 self.field 写**：recvMut 判定先于字段知识（事实是接收者形不是字段）→self 查找（未绑定→组合形 `sits outside any method body`）→recordType 断言→字段 subst 查找→noteCapture→值与字段 agree。closureType 不保存/恢复 recvMut（宽松，无探针钉）。
- **显式实参形从 T7 提前至 T5**（E0821/E0828 同步提前——防错误绿：若裸泛型头先落 E0828 或接口裸名误绿，正例黄金会假红/假绿）；fnCall（TypeParams>0 无 TypeArgs→bndGenerDeterm，有→substArgs 参数+subst 返回）、ctorCall/newtypeCall/constructType 同构；callType 显式 TypeArgs **元数先行于被调者分发**（e0828-dyn-shadow 的用户 Dyn sum 遮蔽→E0828 而非 bndCalleeGap），裸构造头不判元数（`Pair { … }` 停 bndGenerDeterm 待 T7 定出）。
- **T1 黄金修正 4 枚（实现前，披露）**：check-e0601-instantiation 锚 3:9→4:17、check-e0702-instantiation 锚 3:9→4:18（design.md「锚实参 token」为准，生成器误锚行首）；check-e0809-duplicate/overlap **源缺第一个 impl 收口 `}`**（生成器笔误——真机实测 E0105 而非 E0809），补 `}` 后第二 impl 头 9:1→10:1，两处（setup 与 files）同步；m6a_test.go 四探针镜像同步。
- **parser 修正 1 处（T3 规则过宽，披露）**：接口方法签名名原 declare 入**模块**名空间——两接口同名方法在 5:8 撞 E0404，E0814-two-interfaces 黄金永不可达；规范明文（ch10 L5/ch6 名空间枚举）仅接口**名**入模块空间。改为**接口内成员空间**（memberNames map 传入 parseMethodSig）：同接口重名仍 E0404（check-e0404-interface-method 黄金与 parser 探针双绿保持），跨接口同名放行（其碰撞归 E0814 于 impl 处判）。parser 既有套件零回归。
- **修复 2 处本窗口实证缺陷**：(a) callType 元数先行块把 arityFail（无条件判发）当条件检查调用——元数相符也发 E0828（「wants 1, got 1」），改为不匹配才发；(b) constructType 对裸泛型头判元数（「wants 1, got 0」）——裸头应停 bndGenerDeterm，限定显式子句存在时判。
- **组合消息清单（复用码组合形，披露）**：impl 接口槽非接口→E1304 `names no declared interface`（resolveIfaceRef 独立解析，不经 resolveNamed 防 E0821 误发；注册表后续候选）；E0811 paramRef/iface 头形；E0808 接收者形/参数个数形/反向子句形（absent in "Maker"）/额外方法形 `matches no method`；E0814 派生形 `already has the derived method`/继承默认锚 impl 头/内禀（或派生）先行 vs 接口来临形 `already receives %q from %q`；E0813 未绑定 self 形；E0823 newtype 底层形 `the underlying type is %q`/sum 载荷形 `the payload of %q`（field 形为 golden）。
- **验证**：`go test ./internal/typecheck/` TestIfaceValidation/TestReceiverDiscipline/TestDerives 三套件绿，既有 21 套件零回归，余 6 套件各停其任务首探针边界（TestMemberSets :69→bndDyn、TestDynFace :85→bndDyn、TestGenericDetermination :120→bndGenerDeterm、TestWhereBounds :150→bndWhereRules、TestIterProtocol :166→bndForRules、TestCollectionsFace :190→bndStdModules）；`go test ./internal/parser/` 全绿；conformance **218→254 绿（36 翻转：负 27+正 9+build 1——较预估 34 多 2，e0702-instantiation 与 e0501-method-argument 均属 T5 显式实参/方法实参面），双向 diff 零回归**，余 44 红逐枚映射（T6 dyn 面 10/T7 定出与 where 11/T8 协议集合 for 20/擦除 build 3）；三枚「检查暂静默通过」（e0105-method-value/e1004/e0904——黄金红钉住待 T6/T8 发码，T1 计划内）；check-e0827-fn-empty 现红于 E0827@2:5（泛型 fn 体内裸 `None` 尾表达式无 expected——fn 尾 expected 传递问题随 T7 定出层判定，T4 时亦红非回归）；gofmt/`go build`/`go vet` 过（日志 /tmp/t5-*.txt）。

- [x] T6 typecheck 成员解析与 Dyn：D4 四路分流（Dyn 位/泛型参数/名目/内建）、E0815/E0816/E0817、方法调用与方法值 E0105、E0818–E0821、E1004、D10 bndStdModules 收窄与 `.forEach` E0816、bndDyn 行删除
  来源：proposal 目标 2 5 / design D4 D5 D10
  验证：`go test ./internal/typecheck/` 成员解析与 Dyn 套件绿

完成记录（T6，2026-09-05）：
- **D4 四路分流全落**（memberType 重写为 `(x *ast.Member, asCall bool)`）：qualifier 守卫（E1304 接收者）→ **forEach 顶部守卫**（任何接收者、判发先于接收者定型——forEach 处处不存在，序列效应属 for 语句，E0816 专形 `forEach does not exist: sequence effects belong to the for statement`）→ switch 四路：名目（record 字段后方法/newtype `value` 后方法/sum 方法——命中即 **methodUse 守卫**（E0105）先于 fnContainsParam→bndGenerDeterm 边界；未命中先过 combinatorBnd 再 E0816 各专形）/ 接口位（ifaceType 命中同构；未命中：iteratorIface 上组合子名→bndIterCombinators，其余 E0815 接口位形 `an interface position reaches its own interface's methods only`）/ **Dyn 位**（dynType 新分支同构于接口位；未命中 E0815 Dyn 形 `a Dyn value reaches its own interface's methods only`）/ 内建（String 命中 stringMembers（本轮仅 `iterator` → `Iterator<Rune>` 接口值——推断型接口值合法，E0821 只约束**写出的**裸接口名于值槽）；未命中维持 bndStdModules——D10 不私拒）；paramRef → **E0817 无条件判发**（论证：任何带 where 子句的 fn 在 pass 2a 即停 bndWhereRules，能走到体检查即无 where 子句，参数接收者恒无约束）；tuple/fn/unit/never → E0816 通用形；assocRef → bndWhereRules。
- **E0105 方法值规则**（methodUse helper）：方法视图仅适配调用头——`memberType(x, asCall)` 参数化替代 checker 标志位方案（嵌套调用实参 `f(u.g)` 不误免检）；非调用位判发 `unexpected token — %q resolves to a method; a method name fits no ratified value production: call it or wrap it: |u| u.describe()`（wrap 例用接收者 ident 名，裸 `|x| x.m()` 兜底）；callType 被调者路径 Member 分支传 true，typeOf 传 false。E0105 判发先于定出边界（位置真理：即使定得出，方法值仍是 E0105）。
- **E1004**（identType symFn 分支）：泛型 fn 名于值位 → `a family of functions, not one function; call it or wrap it: |x| identity(x)`（TypeParams>0 时无条件判发；裸调用仍走 callType→fnCall→bndGenerDeterm）。
- **Dyn 三函数**：dynFace（元数已于 resolveNamed checkArity 保证；dynIfaceRef 解析接口实参 → 关联类型非空即 E0819 `declares the associated type %q; the erased value could not honor the binding` 锚 Dyn< 内接口名 refPos）；dynIfaceRef 四路（NamedType-无-qual：typeScope→E0820 / syms symIface 命中→checkArity+resolveArgs、其他 kind→E0820 / builtinIface→命中 / List/Map/Set→bndCollections、Shareable→bndShareable、Never/Result/Option/Dyn 与 baseNames→E0820 渲染名 / 真未知→E1304；qual→import 名 bndMultiModule 否则 E1304；非 NamedType→resolveTypeRef 渲染 E0820）；dynCall（TypeArgs≠1→arityFail("Dyn",1,n)（裸调用零实参=arityFail 1,0）；Args≠1→bndArityGap；**E0818 分层判定**：sameType(at, face) 接口自身值直接装箱（ch11-dyn-iterator-green 的 `Dyn<Iterator<Rune>>("abc".iterator())` 经此路）→ paramRef→bndGenerDeterm → 精确 impl（im.iface 同 + sameType(im.head, at) + argsEqual(im.ifaceArgs, box.args)）→ 泛型 impl（nominalDeclName(at)==im.headName）→bndGenerDeterm（T7 unify）→ E0818 锚实参首 token exprPos）。
- **bndDyn/bndMemberSets 行删除**：常量 + 全部使用点（resolveNamed Dyn 分支→checkArity+dynFace；callType Dyn 分支→dynCall；memberType 两处→E0815 真码）。M6a 临时表余 5 行（bndWhereRules/bndGenerDeterm/bndIterCombinators/bndForRules/bndListRules + bndCollections 等 M4 遗留行）。
- **typecheck_test.go 既有探针翻转 2 处（披露，删行必然翻转——T3/T4 先例）**：`fn f(x: Dyn)` wantBnd(bndDyn)→wantDiag E0828 `"Dyn" wants 1 type argument, got 0`@1:9（裸名现过元数判定）；`fn f(x: Dyn<Int64>)` wantBnd(bndDyn)→wantDiag E0820 `"Int64" is not an interface`@1:13（实参现真解析）。
- **T1 黄金修正补全 1 处（T5 窗口披露项的 setup 半边，披露）**：check-e0819-dyn-assoc 的 `setup.files` 仍持旧源 `xs: List<Int64>`（上窗口只改了 `files` 半边——T1 生成器双处同源约定，单改致 setup 与断言源不一致、pass 2a 先撞 bndCollections 而 E0819 不可达）；setup 同步为 `s: String`，锚 2:17 与 stderr 不变。**枚举核对：其余 97 枚黄金无 setup/files 双源不一致**（抽查 e0818/e0820 等负例双处同源）。
- **bndIterCombinators 新临时行（披露）**：T6 落 String.iterator 后 `it.map(...)` 若走 E0815 会是 T8 假红错报——Iterator 面（ifaceType/dynType decl==iteratorIface）或名目头有已验证 Iterator impl（headImplementsIterator）上的组合子闭集名（map/filter/take/skip/collect/fold/reduce/count/any/all/find——iterCombinator，**forEach 刻意不在闭集**）→ bndIterCombinators（T8 落十一默认体签名时删）；其余未命中 → E0815/E0816 真码。名目路由限 headImplementsIterator，防非迭代器头（User 等）误停。
- **提前翻转 2 枚（披露，属 T8 计划面）**：check-ch11-iterator-next-green（`"ab".iterator()` 经 stringMembers 得 `Iterator<Rune>` 接口值，`it.next()` 走接口位自有方法集）、check-ch11-dyn-iterator-green（`Dyn<Iterator<Rune>>("abc".iterator())` 经 dynCall sameType(接口自身值) 路装箱）——两枚黄金的判定机制（成员解析 + Dyn 面）实为 T6 面，T8 只需补组合子签名，故因面机制提前落绿。
- **组合消息清单（新发位，披露）**：E0815 两形（接口位 `an interface position reaches its own interface's methods only` / Dyn 位 `a Dyn value reaches its own interface's methods only`，均锚成员名位，方法集渲染 `Dyn<Greeter>` 形）；E0816 forEach 专形 + tuple/fn/unit/never 通用形 `%s has no member %q`；E0817 `the parameter %q carries no where bound, and an unconstrained parameter is opaque`；E0818 `%q implements no %q; a Dyn box carries a value of an implementing type`（实参类型 × 应用后接口视图）；E0819/E0820 见上；E0105/E1004 见上。
- **验证**：`go test ./internal/typecheck/` **TestMemberSets/TestDynFace 转绿**，24 套件绿 + 4 套件各停其任务首探针边界（TestGenericDetermination :120→bndGenerDeterm、TestWhereBounds :150→bndWhereRules、TestIterProtocol :166→bndForRules、TestCollectionsFace :190→bndCollections——T7×2/T8×2）；`go test ./internal/parser/` 全绿；conformance **254→268 绿（14 翻转：12 计划 + 2 提前，双向 diff 零回归）**，余 30 红逐枚映射（T7 13：generic-fn/impl/record/nested-application 四绿 + where 三绿 + e0827×2 + e0829/e0830×2/e0831；T8 17：build-ch10-erasure + bnd-list + ch11 组合子/for 源/用户 Iterable 三绿 + ch17 三绿 + e0501×2 + e0828-list + e0901×2 + e0902/e0903/e0904 + e1501）；gofmt -l 空、`go build`/`go vet` 过（日志 /tmp/t6-typecheck.log、/tmp/t6-conformance.log、/tmp/t6-names.txt）。

- [x] T7 typecheck 泛型定出与 where：D6 subst/unify 单向统一化、显式实参形三路、E0827/E0828 新发位（既有报文锚点冻结）、实例化类别诚实（E0601/E0702 按实例化）、E0829/E0830/E0831、bound 授方法集与 iterHandle、方法泛型 U 定出
  来源：proposal 目标 3 / design D6
  验证：`go test ./internal/typecheck/` 泛型与 where 套件绿

完成记录（T7，2026-09-06）：
- **D6 全落**：统一化单向 `unifyInto(decl, arg, map)`（paramRef 未绑→绑定、已绑须 sameType；结构同形递归——named/record/newtype/iface/dyn 同头同元、元组同长、fn 型逐位+返回；不同形或已绑不一致 → 推断包装 `inferUnify` 于实参位发既有 E0501）；**被拒替代双向合一（期望型回灌）未实现**——章文明拒「Expected-type inference does not exist」，推断期实参以 nil expected 定型（`g(None)` 于 `g<T>(x: Option<T>)` 发 None 味 E0827（`"None" does not determine the type arguments of Option`）而非调用味——无黄金钉死、按章文直译，披露）；共享推断体 `inferValueArgs(shapes, values, clauseLen, failOpen)`（元数不符先停 bndArityGap；map 有未绑 → failOpen 按位发 E0827）。**四应用面接线**：fnCall（显式形 TypeArgs → resolveArgs+subst；裸形 → inferValueArgs 于调用头 exprPos(x.Fn) 发 fn 形 E0827 `…— %q determines no type argument; …explicit form %s<Int64>()`）、ctorCall（载荷定出，同报文形）、newtypeCall（底层定出）、constructType（裸泛型头 → `inferConstructArgs`：更新式 base 直接给应用（base 型须同 decl record，否则 E0603 既有报文）；全构式先 E0604 全集判定（缺字段是字段集的事实非未定出位）后按 rec.fields 声明序以字段值 unify，E0827 构造组合形锚构造头）。**methodCall 独立于 fnValueCall**：callType 尾部 Member 头 + fnContainsParam 视图分流至 methodCall（openRefs 收集签名（参数+返回）中未绑 paramRef，锚成员名位发方法形 E0827 `the call of %q determines no %q`；实参与 substMap 后参数 agree 失败 → 实参位 E0501）；fn 值调用路径保持纯位置检查（闭包 fn 型可含外围 paramRef——`|y: T| y` 于 fn<T> 内——不可误推断，D6 方法泛型限方法视图）。**显式实参形三路既有路径不动**（callType 前块元数先行、resolveArgs、subst 链）。
- **where 检查**：声明期 `checkWhereDecl(params, wheres)`（fn 于 pass 2a 签名解析后、impl 于 checkImplDecl 子句 scope 后）——subject 须为本声明子句参数（违者 **E0105 组合形** `the where subject %q is no generic parameter of the declaration…` 锚 subject 名，D6 预告的披露位）；bound ref 经 `boundIface`（镜像 dynIfaceRef 层序：qual→import bndMultiModule 否则 E1304 / typeScope 参数名→E0829 / syms symIface→checkArity+ifaceType、其他 kind→E0829 / builtinIface→命中 / 已知非接口 prelude 名（Never/Result/Option/Dyn/List/Map/Set/Shareable/基类型名）→E0829 / 真未知→E1304）非接口 → **E0829** 锚 bound 首 token；等式左侧 assoc 须为已收集 bound 接口的关联类型（否则 **E0105 组合形** 锚 `==`）；右侧 containsParam（含关联名）→ **E0831** 锚右侧首 token。使用期 `checkWhereSatisfies`（fnCall 显式/推断两路均过）：每个 bound 的主体位实参走 `implementsFace`，false → **E0830** 锚调用头（`%q implements no %q; the where bound of %q holds at every call`）。**E0818/E0830 一处实现两码**（D6 被拒替代的正面形态）：`implementsFace(at, face)` 四层——sameType（接口自身值）/ fnBounds entailment（paramRef 位：同 idx+同接口 decl+argsEqual）/ 精确 impl（im.iface 同+sameType(im.head,at)+argsEqual(im.ifaceArgs,face.args)）/ 泛型 impl（nominalDeclName(at)==im.headName → unifyInto(im.head, at) 绑定后 substMap(im.ifaceArgs) 与 face.args argsEqual）；dynCall 尾部改走此判定。
- **bound 授方法集与等式生效**：memberType 拆出 `memberOfType(t, x, asCall)`（关联位需经等式绑定再分派）；paramRef 分支——c.fnBounds 命中该 idx 的 bound 并集方法（methodUse 先行，视图 = substFn(接口方法签名, b.face.args)）；有 bound 未命中成员 → **E0815 组合形** `a where-bounded parameter reaches its bounds' method sets only`（锚成员名位）；无 bound → E0817 既有报文（T6 的「无 where 子句能存活到体检查」论证随本层落地失效，E0817 回归其字面事实——参数无 bound）。assocRef 接收者——fnBounds 等式命中（按关联名）→ 经 eq.rhs 再分派 memberOfType；未绑定 → **E0815 组合形** `an associated position reaches members through its equality binding`。fnBounds 激活：checkFnDecl（fnWheres[fd]）与 checkImplBodies（implWheres[x]）save/restore；接口默认体无 where（解析期拒绝）。
- **fn 尾 expected 传递**（T5 记录预告的 e0827-fn-empty 红因）：walkItems walkFn 模式末项 ExprStmt 且 fnRet≠nil 时以 fnRet 作 expected 传入 typeOf——`fn empty<T>() -> Option<T> { None }` 体内裸 None 经此解析（M3 既有期望型线程机制的补全，非期望型回灌——回灌被拒的是**实参→参数**方向，fn 返回位的线程是 M3 既有事实）。
- **prelude 集合名目类型**：listSum{name:"List",params:["T"]}/mapSum{name:"Map",params:["K","V"]}/setSum{name:"Set",params:["T"]} 登记（namedType decl）；resolveNamed List/Map/Set 分支由 bndCollections 改 checkArity+resolveArgs（E0828 集合元数生效——e0828-list-arity 翻转的前提）；memberOfType 名目 miss 且 collectionSum → bndStdModules（集合方法是标准库的，D7/D10——TestCollectionsFace 的 `xs.get(0)`/`xs.add(1)` 探针维持诚实边界）；dynIfaceRef List/Map/Set 分支由 bndCollections 改 E0820（集合是名目型非接口）；callType `List(…)` 调用行维持 bndCollections（集合无构造调用形，T8 随 D7 集合面处置）。**E0827 集合味**：`Some2(2)` 裸构造推断已生效（namedType sum 路径）。
- **实例化诚实（推断应用）**：checkFieldInstantiation/checkPayloadInstantiation/newtypeCall 内联检查签名由 argRefs []ast.TypeRef 改 anchors [][2]int（`typeArgAnchors`：显式子句锚各实参 token，推断应用锚应用头——定出是头的，无实参 token 存在）；E0601/E0702/E0823 报文不变。
- **bndWhereRules/bndGenerDeterm 死行删除**：常量两行 + 全部使用点（pass 2a fn where、checkImplDecl where → checkWhereDecl；fnCall/ctorCall/newtypeCall/constructType 裸泛型头 → 推断；memberType 六处 fnContainsParam→bnd → 删除（视图流入调用由 methodCall 定出）；dynCall paramRef/泛型 impl 两分支 → implementsFace；memberType assocRef → 等式绑定分派）。M6a 临时表余 3 行（bndIterCombinators/bndForRules/bndListRules，T8 删）+ bndCollections（callType 调用行，T8 删）。
- **T1 黄金修正 2 枚（实现前，披露）**：(a) **check-e0827-method-undetermined 源修正**——impl 方法体 `panic("unimplemented")` 在 List 解析落地后于 bodies 遍先停 bndTermination（impl 源序先于 fn f），E0827@11:16 不可达；接口/impl 签名 `List<T>`→`Option<T>`、体 `panic`→`None`（经 fn 尾 expected 线程解析），stderr 与锚点逐字节不变（报文与锚点不涉返回类型）；setup.files 与 files 双源同步。(b) **check-e0828-list-arity 锚点 1:28→1:14**——生成器误锚子句闭口 `>`；批准约定（design「anchored at the application's own `<` token」+ 全部姊妹黄金：Result@1:17、Option@1:14、M5 翻转的 Dyn 探针）锚 `<`。两处 m6a_test.go 镜像同步。
- **T6 记录勘误（披露）**：T6 完成记录将 check-bnd-list 与 check-e0828-list-arity 映射至 T8——实际两者随 T7 的 List 类型化翻转（前者是 T1 预告改写黄金的兑现、后者元数判定属泛型应用面）；T8 余 15 枚（17−2）。
- **typecheck_test.go 既有探针翻转 2 处（披露，删行必然翻转——T3/T4/T6 先例）**：:289-290 `let xs: List<Int64> = 1` / `let m: Map<String,Int64> = 1` wantBnd(bndCollections)→wantDiag E0501 `the expression is Int64, the annotation is List<Int64>`@2:9（注解位类型化）。
- **组合消息清单（新发位，披露）**：E0105 两形（where subject / 等式左侧关联名）；E0815 两形（where-bounded 参数越集 / 关联位未等式绑定）；E0827 三形（fn/构造组合 `— the construction of %q determines no type argument; …%s<Int64> { ... }`、构造组合、既有 fn 形）；E0501 推断位（报文渲染声明形——`the argument is %s, the parameter is %s` 中 parameter 为含 paramRef 的声明形）。
- **已知边界披露**：(a) 推断期实参无 expected 线程（见上 None 味 E0827）；(b) fnValueCall 不推断（外围 paramRef 语义，D6 方法泛型限方法视图）；(c) **impl 自身 where 的应用点满足判定未实现**——D6 where 使用期文本限「被应用的声明」（fn 调用/构造/实例化），impl where 的构造点满足（`Wrap<User>` 构造时 impl<T> … where T: Eq 的 User: Eq）与 implementsFace 泛型 impl 分支的 where 前置无 D6 明文、无黄金钉死，M6b 候选（声明验证+体授集已落）；(d) constructType 更新式 base 双重定型（推断期 nil + 后续 expected 位）——无副作用（capture ledger 有-if 卫），无黄金面。
- **验证**：`go test ./internal/typecheck/` **TestGenericDetermination/TestWhereBounds 转绿**，26 套件绿 + 2 套件停 T8 首探针边界（TestIterProtocol :166→bndForRules、TestCollectionsFace :190→bndListRules）；`go test ./internal/parser/` 全绿；conformance **268→283 绿（15 翻转：T7 13——generic-fn/impl/record/nested-application 四绿 + where 三绿 + e0827×2 + e0829 + e0830×2 + e0831；+ check-bnd-list（T1 改写黄金兑现）+ check-e0828-list-arity（T6 记录勘误归 T7），双向 diff 零回归**，余 15 红全 T8（build-ch10-erasure + ch11 三绿五负 + ch17 三绿一负 + e0501×2 + e1501）；gofmt -l 空、`go build`/`go vet` 过（日志 /tmp/t7-conformance.log、/tmp/t7-names.txt）。

- [x] T8 typecheck 协议/集合/for + codegen：D9 for 协议（E0901、元素模式绑定）、Range 定型（E0902/E0501 混界）、E0903/E0904、列表字面量（E0501 链/E1501 空字面量期望型）、集合型元数 E0828 扩发、D11 codegen interface/impl 声明擦除、bndCollections/bndRange 行删除与 bndStdModules 文本核对
  来源：proposal 目标 4 5 6 / design D9 D11
  验证：`go test ./internal/typecheck/ ./internal/codegen/` 绿；`go test ./...` 全绿（conformance 298/298——T1 预估 ~301 为估计值，实际总数 298（T7 记录勘误 17→15 已对账））

完成记录（T8，2026-09-06）：
- **D9 for 协议**：`checkFor`（元素判定 → 独立 locals 层 `bindForPattern` → 体 walkControl 丢弃尾（E0605 控制形态）→ 弹层）；`forElem` 定型 iterable 式（nil expected）后走 `iterElemOf` 四层——**内建源**（String→Rune；Range/List/Set→args[0]；Map→tupleType(args) 即 (K,V) 入目元组）/ **用户精确 impl**（im.iface==iterableIface && sameType(im.head,t) → ifaceArgs[0]）/ **泛型 impl**（unifyInto(im.head,t) 绑定后 substMap(ifaceArgs[0])，含参位（定出不完全）不取）/ **paramRef fnBounds**（bound iterableIface → face.args[0]）；不中 → **E0901 两形**锚 iterable 式首 token（exprPos）：非 Iterable 形 `…— %q implements no %q; the iterable expression of a for implements Iterable`，裸迭代器形（`isIteratorValue`：ifaceType/dynType decl==iteratorIface、paramRef bound iteratorIface、名目头 headImplementsIterator）`…— a bare iterator is consumed by explicit next calls; a for iterates an "Iterable" and this expression yields an "Iterator" alone`。`bindForPattern`：PatTuple 对非元组/异长元素 → **E0501 专形** `the loop head pattern is a %d-tuple, the element type is %q` 锚模式首 token（解析期 forHead 限定 irrefutable——绑定/通配/元组，故 PatLiteral/PatVariant 不可达）；PatBinding 排除 `_` 入循环层。
- **Range 定型**：`rangeType`（typeOf `..` 分流）——双侧定型后逐操作数 `isInt` 判定，违者 **E0902** `the operand %q is %q`（标签：Literal 取其源文本，非字面量回退类型渲染——无黄金钉死后者）锚违界操作数首 token；两侧整型异型 → **E0501** `operands of ".." are X and Y` 锚 `..`（binaryType 惯例——Binary.Line/Col 即操作符位）；返回 `Range<T>`（rangeSum byval）。**判定序披露**：先逐操作数整型后同型——`1.5..2i32` 报左操作数 E0902（无黄金钉死该角）。
- **E0903/E0904**（impl 声明期，E0807 块后、方法解析前，锚 impl 头）：inf==iterableIface 时 catOf(head)=="resource" → **E0903**；assocs["Iter"] 存在且 implementsFace(iter, Iterator<ifaceArgs>) 为假 → **E0904**（一机制两码——implementsFace 复用）。
- **列表字面量**：`listLitType`——空 + 期望 List<X> → 整体取期望应用；空无期望 → **E1501** 锚 `[`；非空首元定准（nil expected）、后续 sameType 失败 → **E0501 专形** `the elements are X and Y`（首元型 × 违例元型）锚违例元素首 token；返回 `List<T>`。
- **组合子默认体（十一签名 ch11 逐字）**：init() 重写 iteratorIface.methods = next + map/filter/take/skip（lazy，返回 Dyn<Iterator<…>>）+ collect/fold/reduce/count/any/all/find（eager）——map/fold 另携 typeParams ["U"]（子句位 idx1，fresh 对接口 <T>）；全部 recvMut: true。**defaulted 标记机制（披露）**：`body: &ast.Block{}` 作非 nil 标记——E0807 完整性（`im.body != nil || defines(im.name)`）与继承默认注册（`im.body == nil || defines(im.name)` 均读 body 非空为「已默认」）随标记自动放行；**真实体是 M8 标准库的**，内建接口无 AST 声明故 checkIfaceBodies 不走它们（无体遍历副作用）。用户 impl Iterator 只须定义 next（黄金 user-iterable/combinators 实证）；覆写须逐字重复子句（E0808 既有）。
- **方法泛型期望线程与位置同一性（机制披露，两条）**：(a) methodCall 实参定型由 nil expected 改 `typeOf(a, ft.params[i])`——**声明位线程为期望、方法子句位保持符号**：裸参闭包从声明位取参型（`|r| r` 于 map 得 r: T 实参化 Rune），返回型仍由统一化定 U（ch11「U comes from the call's own text」）；与 inferValueArgs 的 nil-expected（fn/构造/包装路径）**不对称**——方法视图独有线程，披露。closureType 注释参 vs 期望位 agree 加 `!containsParam(et.params[i])` 卫（符号位不可 disagree——注释本身是定出候选）。(b) **unifyInto 位置同一性规则 + 事后核对双侧代换**（实现期发现，无黄金曾钉死旧行为）：`fold(0, |acc, r| acc)` 的闭包视图携 U@1 位再统一撞既有绑定 m[1]=Int64 → 旧 sameType(Int64, U@1)=false 误报 E0501——修复：再绑定时实参为**同 idx+同名** paramRef（即被定出的那个位自身）视为一致（位置与其自身绑定一致，物化即定出结果）；methodCall 事后 agree 由 `agree(argTypes[i], want)` 改 `agree(substMap(argTypes[i], bindings), want)`（双侧代换后同类比较）。**跨子句空间同 idx 撞名**（外围与被调子句同名同 idx 且混具体/位实参）下 (b) 可能漏报于统一化、由 checkArgs/fnValueCall 位置检查兜底——paramRef idx 空间按声明局部是 M6a 既有事实（sameType 比较 idx+name），M6b 候选（同「精确 impl 覆写带子句默认的指标空间错位」条）。
- **集合锚定族**：stringMembers 扩六成员（iterator→Iterator<Rune>、byteLength/byteSlice/runeCount/charAt）+ `collectionMembers`（List.get(Int64)->Option<T>/iterator；Map.get(K)->Option<V>/iterator→Iterator<(K,V)>；Set.has(T)->Bool/iterator）——**iterator 返回接口值形态（判断，披露）**：ch11 列集合为 iterables + ch17「iteration」列锚定 → 集合 iterator 与 String 同构（T6 先例）返回 `Iterator<elem>` 接口值（Map 元素为 (K,V) 元组）；句柄类型本身是标准库的。其余成员 bndStdModules（D10）。resolveNamed 增 Range 分支（checkArity + resolveArgs，E0828 生效）；nameResolvable/boundIface E0829 名单/dynIfaceRef E0820 名单三处 + "Range"。
- **边界行删除与收窄**：删 bndCollections/bndRange/bndForRules/bndListRules/bndIterCombinators 五常量行与全部使用点（M6a 临时表清空）；iterCombinator/combinatorBnd 函数删除（headImplementsIterator 保留归 isIteratorValue）；callType `List(…)`/`Map(…)`/`Set(…)` 调用行由 bndCollections **收窄为 bndCalleeGap**（集合名是类型非构造器——字面量建 List、标准库建其余，镜像 callType symType default 行的裁定）。**bndStdModules 文本核对：不变**（集合名目 miss 后停此行，M5 原文）。
- **D11 codegen 擦除**：Emit 首遍增显式 `case *ast.InterfaceDecl, *ast.ImplDecl:` continue（注释声明级事实零 IR——D11；显式 case 非 Go type switch 静默落空，未来形式到此必须决策不许消失）。build-ch10-erasure 现端到端 build+run 成功（接口/impl 声明擦除后骨架模块照常出码）。
- **T1 黄金修正 1 枚（实现前，披露）**：**build-ch10-erasure 顶层 `files` 键删除**——T1 生成器对 build 形误加全树比对键（产物含 .ll/.o 不可钉），build 形黄金镜像 M4 build-record-erasure 无 files 键（runner `want.Files != nil` 触发全树比对）；setup.files 不变，exit/stdout/stderr 断言不变。
- **typecheck_test.go 既有探针翻转 1 处（披露，删行必然翻转——T3/T4/T6/T7 先例）**：:164 `let x = 1..2` wantBnd(bndRange)→wantOK（Range 定型落地，M5 探针退役）。
- **组合消息清单（新发位，披露）**：E0501 列表元素形 `the elements are X and Y`（无黄金钉死——红名单 15 枚不含该角，注册表报文按 spec 组合）；E0501 循环头模式形（钉死 e0501-for-tuple-head）；E0901 两形/E0902/E0903/E0904/E1501 全句逐字钉死。
- **已知边界与不可达披露**：(a) Range 裸注解应用不查整型（`let r: Range<Float64>` 过——E0902 批准触发位是操作数，注解位无批准触发；无黄金钉死）；(b) 空列表字面量对非 List 期望（`let x: Int64 = []`）走 E1501 通用形（报文措辞按无期望——注册表无该角批准形）；(c) ifaceType/dynType{iterableIface} 不入 iterElemOf——值位不可达（E0821 拒注解、E0819 拒 box 携带）；(d) E1402（fn 型实参纯度）/E1404/E0810 维持 D10 不可达——M6a 无效果段，组合子 fn 型实参纯度恒真；(e) E0501 元素链报文渲染（首元型在前）无黄金钉死。
- **验证**：`gofmt -l` 空、`go build ./...`、`go vet ./...` 过；`go test ./internal/typecheck/` **30 套件全绿**（TestIterProtocol/TestCollectionsFace 由 T8 首探针边界停转绿）；`go test ./internal/parser/`、`./internal/codegen/`、`./internal/cli/` 绿；conformance **283→298/298 全绿（15 翻转零回归：build-ch10-erasure + ch11 三绿（combinators/for-sources/user-iterable）+ ch17 三绿（collections/list-expected/map-iteration）+ e0501-for-tuple-head + e0501-range-mixed + e0901×2 + e0902 + e0903 + e0904 + e1501；对照 /tmp/t7-names.txt 逐枚确认在场，双向零回归）**；既有 M0–M5 黄金仅 check-bnd-list（T1 改写、T7 记录已披露）与 e0827-method-undetermined/e0828-list-arity 源修正（T1 已披露）触及，无新改写（日志 /tmp/t8-all.txt）。


- [x] T9 全量验证与真机电池：`go test ./...` 全绿（缓存清空重跑，既有 201 枚零改写核对）；真机黑盒——接口+impl+泛型+组合子链+for+derives+Dyn 综合绿程序 check 静默、逐码负例真机报文核对（34 首发码全覆盖）、新形式 main 体 build 停 bndMainBody 边界 exit 70、interface/impl 声明擦除程序 build+run 端到端；D10 不可达清单逐条实证记录
  来源：proposal 目标 6 8 / design D10 D11 D14
  验证：输出与不可达实证记于完成记录

完成记录（T9，2026-09-06）：
- **全量重跑**：`go clean -testcache && go test ./...` 全绿（conformance 298/298、typecheck 30 套件、parser/codegen/cli、cmd）。**既有 201 枚零改写核对**：`git status` 黄金目录中 M0–M5 文件唯 check-bnd-list（T1 改写、T7 记录披露）与 e0827-method-undetermined/e0828-list-arity（T1 修正、T5/T7 记录披露）触及——无新改写。
- **真机扫 298/298**：脚本 /tmp/t9-sweep.py（遍 cases/*.json，临时目录写 setup.files+**setup.dirs**——new-exists 黄金需空目录占位，首轮漏 dirs 致 `we new` 在空目录成功 exit 0 ≠ 黄金 2，补齐后单例复验 → 全过），cwd 临时目录跑 /tmp/we-m6a（`go build` 自源码，we 0.1.0 / spec 0.9.0），exit/stdout/stderr 三通道逐字节比对。**298/298 全过**——含全部 97 枚 M6a 新黄金，即 33 枚首发码的真机报文逐字节核对。
- **34 首发码全覆盖（脚本核对）**：E0801–E0831∪E0901–E0904∪E1501∪E1004 − 3 已在发（E0816/E0827/E0828）= 34；黄金 stderr 码抽取核对——**33 枚有黄金负例**（经真机扫覆盖），**E0810 设计性缺席**（判定实现、单模块不可达、永不发射——D10 实证见下）。
- **综合绿程序真机 check 静默**（/tmp/t9-green）：CountIter+Iterator impl（只须 next——组合子默认体在）、Range2+Iterable impl 带关联 Iter 绑定、Pair\<T\> derives Eq/Hash/Show、pick\<T\> where T: Eq、for×5 源（String/Range/List/用户 Iterable/Map 元组头 (k,v)）、组合子链 count/map+collect/fold/find、Dyn\<Iterator\<Rune\> \>装箱+裸 next、Map/Set 参数位 get/has/迭代、main = return Ok(())——`we check src/main.we` exit 0 静默。**四处语言事实（源级修正，非产品缺陷）**：derives 语法为裸名逗号形 `derives Eq, Hash, Show`（括号形 E0105）；Map/Set 是类型名非值产生式——参数位合法、`let mm = Map<...>` 值位 E0105；for 头直接写构造式与体块 `{` 贪心冲突——先 `let r2 = Range2 { n: 0 }` 再迭代（黄金 user-iterable 同形先例）；嵌套角括号须空格分隔 `Dyn<Iterator<Rune> >`（`>>` 词法贪心为移位符，E0104 链报）。
- **manifest 事实**：we.toml 需 `[project]` name/version/type 三键；type 合法值 executable/library（`"bin"` → E1903）；build/run 形为项目根 `we build .`（单文件路径停 bndSingleFile）。
- **bndMainBody 边界实证**（/tmp/t9-bnd）：main 体含方法调用（`"abc".iterator().count()`）——check 过、`we build .` 停 `we: main bodies beyond a single Ok or Err return statement are not implemented in this reference build yet` exit 70 ✓（D11 预告的 bndMainBody 兜底）。
- **bndOtherFns 诚实边界（披露）**：综合绿程序含辅助 fn（mainFn/mapFace/pick）→ build 停 `functions other than main in code generation…` exit 70——M4 受纳集只收 main + 单条 Ok/Err 返回，辅助 fn 不入。这不是缺陷而是受纳集边界；D11 的擦除验证因此取**最小纯声明形**。
- **interface/impl 擦除 build+run 端到端**（/tmp/t9-erasure）：Iterator impl + 自定义接口 Speak+impl + 泛型 record Pair\<T\> derives + newtype 声明 + main = return Ok(())（无辅助 fn）——`we check` exit 0 → `we build .` exit 0 → `we run .` exit 0 全链通过；较 build-ch10-erasure 黄金（真机扫已证 exit 0）多覆盖泛型 record/derives/newtype 擦除共存。
- **D10 不可达清单逐条真机实证**：(1) **E1402/E1404**——效果段语法 M7 前不可解析：fn 声明段（`fn save(msg: String) effect io {}`）与 impl 方法段（`fn hello(self) effect io -> String {}`）双探针均停 `chapter 16 (effects) forms are not implemented…` exit 70——效果集恒空 ⇒ 空对空一致/空⊆空恒真，两码不可达；(2) **E0810**——单模块孤儿不可达：接口未声明先停 E1304@2:6（`"Foreign" names no declared interface`），头类型同理论（typeScope/syms 本模块解析）——孤儿条件（接口或头来自他模块）不可满足，判定在而永不发；(3) **E0809 跨模块面**——三路全堵：`import std` 停 bndStdModules exit 70、`import util` 单文件停 E1302（无 source root）、项目模式多源文件（src/main.we+src/util.we）停 `multi-module programs (chapter 15)…` exit 70——跨模块重叠形 M6b；(4) **E0821/Dyn 遮蔽**——黄金 check-e0828-dyn-shadow 在 298 扫集真机过；(5) **方法显式实参形**——check-e0104-method-generic-args 在扫集（`obj.m<T>(x)` 归比较链 E0104）；(6) **forEach 不存在**——check-e0816-for-each 在扫集（序列效应属 for 语句）；(7) **follow-up #7（赋值目标可变性）/#8（E0604 陈旧子句）维持登记不动**；(8) **bndStdModules 文本核对**——非锚定 stdlib 成员 `"abc".toUpper()` 停 M5 原文 exit 70，What 文本逐字不变（收窄仅可达面）。

- [x] T10 验证阶梯 + 实现审查（welang-code-review 7 条，真二进制证据）+ 归档（status → complete，归档目录 2026-09-05-generics-iterables-collections）+ roadmap M6 行拆为 M6a（done）/M6b（pending）双语同步
  来源：研发流程 + design D14 披露义务
  验证：gofmt -l 空、go build/vet 过、validate --all --strict 过、docs_sync 对数不变（30）、git diff --check 干净；归档目录存在

- [ ] T11 记忆更新与提交：project-overview 增 M6a 条目（含 M6 拆分与 M6b 后继）、MEMORY.md 索引行更新；英文提交信息（无署名 trailer；refr/ 不入提交）
  来源：研发流程 + 用户红线
  验证：`git log -1` 消息为英文且无 Co-Authored-By；`git show --stat` 无 refr/ 路径

完成记录（T10，2026-09-06）：
- **welang-code-review 7 条全过**（结论全文见 proposal.md「## 审查记录」）：规范符合性（ch11 十一组合子签名逐字对照 checker init 表、E0807 报文注册表 title 起头、黄金族钉死）/ 验证诚实性（T1–T9 逐项复核 + 本关最小集重跑）/ 测试先行（red 日志在场、97 枚黄金 §52 格式正则全量核对零违规）/ 诊断协议（internal/diag 零触及、零新增码、--json 字段集不动）/ 单一权威（注释纯英文、零规范增量无提升面、工件语言合规）/ 红线（refr/ 未入树、提交按无署名执行）/ 最小可信验证（gofmt 空、build/vet 过、validate --all --strict OK、docs_sync 30 对齐、git diff --check 干净）。
- **归档**：`git status` 下变更目录为 untracked，`mv openspec/changes/generics-iterables-collections openspec/changes/archive/2026-09-06-generics-iterables-collections`（目录日期前缀取归档执行日——任务行预估 2026-09-05 为撰写时估计，修正为 2026-09-06 披露）；change.yaml status active → archived（M5 先例）；零规范增量故无 specs/ 提升面（规范先行，长期事实已在 docs/spec/）；design 取舍不提炼 ADR——按 M4/M5 先例（docs/decisions/ 仅原则级 ADR-0001/0002，实现型裁决留 design 归档）。
- **roadmap M6 拆分双语同步**：英文权威表与中文表同轮改——M6a `generics-iterables-collections`（ch10/ch11/ch5 for/ch17）done 2026-09-06；M6b `modules-errors`（ch13/ch14 全量/ch15 模块解析全量）pending；原单行 M6 `modules-generics-errors` 移除。
- **复验**：`python3 openspec/tools/validate.py --all --strict` OK（no changes found; registry clean——唯一 active 已归档，符合预期）；`python3 openspec/tools/docs_sync.py` 30 对齐（roadmap 双语同步后对数不变）。
