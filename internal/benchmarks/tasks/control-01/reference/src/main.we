pub type AppError = Failed(String)

pub fn seriesSum(n: Int64) -> Int64 {
    var total: Int64 = 0
    var i: Int64 = 1
    while i <= n {
        total = total + i
        i = i + 1
    }
    return total
}

pub fn main() -> Result<(), AppError> {
    return Ok(())
}
