pub type AppError = Failed(String)

pub fn parityClass(n: Int64) -> Int64 {
    var m: Int64 = n
    while m >= 2 {
        m = m - 2
    }
    return m
}

pub fn main() -> Result<(), AppError> {
    return Ok(())
}
