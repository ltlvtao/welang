{
  "name": "check-e0816-record-unknown-member",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record User { name: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\" }\n    let n = u.email\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:7:15: error[E0816]: no such member on the receiver's type — \"User\" has no member \"email\"; member access names a field or a method of the receiver's type\n",
  "files": {
    "demo/main.we": "record User { name: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\" }\n    let n = u.email\n    return Ok(())\n}"
  }
}
