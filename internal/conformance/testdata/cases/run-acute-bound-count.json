{
  "name": "run-acute-bound-count",
  "args": [
    "run",
    "."
  ],
  "setup": {
    "files": {
      "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\ntype = \"executable\"\n",
      "src/main.we": "import std.io\n\npub type AppError = Failed(String)\n\npub fn main() effect io -> Result<(), AppError> {\n    let xs = [1, 2, 3]\n    let it = xs.iterator().map(|x| x * 2)\n    let c = it.count()\n    if c == 3 { io.println(\"count-ok\") } else { io.println(\"count-bad\") }\n\n    return Ok(())\n}\n"
    }
  },
  "exit": 0,
  "stdout": "count-ok\n",
  "stderr": ""
}
