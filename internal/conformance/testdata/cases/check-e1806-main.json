{
  "name": "check-e1806-main",
  "args": [
    "check",
    "src/main.we"
  ],
  "setup": {
    "files": {
      "src/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    advanceTime(5)\n    return Ok(())\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "src/main.we:4:5: error[E1806]: advanceTime called outside a test block — the virtual clock is bound to test blocks; advance the clock inside the test that observes it\n",
  "files": {
    "src/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    advanceTime(5)\n    return Ok(())\n}\n"
  }
}
