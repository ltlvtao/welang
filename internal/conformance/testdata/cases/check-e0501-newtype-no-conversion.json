{
  "name": "check-e0501-newtype-no-conversion",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "newtype UserId(Int64)\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let id = UserId(42)\n    let n: Int64 = id\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:7:9: error[E0501]: mixed types — the expression is UserId, the annotation is Int64; no coercion is ever inserted\n",
  "files": {
    "demo/main.we": "newtype UserId(Int64)\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let id = UserId(42)\n    let n: Int64 = id\n    return Ok(())\n}"
  }
}
