{
  "name": "check-e1001-bare-no-expectation",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let f = |x| x\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:4:13: error[E1001]: bare-parameter closure without an expected function type — the parameters are bare and no explicit function type is expected at this binding; annotate the parameters or bind under a function-type annotation\n",
  "files": {
    "demo/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let f = |x| x\n    return Ok(())\n}"
  }
}
