{
  "name": "check-ffi-e1705-never",
  "args": [
    "check",
    "."
  ],
  "setup": {
    "files": {
      "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\ntype = \"executable\"\n",
      "src/main.we": "foreign \"c\" {\n    fn poison(x: Never) effect io -> Int64\n}\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "src/main.we:2:18: error[E1705]: foreign function signature holds a type outside the crossing set — parameter \"x\" holds Never, which does not cross; Never is a declared return position only\n"
}
