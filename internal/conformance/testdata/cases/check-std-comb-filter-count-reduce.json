{
  "name": "check-std-comb-filter-count-reduce",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "fn work(xs: List<Int64>) {\n    let ys = xs.iterator().filter(|x| x > 1).collect()\n    let n = xs.iterator().count()\n    let red = xs.iterator().reduce(|a, b| a + b)\n    let _ = ys\n    let _ = n\n    let _ = red\n    return\n}\n"
    }
  },
  "exit": 0,
  "stdout": "",
  "stderr": "",
  "files": {
    "demo/main.we": "fn work(xs: List<Int64>) {\n    let ys = xs.iterator().filter(|x| x > 1).collect()\n    let n = xs.iterator().count()\n    let red = xs.iterator().reduce(|a, b| a + b)\n    let _ = ys\n    let _ = n\n    let _ = red\n    return\n}\n"
  }
}
