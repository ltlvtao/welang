{
  "name": "check-e1905-missing-key",
  "args": [
    "check",
    "."
  ],
  "setup": {
    "files": {
      "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\n",
      "src/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "we.toml:1:1: error[E1905]: project manifest missing or incomplete — the manifest's \"type\" key is missing; create we.toml with the name, version, and type keys, or run we new to write the skeleton\n",
  "files": {
    "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\n",
    "src/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}\n"
  }
}
