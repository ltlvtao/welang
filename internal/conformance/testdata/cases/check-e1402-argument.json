{
  "name": "check-e1402-argument",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "effect db\n\nfn save(s: String) effect db {\n    return\n}\n\nfn pure(f: fn(String) -> ()) {\n    return\n}\n\nfn work() {\n    pure(|s: String| { save(s) })\n    return\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:12:10: error[E1402]: function value effect set does not match the expected type's — the value performs effect \"db\" and the expected type's effect segment does not include it; widen the expected type's effect segment to cover the value's set, or supply a value that performs less\n",
  "files": {
    "demo/main.we": "effect db\n\nfn save(s: String) effect db {\n    return\n}\n\nfn pure(f: fn(String) -> ()) {\n    return\n}\n\nfn work() {\n    pure(|s: String| { save(s) })\n    return\n}\n"
  }
}
