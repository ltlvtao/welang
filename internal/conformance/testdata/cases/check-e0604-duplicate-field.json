{
  "name": "check-e0604-duplicate-field",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record User { name: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\", name: \"b\" }\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:6:31: error[E0604]: field set does not match the record — \"User\" names the field \"name\" twice; a full construction names every declared field exactly once\n",
  "files": {
    "demo/main.we": "record User { name: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\", name: \"b\" }\n    return Ok(())\n}"
  }
}
