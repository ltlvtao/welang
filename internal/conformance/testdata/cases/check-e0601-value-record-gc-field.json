{
  "name": "check-e0601-value-record-gc-field",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record Inner { name: String }\n\nbyval record Outer { inner: Inner }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:3:29: error[E0601]: value record field is not of the value category or a base type — the field \"inner\" is \"Inner\", a gc record; a copy is only honest when everything in it is copyable by value\n",
  "files": {
    "demo/main.we": "record Inner { name: String }\n\nbyval record Outer { inner: Inner }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}"
  }
}
