{
  "name": "check-ffi-cross-module",
  "args": [
    "check",
    "."
  ],
  "setup": {
    "files": {
      "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\ntype = \"executable\"\n",
      "src/main.we": "import net\n\npub type AppError = Failed(String)\n\nforeign \"c\" {\n    fn probe(s: net.Socket) effect -> Bool\n}\n\npub fn main() effect io -> Result<(), AppError> {\n    let sock = net.socket()\n    let ok = probe(sock)\n    let _ = ok\n    return Ok(())\n}\n",
      "src/net.we": "foreign \"c\" {\n    pub record Socket { }\n    pub fn socket() effect io -> Socket\n}\n"
    }
  },
  "exit": 0,
  "stdout": "",
  "stderr": ""
}
