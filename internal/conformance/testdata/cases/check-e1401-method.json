{
  "name": "check-e1401-method",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record FileClient { fd: Int64 }\n\ninterface Client {\n    fn send(self, s: String) effect net\n}\n\nimpl Client for FileClient {\n    fn send(self, s: String) effect net {\n        return\n    }\n}\n\nfn work(c: FileClient) {\n    c.send(\"x\")\n    return\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:14:5: error[E1401]: undeclared effect at a call — \"send\" performs effect \"net\" which \"work\" does not declare; add the missing tag to the enclosing declaration's effect segment, or call a pure function instead\n",
  "files": {
    "demo/main.we": "record FileClient { fd: Int64 }\n\ninterface Client {\n    fn send(self, s: String) effect net\n}\n\nimpl Client for FileClient {\n    fn send(self, s: String) effect net {\n        return\n    }\n}\n\nfn work(c: FileClient) {\n    c.send(\"x\")\n    return\n}\n"
  }
}
