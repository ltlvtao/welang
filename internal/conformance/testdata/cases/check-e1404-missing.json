{
  "name": "check-e1404-missing",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "effect db\n\ninterface Store {\n    fn put(self, s: String) effect db\n}\n\nrecord Rec { fd: Int64 }\n\nimpl Store for Rec {\n    fn put(self, s: String) {\n        return\n    }\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:10:8: error[E1404]: impl method effect set disagrees with the interface — the impl method declares no effect segment and the interface method declares \"db\"; copy the interface method's effect segment into the impl method's signature exactly, in both directions\n",
  "files": {
    "demo/main.we": "effect db\n\ninterface Store {\n    fn put(self, s: String) effect db\n}\n\nrecord Rec { fd: Int64 }\n\nimpl Store for Rec {\n    fn put(self, s: String) {\n        return\n    }\n}\n"
  }
}
