{
  "name": "clean-file-usage",
  "args": [
    "clean",
    "main.we"
  ],
  "setup": {
    "files": {
      "main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}\n"
    }
  },
  "exit": 2,
  "stdout": "",
  "stderr": "we: clean wants a project directory, got \"main.we\"\nusage: we <subcommand> [path] [options]\nsubcommands: new build check run test fmt vet doc clean version lsp\noptions: --json --color auto|always|never --verbose\n",
  "files": {
    "main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    return Ok(())\n}\n"
  }
}
