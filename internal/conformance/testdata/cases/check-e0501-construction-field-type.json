{
  "name": "check-e0501-construction-field-type",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record User { name: String, age: Int64 }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\", age: true }\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:6:31: error[E0501]: mixed types — the field \"age\" is Bool, the declared field is Int64; no coercion is ever inserted\n",
  "files": {
    "demo/main.we": "record User { name: String, age: Int64 }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\", age: true }\n    return Ok(())\n}"
  }
}
