{
  "name": "check-e1405-multi-tag",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "effect db\n\nfn save(s: String) effect db net {\n    return\n}\n\nlet base = save(\"x\")\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:7:12: error[E1405]: effectful call in a top-level initializer — the initializer of \"base\" calls \"save\", which performs effect \"db\", \"net\"; move the work into main, or initialize from a pure computation over constants\n",
  "files": {
    "demo/main.we": "effect db\n\nfn save(s: String) effect db net {\n    return\n}\n\nlet base = save(\"x\")\n"
  }
}
