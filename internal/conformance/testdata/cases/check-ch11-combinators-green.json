{
  "name": "check-ch11-combinators-green",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "pub type AppError = Failed(String)\npub fn main() -> Result<(), AppError> {\n    let mapped = \"abc\".iterator().map(|r| r).collect()\n    let total = \"ab\".iterator().fold(0, |acc, r| acc)\n    let n = \"abc\".iterator().count()\n    return Ok(())\n}\n"
    }
  },
  "exit": 0,
  "stdout": "",
  "stderr": "",
  "files": {
    "demo/main.we": "pub type AppError = Failed(String)\npub fn main() -> Result<(), AppError> {\n    let mapped = \"abc\".iterator().map(|r| r).collect()\n    let total = \"ab\".iterator().fold(0, |acc, r| acc)\n    let n = \"abc\".iterator().count()\n    return Ok(())\n}\n"
  }
}
