{
  "name": "check-ffi-byres-e1101",
  "args": [
    "check",
    "."
  ],
  "setup": {
    "files": {
      "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\ntype = \"executable\"\n",
      "src/main.we": "foreign \"c\" {\n    byres record File { }\n    fn open(path: String) effect io -> File\n}\n\npub type AppError = Failed(String)\n\npub fn main() effect io -> Result<(), AppError> {\n    return Ok(())\n}\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "src/main.we:2:18: error[E1101]: resource record must implement Releasable — \"File\" declares no impl of Releasable in its own module; add impl Releasable for File { fn release(mut self) { ... } } in the record's module, or declare a gc or value record if it owns no resource\n"
}
