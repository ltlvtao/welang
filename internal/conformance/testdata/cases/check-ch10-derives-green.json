{
  "name": "check-ch10-derives-green",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record Point { x: Int64, y: Int64 } derives Eq, Hash, Show\npub type AppError = Failed(String)\npub fn main() -> Result<(), AppError> {\n    let p = Point { x: 1, y: 2 }\n    let same = p.equals(Point { x: 1, y: 2 })\n    let h = p.hash()\n    let text = p.toDebugString()\n    return Ok(())\n}\n"
    }
  },
  "exit": 0,
  "stdout": "",
  "stderr": "",
  "files": {
    "demo/main.we": "record Point { x: Int64, y: Int64 } derives Eq, Hash, Show\npub type AppError = Failed(String)\npub fn main() -> Result<(), AppError> {\n    let p = Point { x: 1, y: 2 }\n    let same = p.equals(Point { x: 1, y: 2 })\n    let h = p.hash()\n    let text = p.toDebugString()\n    return Ok(())\n}\n"
  }
}
