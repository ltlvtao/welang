{
  "name": "check-conc-advancetime-bnd",
  "args": [
    "check",
    "."
  ],
  "setup": {
    "files": {
      "src/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let t = advanceTime(5)\n    let _ = t\n    return Ok(())\n}\n",
      "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\ntype = \"executable\"\n"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "src/main.we:4:13: error[E1806]: advanceTime called outside a test block — the virtual clock is bound to test blocks; advance the clock inside the test that observes it\n",
  "files": {
    "src/main.we": "pub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let t = advanceTime(5)\n    let _ = t\n    return Ok(())\n}\n",
    "we.toml": "name = \"demo\"\nversion = \"0.1.0\"\ntype = \"executable\"\n"
  }
}
