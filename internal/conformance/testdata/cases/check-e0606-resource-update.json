{
  "name": "check-e0606-resource-update",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "byres record Conn { host: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let c = Conn { host: \"h\" }\n    let d = Conn { host: \"x\" with &c }\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:7:13: error[E0606]: update expression on a resource record — the head \"Conn\" names a byres record; a resource has identity, and copying its fields would duplicate the handle\n",
  "files": {
    "demo/main.we": "byres record Conn { host: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let c = Conn { host: \"h\" }\n    let d = Conn { host: \"x\" with &c }\n    return Ok(())\n}"
  }
}
