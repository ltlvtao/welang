{
  "name": "check-e0603-update-base-mismatch",
  "args": [
    "check",
    "demo/main.we"
  ],
  "setup": {
    "files": {
      "demo/main.we": "record User { name: String }\n\nrecord Admin { name: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\" }\n    let v = Admin { name: \"b\" with &u }\n    return Ok(())\n}"
    }
  },
  "exit": 1,
  "stdout": "",
  "stderr": "demo/main.we:9:37: error[E0603]: construction or update head or base is not the record type — the update base is \"User\", the head names \"Admin\"; an update base has exactly the head's record type\n",
  "files": {
    "demo/main.we": "record User { name: String }\n\nrecord Admin { name: String }\n\npub type AppError = Failed(String)\n\npub fn main() -> Result<(), AppError> {\n    let u = User { name: \"a\" }\n    let v = Admin { name: \"b\" with &u }\n    return Ok(())\n}"
  }
}
